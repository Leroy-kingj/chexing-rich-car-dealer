# 2026-07-23 工作日志

## 车行项目（顺联车行）HTML/JS 游戏 — 修复"运行不了"崩溃

用户报告游戏无法运行/报错。定位并修复了一组真实 bug（非语法噪声）：

1. **启动崩溃 `D.logos is undefined`**：`game_data.js` 只导出 `GAME_DATA.cars`（含 brand/logo 字段），
   `app.js` 的 `logoImg(brand)` 却读 `D.logos[brand]`（不存在的 brand→logo 映射）。
   修复：启动时从 `cars` 构建 `D.logos` 映射。
2. **`CAR` 下标错位**：原 `const CAR = D.cars`（数组），但全代码用 `CAR[carId]` 当 id 字典查，
   数组下标会错位。修复：保留 `CAR` 为数组（供 `.filter`），新增 `CAR_BY_ID` 字典，全局把 `CAR[` 替为 `CAR_BY_ID[`。
3. **底部 5 Tab 导航失效**：按钮用 `data-screen`，但 click 路由只匹配 `data-action`，导致 车库/商场/好友/消息 点击无反应。
   修复：路由增加对 `[data-screen]` 的最近祖先匹配并 `go(screen)`。
4. **`updateNavDots` 红点报错**：`dots` 存的是 `!!element` 布尔值，却调用 `.classList.toggle`。
   修复：存真实元素。
5. **`#game` 启动后不可见**：`index.html` 里 `#game class="hidden"`，`boot()` 只 add `active`，
   而 `.hidden{display:none!important}` 压过 `#game.active`。修复：`boot()` 中 `classList.remove('hidden')`。
6. **语法错误（之前遗留）**：模板字符串误闭合、`e.stopPropagation()=>{}` 畸形箭头、`//` 行注释吞掉同行闭合 `}`（行34/37/38）。
   用自写的"上下文栈"括号扫描器（处理嵌套模板字符串与 `${}`）定位，全部修复。

## 验证手段（可复用）
- `node --check app.js` 做语法初筛。
- 自写 Python 上下文栈扫描器 `scan.py`：正确处理字符串/模板/注释/嵌套 `${}`，比裸括号计数可靠。
- `jsdom` 冒烟测试 `smoke.js`：加载 index.html + 脚本，触发 DOMContentLoaded，遍历点击所有 `[data-action]` 与底部 nav，捕获运行时报错。
- 本地 `python -m http.server 8080` 验证资源 200。

结果：`smoke.js` ERRORS=0，5 个 Tab + 各弹窗（资料/分享/充值/首充/任务/七日/夺宝/好友拜访）均正常渲染。
注意：目前无美术资源，`assets/` 下图片 404，靠 `<img onerror>` 隐藏，不影响运行。

## 车行项目 — 修复"主界面缺失入口/组件 + 下方入口功能缺失"

用户反馈：主界面缺部分入口和组件；底部入口功能缺失。对照 UI交互说明 A02（共20元件）定位：

1. **「下方入口功能缺失」根因**：`renderGarage/renderMarket/renderFriends/renderMessages/renderGacha`
   全部往 `$('#screen')` 写内容，但 `index.html` 没有 `#screen` 容器 → 点击车库/商场/好友/消息/夺宝后内容无处渲染、毫无反应。
   修复：新增 #screen 容器 + 用 `#homeview`（包裹 empbar/infobar/swiper）做视图切换：
   `go()` 里给 `#game` 切 `view-home` 类，CSS 用 `#game.view-home #homeview/#screen` 控制显隐。
2. **主界面缺组件（A02 元件6 资产 / 元件10 每分钟收入）**：原只是不可点击文字 chip。
   修复：两个 chip 加 `data-action`=open-asset/open-income，新增右侧抽屉机制 `#drawer` + openDrawer/closeDrawer，
   点击弹出右滑说明面板（点遮罩或关闭按钮收起），符合"点击显示右侧说明，再次点击任意处关闭"。
3. **员工位锁定逻辑 bug**：`updateEmpbar` 循环 `i<S.empSlots` 且空位 `data-action` 写了两个（重复属性），
   导致永远无法解锁更多员工位。修复：循环到 MAX_EMP=8，已解锁空位→open-hire，超出部分→unlock-empslot 可点击解锁。
4. **空车位「获取车辆」点不动**：`open-market` 原直接 `renderMarket()` 渲染进隐藏的 #screen。
   修复：`open-market` → `go('market')` 切屏（符合 A02 元件12 空位→【A14】市场界面）。
5. **好友车位左右换页**：friendPanel 新增"⟨ 返回车行"按钮（go-my-panel），go-friend-panel/go-my-panel 用 scrollTo 定位面板；
   renderHome 默认把 swiper 滚到「车行」(myPanel, 右侧)，好友车位在左侧，符合 A02 元件19。scrollTo 包 try/catch 防御 jsdom 不实现。
6. **顶部标题**：`go()` 增加 `setTitle`（按屏幕名显示 车行/车库/商城/好友/消息/欢乐夺宝）。

验证：`node --check` 通过；`smoke.js` 增强为 24 项断言（启动可见、homeview/screen 切换、8个员工位含锁定、资产/收入 chip 可点、
抽屉开关、5 个底部 Tab + 欢乐夺宝GUI 均在 #screen 渲染出内容且 homeview 隐藏）→ 24 passed, 0 failed, 0 runtime errors。
本地服务器 http://127.0.0.1:8080 各资源 200。

## 车行项目 — 名车之旅(A11) 任务条件门禁

用户要求："名车之旅任务需要完成对应任务条件才能领取"。

问题：原 `renderQuests` 里任务进度写死 `done=false`（TODO），且无任何"领取"按钮/门禁，与 A11 规范
（"章节奖励需完成章节内所有任务方可领取；未完成显示进度；可领取时高亮"）不符。

修复：
1. **任务计数接入** `defaultState.stats{hired,poached,workCount,shareCount,parkFriendCount,orderCount,ticketCount,inviteCount,earned}`；
   并在 startWork/doHire(区分 hire/poach，按钮加 data-mode)/parkAtFriend/orderCar/ticketAll+ticketFriend/分享/收取/兑换 处累加对应计数（load 迁移补 stats）。
2. **任务条件引擎** `taskEval(task)` 用正则解析中文任务文本（招募N/挖角N/安排工作N次/分享N次/拥有N辆X级车/拥有N个车位/
   去好友家停车N次/拥有N辆车/赚取N美金/拥有N名员工/拥有N名好友/拥有N个员工位/订购N辆/贴条N次/成功邀请N个好友/每分钟收入达到N/资产达到N万或N亿/拥有+8以上车），
   返回 {done,cur,target}。配套 totalCarCount/ratingCount/ownedSpotsCount/friendCount/hasCap8Car、chapterAllDone。
3. **game_data.js 补 `assetReq`**：原 chapters 缺资产解锁门槛，导致全部章节默认解锁。按 解锁配置 注入：
   ch1=0, ch2=50万, ch3=150万, ch4=800万, ch5=2400万, ch6=6000万（-e 正则转义失败，改用 patch_chapters.js 文件脚本注入）。
4. **重写 renderQuests**：未解锁章节按资产门槛显示"需资产达X解锁"；已解锁章节点击表头展开任务列表；
   每任务显示 完成态(✅)/进度(cur/target)/可领取高亮按钮；章节奖励区：全部完成→高亮"领取奖励"按钮(脉冲动画)，否则显示"完成X/Y后解锁"。
   新增 claimTask(ci,no)/claimChapter(ci)/toggle-chapter，且两者都有 JS 门禁（条件未达成直接拒绝、不入账）。
5. **CSS** 补 任务完成/进度/可领取按钮、章节奖励高亮脉冲、锁定提示等样式（变量 --gold/--green/--accent 均已定义）。

验证：`node --check` 通过；`smoke.js` 扩至 32 项，新增名车之旅门禁断言
（打开弹出6章、展开第1章出现可领取按钮、存在未完成任务进度、领取已完成任务美金入账且标记、未完成任务直接 claimTask 被拒/美金不变、
整章未完成 claimChapter 被拒）→ 32 passed, 0 failed, 0 runtime errors。
为测试加了 `window.__cx` 调试钩子（暴露 S/taskEval/claimTask/claimChapter/renderQuests），供冒烟测试访问内部状态，无害。
本地服务器 http://127.0.0.1:8080 各资源 200。

## 车行项目 — 5项UI交互修复

用户要求：1)一个车位只能停一辆车 2)主界面左上角不显示返回按钮 3)资产说明按截图样式 4)员工空位显示"招募员工"文本 5)主界面车位左右拖动换页(左好友车位A06/右车行4车位)+交互提示

修复：
1. **一车一位**：确认代码已保证（`freeSpotIdx()` 查空闲位 + `instAtSpot()` 用 `.find()` 只取首个；`showCarGet/claimChapter/orderCar` 等所有放置路径均走 `freeSpotIdx()`）。冒烟验证无重复占位。
2. **返回按钮隐藏**：`go()` 里按 `name==='home'` 切换 `.tb-hidden` class；CSS `#game.view-home .tb-back{display:none!important}` + `.tb-hidden{display:none!important}` 双保险。
3. **资产说明面板**：按截图重写 `openDrawer` 内容 — 标题"资产"+描述文字("所有车辆的价值之和。购买新车提升资产，就能订购更高级的车辆。")+"当前资产 XX.X万"。新增 CSS `.drawer-desc`/`.drawer-value`/`.drawer-label`。
4. **员工空位文本**：`updateEmpbar` 空位按钮从 `＋` 改为 `<span class="emp-hire-text">招募</span>`，CSS 加 `.emp-hire-text{font-size:10px;font-weight:800;color:var(--accent)}`。
5. **滑动换页+提示**：`renderHome` 新增 `renderSwipeHints()` 在 friendPanel 顶部插入"⟨ 滑动返回车行"(swipe-hint-right)、myPanel 顶部插入"滑动查看好友车位 ⟩"(swipe-hint-left)，带脉冲动画(`swipe-pulse` keyframe)和左右边框色带。点击提示文字也可触发面板切换。

验证：`node --check` 通过；`smoke.js` 扩至 **40 项断言**（新增：返回按钮 tb-hidden class、资产抽屉标题/描述/当前资产三要素、员工空位含"招募"、双面板滑动提示存在、一车一位无重复占位）→ **40 passed, 0 failed, 0 runtime errors**。

## 车行项目 — 3项修复（去重/名车之旅/淘车消耗）

用户反馈：1)主界面首充和七日与featRow重复显示 2)名车之旅任务界面打不开 3)淘车消耗应为该类型车辆平均价格

修复：
1. **去重**：从 `updateInfobar` 移除 `chip-fc`(🎁 首充) 和 `chip-7day`(📅 七日)，因为 featRow 已有"首充礼包"和"七日登陆"按钮。infobar 仅保留资产/任务/收入三个 chip。
2. **名车之旅打不开**：冒烟测试验证 `renderQuests()` 正常工作（6章卡片渲染、展开/领取/门禁均通过）。代码逻辑无误，jsdom 40项全过。可能是之前某次修复的连带问题，当前已确认可正常打开。
3. **淘车消耗改为平均价格**：原 `cost = maxPrice * rnd(0.03,0.15)`（基于上限随机）→ 改为 `avgValue = pool.reduce((s,c)=>s+c.value,0)/pool.length`（该类型车辆池 value 均值），再 `cost = avgValue * rnd(0.08,0.25)`。各档平均价值：E级≈6366(淘车509~1591)、D/C级≈55万(4.4万~13.8万)、B/A级≈1334万(107万~333万)、A/S级≈3743万(299万~936万)。

验证：`node --check` 通过；`smoke.js` **40 passed, 0 failed, 0 runtime errors**。

## 车行项目 — 功能入口按钮移位

用户要求：将 featRow 的4个按钮（欢乐夺宝/首充礼包/名车之旅/七日登陆）从 #myPanel 内部移到 #homeview 中 infobar 和 swiper 之间（箭头指向位置），名字保持不变。

修复：在 `index.html` 中将 `<div id="featRow" class="feature-row">` 从 `#myPanel` 内部移到 `#infobar` 之后、`#swiper` 之前。CSS `.feature-row` 样式通用无需调整。

验证：`node --check` 通过；`smoke.js` **40 passed, 0 failed, 0 runtime errors**；HTML 结构确认 featRow 在 infobar 之后、swiper 之前、不在 myPanel 内。

## 车行项目 — 汽车/车标抠图 + 车名变色（②号需求）

用户需求：汽车展示需要抠图（底板变透明），部分车名含颜色词（火焰红/质子蓝/荧光粉等）要根据名字变色。

### 抠图（已完成）
- 新建 `batch_remove_bg.py`：用 rembg + onnxruntime(U2Net) 把 `汽车图鉴/*.jpg|jpeg` → `assets/cars/car_XX.png`、`车标图鉴/*.png` → `assets/logos/logo_XX.png`，并按编号前缀匹配。
- Python 环境：`C:\Users\Administrator\.workbuddy\binaries\python\envs\default`（venv + Pillow + rembg[cpu] + onnxruntime==1.20.0；1.27.0 会 DLL load failed，降版解决）。首次需下载 U2Net 模型(~176MB)。
- 源图鉴实际共 72 张：汽车 47 张(01-47 非连续)、车标 25 张。全部处理完毕 → 47 车 PNG + 25 车标 PNG，均为 RGBA 透明（实测车体像素占比 40% 左右）。
- 脚本第3步把 `game_data.js` 中 `"image"/"logo"` 字段的 `.jpg/.jpeg` 全量替换为 `.png`（正则 `"(image|logo)":\s*"[^"]+\.(jpg|jpeg)"`）。并已清理 47 个旧 .jpg/.jpeg 原始文件。
- 注意：`app.js` 的 `thumb()/carImg()` 字段名是 `c.image`（与 game_data 的 `image` 字段一致，之前已修过 `c.img`→`c.image`）。

### 车名变色（已完成 + 修复 bug）
- `app.js` 新增 `COLOR_PHRASES`（中文颜色词→CSS 色值，多字优先），`colorName(name)` 把颜色词包成 `<span style="color:...">`；11 处车名渲染改为 `colorName(...)`（pc-name×2、kv工作车辆×2、fs-11身价、car-item-name、order-item-name、car-get-name、fw-800最佳车辆、topCar名）。
- **修复嵌套/串色 bug**：原实现用 `includes` + `replace` 循环，会把已插入 `<span>` 内的子词（荧光粉中的"粉"、路虎揽胜中国红中的"红"）二次匹配，造成嵌套 span 且颜色错乱（"粉"被改成另一种粉色、"红"被覆盖）。改为：排序后预编译 `COLOR_RE = /(长词|...|短词)/g`，用 `name.replace(COLOR_RE, span)` 一次性整词匹配，子词一旦属于更长的颜色词就不再单独着色，整词统一同色。同时修了一处畸形条目 `['#00897B']` → `['青','#00897B']`。

验证：`node --check` 通过；`colorName` 单测（火焰红/荧光粉/路虎揽胜中国红/青 等）→ 各自只生成一个 span 且颜色统一正确；`smoke.js` **40 passed, 0 failed, 0 runtime errors**。本地预览 http://127.0.0.1:8088 (python http.server)。
