# 2026-07-27 工作日志

## cu — 个人信息/好友信息弹窗：车名前评级标签改为品牌图标
- **问题**：个人信息界面（我的信息 + 好友信息）"最有价值的车"区域，车名左边显示了两个评级素材（ratingBadge B/S/A），箭头指向名字左边的应改为车辆品牌图标
- **修改**：`app.js` 两处 `uip-car-name-row` 内的 `${ratingBadge(...)}` 替换为 `${logoImg(...brand)}`
  - 第1538行（我的信息面板）：`CAR_BY_ID[topCar.carId].rating` → `CAR_BY_ID[topCar.carId].brand`
  - 第3249行（好友信息面板）：`topCar.rating` → `topCar.brand`
- **验证**：node -c 通过，smoke.js 74pass/7fail/0error 无回归
- **版本**：index.html app.js `?v=20260726cu`

## cv — 车名统一默认颜色（不再按颜色词变色）
- **需求**：汽车名称颜色统一默认颜色，不需要变色
- **修改**：`app.js` 第121行 `colorName()` 函数直接 `return name`（移除 `COLOR_RE` 包彩色 span 逻辑）；所有车名显示点（uip-car-name、emp-work-carname、车库、好友车行、好友列表、订单）自动回到默认文字颜色
- **验证**：node -c 通过，smoke.js 74pass/7fail/0error 无回归
- **版本**：index.html app.js `?v=20260726cv`

## cw — 修复好友车位(机器人停车)收益不按真实时长累积（开罚单30分钟门槛/满仓增长失效）
- **根因**：`tick()` 中机器人/好友车收益用 `dt=Math.min((now-lastTick)/1000,300)` 增量累积，离线/刷新后最多只补5分钟，导致：①停在车位上的车永远到不了30分钟（开罚单门槛形同虚设）；②关页期间不增长，不会"一直增长到满仓"
- **修改**：
  - 新增停车时间戳 `parkedAtTs`：机器人停车(`autoParkBotsDaily`)和好友字段归一化(migration)时写入 `now()`
  - `tick()` 机器人循环改为按真实停车时长累积：`elapsedMin=(now()-parkedAtTs)/60000; parkAccrued=min(incPerMin*elapsedMin,cap)`（绕过5分钟离线封顶）；满仓仍自动收回并清 `parkedAtTs`
  - 开罚单拒绝提示统一为"需要至少停留30分钟才可以开罚单"（与规则"≥30分钟"一致；用户原文误写3分钟，按明确的30分钟规则实现）
- **验证**：node -c 通过；smoke.js 74pass/7fail(预存)/0error 无回归；_verify_park.js 验证：停10分钟→估算10分钟且未收回不可开罚单；停200分钟→满仓自动收回
- **版本**：index.html app.js `?v=20260726cw`

## cx — 未解锁好友车位：文本改"添加更多好友解锁"+点击跳转好友界面[新的车友]标签
- **需求**：
  1. "邀请更多好友解锁"文本改为"添加更多好友解锁"
  2. 未解锁车位（锁图标区域）点击后直接跳转到好友界面的"新的车友"标签
- **修改**：
  - `app.js` 两处 `pc-lock-hint` 文本："邀请更多好友解锁" → "添加更多好友解锁"
  - 两处 locked park-card 加 `class="clickable"` + `data-action="go-add-friends"`
  - 新增 action handler：`case 'go-add-friends'`: closeModal → go('friends') → rankTab='new' → renderFriendTab()
  - `css/style.css` 加 `.park-card.locked.clickable { cursor: pointer }`
- **验证**：node -c 通过，smoke.js 74pass/7fail/0error 无回归
- **版本**：index.html css+app.js `?v=20260726cx`

## cy — 修复埃文塔多橙/粉车漆：轮胎和车窗不再被染色
- **问题**：埃文塔多-动感橙/荧光粉的车辆素材中，轮胎和车窗被错误地染成了橙色/粉色（上一次 cr 的亮度蒙版着色未排除极暗像素）
- **修复**：基于原始蓝车底图（1024x1024）重新着色，增加 **DARK_THRESH=25** 亮度阈值：
  - brightness < 25 → 轮胎/车窗/黑色部件 → **保持原色不变**
  - 25 ≤ brightness < 230 → 车身 → HSV 亮度蒙版着色（保留3D光影）
  - brightness ≥ 230 → 背景 → 保持白色
- **验证**：轮胎区域像素确认 RGB≈(0,0,0) 纯黑；之前出问题的位置 (115,100) 从 RGB(89,44,16) 橙棕色恢复为纯黑
- **文件**：`assets/cars/Vibrant_orange_Lamborghini_Ave_*.png`、`Fluorescent_pink_Lamborghini_*.png`（覆盖重写）
- **版本**：index.html `?v=20260726cy`

## cz — 修复夺宝转盘"3次夺宝"实际只给2次的BUG
- **问题**：`gachaSpin()` 先消耗1次 stamina（`stamina--`），结算时 `gainGachaStamina(3)` 只加回3，净结果=+2（而非显示的+3）；同理"2次夺宝"净得+1
- **修复**：stamina 类奖励的 gain 调用改为 `gainGachaStamina(selected.val + 1)`，额外补回1次抵消本次 spin 消耗
  - "3次夺宝" → 实际净得 +3 ✓
  - "2次夺宝" → 实际净得 +2 ✓
- **验证**：node -c 通过，smoke.js 74pass/7fail/0error 无回归
- **版本**：index.html app.js `?v=20260726cz`

## da — 修复夺宝收费/掠夺跳转后直接回到主界面的BUG（根因：CSS view-home 隐藏 #screen）
- **问题**：欢乐夺宝转到收费/掠夺时，直接关闭弹窗回到主界面，目标场景（好友车行+遮罩+按钮）不显示
- **根因**：CSS 规则 `#game.view-home #screen { display: none }` — 用户在主界面打开夺宝时 `#game` 有 `view-home` 类，导致 `#screen` 被隐藏。`renderGachaTargetScene()` 直接写 `#screen.innerHTML` 但从未调用 `go()` 移除 `view-home`，所以内容写入了但不可见。closeModal 后用户只看到底层主界面
- **修复**：
  - `renderGachaTargetScene()`: 写入前 `$('#game').classList.remove('view-home')` 让 #screen 可见
  - `returnFromGachaTarget()`: 返回时恢复 `$('#game').classList.add('view-home')`
  - `executeGachaAction()`: 完成收费/掠夺后同样恢复 view-home
- **验证**：node -c 通过，smoke.js 74pass/7fail/0error 无回归
- **版本**：index.html app.js `?v=20260726da`

## db — 修复刷新后员工工作头像消失（多重保险策略）
- **问题**：安排员工工作后点击浏览器刷新按钮，车位卡片上车辆左下角的员工工作状态头像（👤圆圈）消失。用户反馈多次（ct版本删除CSS `display:none!important`后仍未解决）
- **排查结论**：
  - 数据层正确：`inst.empIid`、`emp.workEnd` 均正确持久化到 localStorage，刷新后读取无误
  - tick() 正确：工作未到期时不会清除 empIid
  - 渲染逻辑正确：`renderParkCard()` 条件判断（empIid存在 && 员工找到 && workEnd>now()）全部通过，HTML 输出包含完整的 `pc-driver-overlay busy` 元素
  - CSS 无 display:none 规则：ct 版已删除 `.pc-driver-overlay { display:none!important }`
  - **推测根因**：浏览器缓存了旧版 CSS 文件（含隐藏规则），或某些边缘情况下样式优先级被覆盖
- **修复（多重保险）**：
  1. **CSS 层**：`.pc-driver-overlay` 的 `display`/`visibility`/`opacity` 全部加 `!important`
  2. **HTML 层**：渲染时 `<div class="pc-driver-overlay busy" ...>` 加内联 `style="display:flex!important;visibility:visible!important;opacity:1!important;"` 强制覆盖任何外部规则
- **验证**：node -c 通过；smoke.js 74pass/7fail(预存)/0error 无回归；_debug_emp_refresh2.js 确认 HTML 输出包含完整 overlay 元素
- **版本**：index.html css+app.js `?v=20260726db`

## dc — 修复员工信息弹窗头像显示异常（绿底/不居中/框色不一致）
- **问题**：
  1. 员工信息弹窗顶部员工头像（王宝强）下面多了绿色底
  2. 底部"现任老板"头像没有在框内居中
  3. "最栽培Ta的老板"头像框颜色和顶部的不一致
- **根因**：
  - `.emp-panel-avatar .emp-avatar-ph` 缺少尺寸/布局属性，span 未填满容器导致底层背景透出
  - `.emp-boss-avatar .emp-avatar-ph` 同样缺少 flex 居中属性
  - `.emp-boss-avatar` 使用了与顶部完全不同的样式（`border:2px solid var(--line)` + `border-radius:8px` + `background:var(--bg)`），而顶部用 `background:var(--panel)` + `box-shadow` + 无边框
- **修复**（纯 CSS）：
  - `.emp-panel-avatar .emp-avatar-ph`: 加 `display:flex;align-items:center;justify-content:center;width:100%;height:100%;border-radius:10px`
  - `.emp-boss-avatar`: 改为与顶部一致的风格 — `border-radius:10px; background:var(--panel); box-shadow:var(--shadow-sm);`（去掉 border）
  - `.emp-boss-avatar .emp-avatar-ph`: 加 `display:flex;align-items:center;justify-content:center;width:100%;height:100%;border-radius:8px`
- **验证**：node -c 通过，smoke.js 74pass/7fail(预存)/0error 无回归
- **版本**：index.html css `?v=20260726dc`

## dd — 车库卡片员工信息布局调整（头像+名字横排，进度条右移）
- **需求**：车库卡片中，员工信息区域（头像+名字+进度条）重新布局：
  - 名字从头像下方移到头像右侧（上移）
  - 进度条移到头像右侧（与例图一致：左[头像 名字] 右[进度条]）
- **修改**（纯 CSS）：
  - `.pc-income-emp-wrap`: `flex-direction: column` → 改为默认 row（横向排列），`gap: 1px` → `gap: 4px`
  - `.pc-income-emp-name`: 字号 9px→11px，颜色 var(--sub)→var(--fg)，去掉 max-width/overflow/text-align:center 限制
- **验证**：node -c 通过，smoke.js 74pass/7fail(预存)/0error 无回归
- **版本**：index.html css `?v=20260726dd`

## de — 车库三种操作按钮颜色区分
- **需求**：车库卡片底部"出车"、"收回车库"、"取回车辆"三种按钮需要视觉区分
- **修改**（纯 CSS，基于 `data-action` 属性选择器）：
  - `deploy-car`（出车）：**绿色渐变** `#dcfce7→#bbf7d0`，深绿文字 `#166534`
  - `recall-car`（收回车库）：**蓝色渐变** `#dbeafe→#bfdbfe`，深蓝文字 `#1e40af`
  - `recall-from-friend`（取回车辆）：**橙黄渐变** `#fef3c7→#fde68a`，深棕文字 `#92400e`
  - 每种按钮均有独立的 `:active` 按下态
- **验证**：node -c 通过，smoke.js 74pass/7fail(预存)/0error 无回归
- **版本**：index.html css `?v=20260726de`

## df — 统一所有车辆素材尺寸为 210×140（修复思域TYPE R等车显示过小）
- **问题**：思域TYPE R（id:51）等 13 辆车的素材未经过裁剪缩放处理，仍保持原始分辨率（1024×1024 / 1920×1080 等），在 `object-fit:cover` 约束下因留白占比大导致视觉上比其他车小很多
- **处理方式**（PIL 批量）：
  - 对每张图执行 `auto_crop_resize`：先检测非白色区域边界框（`ImageChops.difference.getbbox()`），裁掉四周留白
  - 保持宽高比缩放到 210×140 容器内最大尺寸，居中放置于白底画布
  - 覆盖原文件保存为 RGB 格式
- **处理的 13 张图片**：
  - 思域TYPE R（1024→210）、火神-层云白/苹果绿/火焰红（1920→210）、car_08（1920→210）
  - 保时捷911 全6色（1024→210）、car_44/45/46/47（480~808→210）
- **验证**：处理后全部 88 张车辆素材统一为 210×140，零遗漏
- **版本**：index.html css `?v=20260726df`

## dg — 增加好友数量上限（玩家100人 + 对方上限提示）
- **需求**：①玩家最多拥有好友100人，若已有100个好友，添加或通过新好友申请时拒绝提示"好友数量已达上限"；②若对方好友已满，提示"对方好友数量已达上限"
- **实现**：
  - 新增常量 `FRIEND_MAX = 100`（app.js 顶部）
  - BOT_POOL（makeFriends）与 REAL_PLAYER_POOL 每条新增 `friendsCount: rndi(20,90)` 字段，代表"该好友自身的好友数"
  - `addFriend(fruid)`：先判 `friendCount() >= FRIEND_MAX` → "好友数量已达上限"；再判 `bot.friendsCount >= FRIEND_MAX` → "对方好友数量已达上限"；成功后 `bot.friendsCount++`
  - `acceptFriend(mid)`：原本只标记 claimed、并未真正加好友；现改为真正把对方加入 S.friends（查 BOT_POOL/REAL_PLAYER_POOL 取数据，查不到则用消息字段兜底），并同样校验双方上限
  - `searchUser()`：原本仅 toast 演示，现改为按 uid 查池后调用 `addFriend()`，使"输入ID申请好友"路径也受双方上限约束
  - "新的车友"页新增 `👥 我的好友 X / 100` 计数胶囊（达上限变红 `.full`）
  - 调试钩子 `window.__cx` 新增暴露 `addFriend / acceptFriend / msgFriendReq / friendCount / FRIEND_MAX / BOT_POOL / REAL_PLAYER_POOL`
- **验证**：node -c 通过；smoke.js 新增10项好友上限测试全部 PASS（84pass/7fail预存/0error）；7项fail为历史预存（资产抽屉/Swiper面板数/空闲车位占位），无新增回归
- **版本**：index.html 三处 `?v=20260727a`

## dh — 玩家 UID 统一变为 8 位数
- **需求**：玩家 UID 统一为 8 位数
- **实现**：
  - `defaultState()` 中 `uid:1` → `uid: rndi(10000000, 99999999)`（新号直接生成 8 位整数）
  - `load()` 归一化段新增玩家 UID 迁移：若 `S.uid` 非 8 位数（`Number` 整数且 ∈[10000000,99999999]），生成新 8 位 uid，并同步更新存档中所有「指向玩家自身 uid」的引用，避免雇佣/身价贡献关系断裂：
    - 员工 `hiredFrom` / `topHirerUid` / `employedBy`
    - 员工 `nwContributors` 的 key（旧 uid → 新 uid，保留值）
    - 好友 `employedBy`
  - 调试钩子 `window.__cx` 新增暴露 `load` / `save`
- **注意**：`_uid`（mk 中实例/员工计数器，line 202/204）与此无关，未改；消息 `uid()`（line 44 `'id_...'`）为消息 mid，与玩家 uid 无关，未改；机器人/真实玩家 uid 仍为 `fr_*` / `vp_*`，不受此需求影响（需求仅针对玩家自身 uid）
- **验证**：node -c 通过；smoke.js 新增5项 UID 测试全部 PASS（89pass/7fail预存/0error）；新号 uid 实测 77527219，老号迁移 89204500 且引用同步
- **版本**：index.html 三处 `?v=20260727b`

## di — 机器人 8 位 UID + 搜索弹出信息面板 + 无效 ID 提示
- **需求**：
  1. 搜索玩家 ID 后弹出对方信息面板（而非直接添加好友）
  2. 机器人也使用 8 位 UID，与玩家不重叠
  3. 搜索为空或目标不存在时提示「请输入正确的ID」
- **实现**：
  - `makeFriends()` 中机器人 uid 从 `'fr_'+i` → `''+(10000001+i)`（8 位，区间 10000001~10000010）
  - `REAL_PLAYER_POOL` 中 uid 从 `'vp_1~5` → `'20000001~20000005'`（8 位，独立区间）
  - `searchUser()` 重构：支持可选参数 `searchUser(uid)` 直接传参（测试/程序化调用），不传参时从 `#searchUidInput` 读取；找到用户后调用 `showUserInfoPopup(q)` 弹出信息面板（面板内自带加好友/删除好友按钮），不再直接 `addFriend()`
  - 空输入和不存在 ID 统一提示 `toast('请输入正确的ID')`
  - 调试钩子新增暴露 `searchUser` / `showUserInfoPopup`
- **验证**：node -c 通过；smoke.js 新增 7 项测试全部 PASS（98pass/7fail预存/0error）；7 项 fail 为历史预存无回归
- **版本**：index.html 三处 `?v=20260727c`

## dj — 礼包码系统
- **需求**：
  1. 个人信息面板加礼包码入口按钮，点击弹出输入弹窗
  2. 输入正确礼包码给予对应奖励，每个码每玩家只能用1次
  3. 错误码提示「请输入正确的礼包码」
  4. 已领过提示「已领取过同类型礼包」
  5-8. 四个有效码：VIP666(66666刀乐)、VIP888(88888刀乐)、VIP999(99999刀乐)、leroynb(66666黄金+666666刀乐)
- **实现**：
  - 新增常量 `GIFT_CODES`（app.js 顶部），含4个码的 label + rewards 数组（支持多奖励）
  - `defaultState()` 新增 `redeemedCodes: []` 字段，持久化到 localStorage
  - `renderProfile()` 的 `uip-top-row` 右侧新增 `.giftcode-entry-btn`（蓝渐变六角形🎁+文字）
  - 新函数 `openGiftCodeModal()`：渲染弹窗（标题"礼包码"+输入框+确定按钮），自动聚焦输入框
  - 新函数 `redeemGiftCode()`：读取输入→大写→查表→校验已领→发放奖励(S.dollars/S.beans)→标记已领→save→toast→刷新HUD→回个人信息面板
  - action handler 绑定：`open-giftcode` → `openGiftCodeModal()`；`redeem-giftcode` → `redeemGiftCode()`
  - CSS：`.giftcode-entry-btn`（蓝渐变圆角按钮）、`.gc-modal-wrap`（弹窗整体）、`.gc-input`（米黄底输入框）、`.gc-confirm-btn`（金底确定按钮）
  - 调试钩子暴露 `openGiftCodeModal / redeemGiftCode / GIFT_CODES`
- **验证**：node -c 通过；smoke.js 新增10项测试全 PASS（108pass/7fail预存/0error）；覆盖常量完整性、单/双重奖励、默认空数组、无效码拒绝、有效码兑换成功+余额增加+记录、重复兑换拒绝+不重复发放
- **版本**：index.html 三处 `?v=20260727d`

## dk — 拜访好友车行界面优化（4项）
- **需求**：
  1. 好友没有员工时不显示员工区域
  2. 好友员工显示样式与主界面 empbar 一致
  3. 好友头像和资产图标与主界面一致
  4. 移除「顺联车行」标题栏
- **实现**：
  1. `renderVisitEmps()`：count===0 时 `el.innerHTML=''; el.style.display='none'` 整个区域隐藏（不再显示"暂无员工"）
  2. 员工渲染改用主界面 `emp-slot-wrap > emp-slot(busy/idle) + emp-avatar + emp-status-dot + emp-name` 结构，复用全局 CSS；废弃旧 `vf-emp-card/vf-emp-status/vf-emp-avatar/vf-emp-name` 样式
  3. 头像：72px 圆形深色渐变+金边 → 52px 圆角16px 方形 + var(--bg)底 + var(--shadow-sm)（与 uip-avatar 一致）；资产：单行👛钱包+数字 → 多行 vf-stats-col（🏆身价 + 📁资产），与主界面 HUD 风格统一；夺宝收费场景 `renderGachaTargetScene()` 同步更新
  4. 移除 `<div class="vf-titlebar">` 整块（含"顺联车行"标题和返回按钮）；返回功能由底部导航"回家"按钮承担；`.vf-titlebar` / `.vf-title` CSS 改为 `display:none`
- **验证**：node -c 通过；smoke.js 108pass/7fail预存/0error 无回归
- **版本**：index.html 三处 `?v=20260727e`

## dl — 生成并部署《车行》隐私政策页面供 TapTap 填写
- **背景**：TapTap 开发者后台「隐私说明」需要填写以 https 开头的隐私政策链接；用户提供了官方模板 docx 与截图
- **信息确认**：开发者 Leroy，联系邮箱 648854918@qq.com；游戏为单机本地存储，当前未接入第三方 SDK
- **修改**：基于模板与现有 `store-listing/privacy-policy.html` 重写完整隐私政策（收集使用、保存、第三方服务、保护、用户权利、未成年人、政策更新、联系方式），填入开发者信息与邮箱
- **部署**：将页面复制到 `deploy-privacy/index.html` 并部署到 CloudStudio，获得 HTTPS 链接 `https://8d078a756e55407593c74afde8868864.app.codebuddy.work`
- **输出**：告知用户把该链接填入 TapTap 后台「隐私政策链接」字段

## dm — 隐私政策游戏名改为「抢车位：华夏崛起」
- **变更**：用户确认实际游戏名称为《抢车位：华夏崛起》（此前误用「车行」）。`store-listing/privacy-policy.html` 与 `deploy-privacy/index.html` 中《车行》全部替换为《抢车位：华夏崛起》（含 title、h1、正文、footer 共4处）。
- **部署**：CloudStudio 重新部署，链接不变 `https://8d078a756e55407593c74afde8868864.app.codebuddy.work`
- **待确认**：`store-listing/listing.md`、游戏 UI（`index.html`/`app.js` 现显示「车行/首富车行」）仍用旧名，是否一并更名待用户决定

## dl — 好友车位选车弹窗车辆排序规则
- **需求**：在好友家停车，选车弹窗内车辆排序：先按 闲置中 > 停在好友家 > 停在自己家，再按收入从高到低
- **实现**：`renderParkAtFriendModal()`（app.js ~3577）排序改为：
  - 主排序 `locOrder = {'garage':0,'atFriend':1,'spot':2}`，`la!==lb ? la-lb : ...`
  - 次排序由原本 `capacity` 改为 `income`（收入从高到低）
- **关键 BUG 修复**：原代码 `locOrder[a.loc]||9` 会把 garage 的 `0` 值误判为 falsy 变成 `9`，导致「闲置中」始终排到最后（原规则13实际从未把闲置中放最前）。改为 `??9`（nullish 合并）保留 0 值，排序才正确
- **验证**：node -c 通过；smoke.js 新增4项排序测试全 PASS（112pass/7fail预存/0error），覆盖状态顺序、组内收入降序
- **版本**：index.html 三处 `?v=20260727f`

## dm — 好友家停车规则：员工车拒绝飘字修复
- **背景**：用户重述三条停车规则（员工车拒绝、正常车结算刀乐、超4辆拒绝），实际规则本身已实现，但员工车拒绝飘字不生效
- **根因 BUG**：`renderParkAtFriendModal()` 给员工工作车按钮加 `disabled` 属性；浏览器对 disabled 按钮不派发 click 事件，导致委托处理器（`case 'park-at-friend'`）永远接收不到点击，`doParkAtFriend()` 内的拒绝飘字 `else toast(...)` 分支从未触发
- **修复**：
  1. 员工车按钮不再用 `disabled` 属性，改为加 `is-locked` class（灰显但仍可点击），`data-action`/`data-iid` 保留
  2. 点击事件处理器简化为 `case 'park-at-friend': doParkAtFriend(...)`（所有校验交给 doParkAtFriend）
  3. `doParkAtFriend()` 员工车分支去掉 `closeModal()`，仅 toast 拒绝并 `return`，弹窗保持打开方便改选其他车
  4. CSS：`.vf-park-btn.is-locked { background:#c4c4c4; cursor:not-allowed; opacity:.75; }`（替代原 `.vf-park-btn:disabled`）
- **验证**：node -c 通过；smoke.js 新增7项停车规则测试全 PASS（119pass/7fail预存/0error）：员工车→拒绝飘字+未停入、满4辆→拒绝飘字+未停入、正常停车→刀乐+150结算+停入+累积归零
- **版本**：index.html 三处 `?v=20260727g`

## dn — 员工安排工作选车弹窗卡片高度不足
- **需求**：给员工安排工作，选择车辆弹窗内单个车辆组件高度不够，车辆名称和加成等信息显示不全
- **实现**（纯 CSS 修改）：
  - `.cs-car-row`：`overflow: hidden` → `overflow: visible`（防止内容被裁切）
  - `.cs-car-left`：padding `8px 6px` → `10px 6px 8px`；gap `4px` → `6px`
  - `.cs-car-img-wrap`：height `56px` → `58px`
  - `.cs-car-name`：font-size `11px` → `12px`；新增 `line-height:1.3`
  - `.cs-car-right`：padding `6px 8px` → `8px 10px`；gap `4px` → `5px`
  - `.cs-data-row`：font-size `11px` → `12px`
  - `.cs-btn`：padding `6px 0` → `7px 0`；font-size `12px` → `13px`
- **验证**：node -c 通过；smoke.js 119pass/7fail预存/0error 无回归
- **版本**：index.html 三处 `?v=20260727h`

## do — 工作效率转盘动画未结束就显示结果
- **需求**：转盘动画没结束时结果区应显示「？」，否则转盘动画没有意义
- **实现**（app.js）：
  - `renderWorkSpinner()` 结果区初始渲染改为：倍率显示 `？` + 标签`等待结果`，身价提升/收入提升均显示 `？`
  - `playWheelAnimation()` 动画结束回调（3.5s 后）：调用 `updateSpinResultIcon(seg)` 更新倍率图标，同时动态计算并填充实际身价提升和收入提升数值到 `.ws-result-info`
  - `confirmSpin()` 不受影响，仍用 `pickWorkMultiplier()` 重新 roll 确认值
- **验证**：node -c 通过；smoke.js 119pass/7fail预存/0error 无回归
- **版本**：index.html 三处 `?v=20260727i`

## dp — 章节任务红点位置 + 奖励车辆二级弹窗
- **需求**：
  1. 章节任务内有可领奖时，红点从卡片右上角移到展开按钮（▼）的右上角
  2. 点击章节奖励车辆显示车辆信息弹窗（二级弹窗，不关闭章节任务面板），关闭后回到章节页面
- **实现**：
  1. **红点移位**：`.ch-chapter-rdot` 从 `.ch-card` 的子元素移到 `.ch-body`（展开按钮区域）内部；CSS `position: absolute; top:2px; right:2px` 相对 `.ch-body`（新增 `position:relative`）定位到▼按钮右上角
  2. **二级弹窗**：新增 `showCarInfoOverlay(carId)` 函数，在 `#modal` 内创建 `.car-info-overlay` 绝对定位覆盖层（半透明 backdrop + 居中卡片），含车辆图/评级/名称/属性 + 关闭按钮；`viewChapterReward()` 未达成时调用此函数替代原 `showCarInfo()`
  3. **关闭逻辑**：action handler 新增 `case 'close-car-overlay'`，移除 overlay 时带 fade-out 动画（200ms）；点击遮罩/关闭X/确定按钮均可关闭
  4. CSS：`.car-info-overlay`（absolute inset:0, z-index:100, opacity过渡）、`.cio-wrap`（居中卡片, scale动画）、`.cio-backdrop`（半透明黑）、`.cio-close-x`/`.cio-confirm-btn`
- **验证**：node -c 通过；smoke.js 119pass/7fail预存/0error（更新了章节奖励测试适配新二级弹窗结构）
- **版本**：index.html 三处 `?v=20260727j`

## dq — 拜访好友员工区域显示玩家自身员工（而非好友员工）
- **需求**：拜访好友时，右侧员工区域显示的是好友的员工，应改为显示玩家自己的员工
- **分析**：`renderVisitEmps()` 代码数据源为 `S.employees`（玩家自身员工），但注释写的是"渲染好友的已雇佣员工"，存在误导。代码逻辑本身已是读取玩家自己的员工
- **修改**：
  - 注释 `/* 元件4: 渲染好友的已雇佣员工 */` → `/* 元件4: 渲染玩家自己的员工（拜访好友时仍显示自身员工，方便管理） */`
  - 内联注释 `// 好友没有员工` → `// 玩家没有员工`
- **验证**：node -c 通过；smoke.js 119pass/7fail预存/0error 无回归
- **版本**：index.html 三处 `?v=20260727k`

## dr — 埃文塔多动感橙/荧光粉素材重制（透明底+去水印）
- **问题**：埃文塔多-动感橙和荧光粉两张素材有彩色背景（橙/粉色底）+「图片由AI生成」水印文字
- **方案**：基于哑光白底图（`Matte_white_Lamborghini_Aventa_...png`，灰车白底），用 Python PIL 重新着色：
  - 白色背景 → 透明（亮度阈值238，9324像素保留/20076像素透明化）
  - HSV亮度蒙版着色：极暗区域(brightness<30)轮胎/车窗不变色；高光区(>210)微弱着色；车身主体正常着色
  - 动感橙：hue=0.075, sat=0.85, strength=0.78；荧光粉：hue=0.90, sat=0.70, strength=0.72
  - 边缘半透明杂色清理（alpha<10→完全透明）
- **输出**：直接覆盖原文件 `Vibrant_orange_...png` 和 `Fluorescent_pink_...png`
- **验证**：目视确认透明底、无水印、轮胎/车窗保持深色不变、车身颜色正确
- **版本**：index.html 三处 `?v=20260727l`

## ds — 修复礼包码兑换 toast 显示原始 HTML 标签
- **问题**：兑换礼包码成功后，toast 提示显示 `<img class="dollar-ic" src="assets/green-bills.png" alt="$">` 原始 HTML 文本，而非渲染后的图标
- **根因**：`toast()` 函数使用 `t.textContent=msg`（纯文本模式），但 `redeemGiftCode()` 中奖励文本拼接了 `DOLLAR_IC` / `BEAN_IC`（HTML `<img>` 标签常量），导致 HTML 被当作纯文本输出
- **修复**：`redeemGiftCode()` 中奖励文本改为纯文字：`DOLLAR_IC + ' ' + f(val)` → `f(val) + ' 刀乐'`；`BEAN_IC + ' ' + fbean(val)` → `fbean(val) + ' 黄金'`
- **排查确认**：全项目搜索无其他地方向 `toast()` 传入 HTML 内容
- **验证**：node -c 通过；smoke.js 119pass/7fail预存/0error 无回归
- **版本**：index.html 三处 `?v=20260727m`

## dt — 机器人/好友 UID 老存档迁移（fr_X → 8位数）
- **问题**：di 任务已将 `makeFriends()` 改为生成8位UID（10000001+i），但老存档 localStorage 中好友仍为旧格式 `fr_3`，无迁移逻辑导致显示不变
- **根因**：玩家 UID 有迁移（load() 中 is8 检查+替换），但好友 UID 缺少对应迁移
- **修复**：在 load() 玩家 UID 迁移块之后新增好友 UID 迁移（IIFE）：
  - 遍历 S.friends，检测非8位数字的 uid
  - `fr_\d+` → `10000001 + 数字`（与 makeFriends 新规则一致）
  - `vp_\d+` → `20000001 + 数字`（与 REAL_PLAYER_POOL 一致）
  - 其他格式 → 基于字符串哈希稳定映射到 30000001~39999999 区间
  - 同步更新所有引用：员工 hiredFrom/topHirerUid/nwContributors key、好友 employedBy、车辆 atFriendUid
  - 迁移完成后立即 save() 持久化（一次性，下次加载不再触发）
- **测试适配**：smoke.js 老号迁移测试中查找好友从 `f.uid === 'fr_mig'` 改为 `f.name === '迁移友'`（因 uid 已被迁移）
- **验证**：node -c 通过；smoke.js 119pass/7fail预存/0error
- **版本**：index.html 三处 `?v=20260727n`

## du — 保时捷 911 (ID53-57) 素材替换 + 名称更新
- **需求**：ID53-57 是保时捷 911 不同配色，素材偏小，需要更换为高清新素材（透明底、去水印），并修改名称
- **处理**：
  - 5 张 clipboard 源图（566×304 ~ 550×258）→ PIL 批量处理：
    - 去背：亮度>210 且 饱和度<0.18 → 透明（保留车身+轮胎/车窗等有颜色区域）
    - 去水印：车牌 ROI 区域高斯模糊(radius=3) + 环境色混合(50:50)，消除"有驾"文字
    - 裁剪：getbbox 获取非透明边界 → 等比缩放居中到 210×140
    - 边缘清理：alpha<8 → 完全透明
  - 新文件名：`Porsche_911_classic_black/elegant_purple/racing_yellow/guard_red/gem_blue.png`
- **名称/图片映射**：
  | ID | 旧名 | 新名 | 旧图片 | 新图片 |
  |----|------|------|--------|--------|
  | 53 | 911黑色 | 911 经典黑 | Glossy_black_... | Porsche_911_classic_black.png |
  | 54 | 911白色 | 911 韵味紫 | Pearl_white_... | Porsche_911_elegant_purple.png |
  | 55 | 911蓝宝石 | 911 竞速黄 | Sapphire_blue_... | Porsche_911_racing_yellow.png |
  | 56 | 911卫红 | 911 卫红色 | Guards_red_... | Porsche_911_guard_red.png |
  | 57 | 911竞速黄 | 911 宝石蓝 | Racing_yellow_... | Porsche_911_gem_blue.png |
- **验证**：node -c game_data.js + app.js 通过；smoke.js 119pass/7fail预存/0error；目视确认5张均为透明底、水印模糊化、车辆清晰
- **版本**：index.html 三处 `?v=20260727o`

## dv — 修复埃文塔多动感橙/荧光粉着色错误（橙变绿、粉变紫）
- **问题**：dr 版用 HSV 色调偏移对灰度底图着色，效果严重偏差：
  - 动感橙：显示为橄榄绿/暗黄色（非橙色 🟠）
  - 荧光粉：显示为深紫色（非粉色）
- **根因**：HSV hue-shift 对低饱和度灰度像素效果不可预测；灰度像素的 hue 未定义，偏移后产生错误色相
- **修复方案**：改用「基于亮度的颜色混合着色法」：
  - 读取每个像素亮度 → 极暗区(brightness<45)保持原色不变（轮胎/车窗/黑色部件）
  - 车身主体(70~210)：用目标色的 HSL 色调+饱和度 + 原始明度混合，blend 强度中间高(85%)边缘低(42%)
  - 高光区(>210)：微弱着色(≤25%)保持反光
  - 动感橙目标色 RGB(255,130,20) 鲜艳橙；荧光粉目标色 RGB(255,100,170) 明亮粉
- **验证**：目视确认橙色为🟠橙色调、粉色为明亮粉红、轮胎/车窗深色不变
- **版本**：index.html 三处 `?v=20260727p`

## dq — 修复好友车位开罚单30分钟门槛 + 停留时间不增长（用户多次反馈）
- **问题1**：给好友专用车位上的车开罚单需停≥30分钟，否则提示"需要至少停留30分钟才可以开罚单"——该拒绝此前未生效（用户称"停1分钟就能开罚单"）。
- **问题2**：好友（机器人）停在玩家好友专用车位的车，停留时间不会增长。
- **根因**：
  - tick 好友车增长循环有 `if(!c) return` 早退：好友 `parkCarId` 缺失（老存档/异常）时 `CAR_BY_ID` 查不到 → `parkAccrued` 永远不更新 → 停留时间冻结在 00:00（即"时间不增长"）。
  - 满仓自动收回与开罚单门槛**规则冲突**：car1（王宝强，满仓时长恰好30分钟）在满仓瞬间被 tick 自动遣送回家，正好抢在"停满30分钟才能开罚单"之前 → 该车永远无法被开罚单。
- **修复（app.js）**：
  1. tick 好友车循环：增长计算统一用 `fcarId = fr.parkCarId || fr.bestCarId` 兜底，杜绝因拿不到车辆对象而早退冻结时间。
  2. **移除满仓自动收回**：好友车满仓后停在车位继续"占着"，直到玩家开罚单（≥30分钟）才遣送回家。这样30分钟门槛对所有车（含满仓=30min的car1）都成立。
  3. `ticketFriend`/`ticketAll` 门槛改用 `parkedAtTs` 真实停车分钟数为唯一判定；罚金按 `min(incPerMin*真实分钟, cap)*0.5` 实时核算。
  4. `formatParkTime` 优先用 `parkedAtTs` 推算停留时间，并封顶在满仓时长（`满仓后停在此处不再增长`，呼应"一直增长直到满仓"）。
  5. fspot 渲染 `pcar` 同样加 `parkCarId||bestCarId` 兜底。
  6. 调试钩子 `window.__cx` 暴露 `CAR_BY_ID` 便于测试。
- **测试（smoke.js）**：重写 9d/9f——9d 用 `parkedAtTs=now-35min` 模拟满30分钟开罚单窃取50%收益；9f 改为断言"满仓后不自动收回(停在车位等待开罚单)"+ 满30分钟后开罚单成功遣送。
- **验证**：node -c 通过；smoke.js 120pass/7fail预存(资产抽屉/Swiper/空闲车位占位，与本次无关)/0error。端到端复现：car1 停5→20→30分钟 UI 显示 00:05→00:20→00:30；停1分钟开罚单被拒(车在、刀乐不变)；停31分钟开罚单成功(罚金3150、车遣送)；满仓车 tick 后 parkedAtMe 仍为1（不再自动收回）。
- **版本**：index.html 三处 `?v=20260727q`

## dr — 修复兰博基尼橙/粉色车底阴影变色问题
- **问题**：动感橙/荧光粉埃文塔多车底阴影区域被染成了橙色/粉色，阴影应保持黑灰色。
- **修复**：重写着色脚本，增加三层阴影保护规则：
  1. 车辆底部 18% 区域 + 低饱和度 + 暗色 → 强制压暗为深灰
  2. 全图极暗像素（亮度<40）→ 完全不变（轮胎、车窗、深影）
  3. 近黑像素（RGB均<60）→ 保持原灰度
- **版本**：index.html 三处 `?v=20260727r`

## ds — 欢乐夺宝次数允许超出上限12（用户多次反馈）
- **需求**：夺宝次数上限显示 X/12，但「签到 / 前日登录 / 欢乐夺宝获取」来源【允许超出12】（例：11/12 + 签到5 → 16/12，可用16次）；只有【自然恢复】封顶12，超出部分不被自然恢复叠加。
- **根因（此前为何无法超限）**：`load()` 里有一行 `if(S.gacha.stamina > GACHA_STAMINA_MAX) S.gacha.stamina = GACHA_STAMINA_MAX;` 每次刷新都把超限值收敛回12；同时 `gainGachaStamina()` 也用 `Math.min(MAX, ...)` 封顶；`updateGachaCenter()` 显示 `Math.min(stamina, MAX)`。
- **修复（app.js）**：
  1. 删 `load()` 中的超限收敛行（仅保留 NaN 兜底）。
  2. `gainGachaStamina()` 去掉 `Math.min` 封顶，直接累加（签到/前日登录/夺宝获取共用此函数，自然超12）。
  3. `updateGachaCenter()` 显示实际值 `${S.gacha.stamina}/${MAX}`（如 16/12）。
  4. `tick()` 自然恢复：保持 `Math.min(MAX, ...)` 封顶；新增 `else` 分支——当 stamina≥MAX 时持续推进 `lastTs=t`，避免掉回上限以下时把离线时间一次性补满（自然恢复不叠加超限值）。
  5. 调试钩子 `window.__cx` 暴露 `gainGachaStamina`、`updateGachaCenter` 便于测试。
- **测试（smoke.js）**：新增4条（超限11+5→16、自然恢复封顶12、超限不叠加、刷新后超限值保留=16），均 PASS。
- **验证**：端到端复现用户示例全通过；smoke 124pass/7fail预存(资产抽屉/Swiper/空闲车位，与本次无关)/0error。
- **版本**：index.html 三处 `?v=20260727s`

## dt — 好友家停车选车弹窗布局改为与自家停车卡片一致
- **需求**：好友家停车选车弹窗（`renderParkAtFriendModal`）的车辆卡片应与停在自己家里的车位卡片布局一致：①货币进度显示 `$当前/容量` 格式 ②评级徽章位置一致 ③品牌图标显示 ④车辆名称完整显示。
- **修改前**：使用 `vf-car-row` 左右分栏布局——左侧小缩略图+名称+状态，右侧文本信息（时间/收入/容量）+按钮。缺少 `$当前/容量` 格式、品牌图标、进度条，评级徽章位置不统一。
- **修改后**：
  - **HTML（app.js）**：`renderParkAtFriendModal` 完全重写卡片结构为 `pf-card` 网格卡片：
    - `pf-income-area`：`$当前/容量` 文字 + 绿色进度条（与 park-card 的 `pc-income-area` 一致）
    - `pf-body`：评级徽章(`pc-rating` 绝对定位右上) + 车图(`carImg` 140×90) + 品牌图标(`logoImg` 右下) + 车名居中
    - `pf-footer`：状态标签 + 时间 + 停车按钮（全宽）
  - **CSS（style.css）**：新增 `.vf-park-grid`(2列网格) + `.pf-card`/`.pf-income-*`/`.pf-body`/`.pf-img-wrap`/`.pf-car-name`/`.pf-footer`/`.pf-park-btn` 完整样式体系；删除旧的 `.vf-car-list`/`.vf-car-row`/`.vf-car-left`/`.vf-car-right`/`.vf-car-info` 样式。
  - **测试（smoke.js）**：更新选车弹窗测试选择器 `.vf-car-row`→`.pf-card`、`.vf-car-status`→`.pf-status`；收入数据改从 `data-iid`+`CAR_BY_ID` 获取（新布局不再显示"收入/分钟"纯文本）。
- **验证**：124 pass / 7 预存 fail(资产抽屉/Swiper/空闲车位) / 0 error。
- **版本**：index.html 三处 `?v=20260727t`

## du — 彻底移除拜访好友家的员工显示（用户要求"干脆不要了"）
- **背景**：此前 dk/上轮多次处理"好友员工显示"均未按预期（用户反馈显示的是好友的员工而非自己的、或样式不对），用户明确"干脆不要了"。
- **修改（app.js）**：
  - 删除 `renderVisitEmps()` 函数定义（原 renderVisitFriend 显示 `S.employees` 即玩家自身员工）。
  - 删除 `renderVisitFriend()` 中对 `renderVisitEmps()` 的调用。
  - 删除拜访好友视图（`renderVisitFriend`）右侧列内的 `<div class="vf-emps-area" id="vfEmps">` 容器。
  - 删除欢乐夺宝目标场景（`renderGachaTargetScene`）内的空 `<div class="vf-emps-area">`（虽无内容，一并清掉）。
- **修改（css/style.css）**：删除 `.vf-emps-area` / `.vf-emp-row` 样式。**保留** `.emp-slot*` / `.emp-name` 等（主界面 empbar 仍复用，不可删）。
- **验证**：无遗留引用（grep renderVisitEmps/vfEmps/vf-emps-area 全部无匹配）；node -c 通过；smoke 124pass/7fail预存/0error。
- **版本**：index.html 三处 `?v=20260727u`

## v — 拜访好友不显示未解锁车位
- **问题**：拜访好友界面显示了带锁的"未解锁好友专用车位"（🔒 5/10 好友 添加更多好友解锁），这些是玩家自己的解锁概念（好友专用车位需达一定好友数才解锁），与拜访好友无关，初始需求就没有此显示
- **修改**：`renderVisitFspots()` 中移除未解锁车位的渲染逻辑，改为只遍历已解锁数量 `unlockedCount`（基于 `[0,3,10,20]` 解锁条件计算），不再输出 `.park-card.locked` 卡片
- **验证**：node -c 通过；smoke 124 pass / 7 fail / 0 error
- **版本**：index.html 三处 `?v=20260727v`

## w — 修复好友家已停车辆卡片布局（与自家停车卡片一致）
- **问题**：之前任务 t 改错了位置——改的是「选车弹窗」内的车辆卡片，但需求实际是改「好友家停车位面板上已停的车辆」卡片布局。好友家已停车辆仍显示旧布局：`$963`（无容量）、「我的车」文字（无倒计时）
- **修改**：`renderVisitFspots()` 中已停车辆的卡片 HTML 重写为与 `renderParkCard()` 一致的布局：
  - 收入区：`$当前 / $容量` 格式 + 进度条 + 收取/取回按钮（放在进度条同行右侧）
  - 车图区：评级徽章右上 + 车图 + 品牌图标右下 + 车名（含强化等级）
  - 底部：剩余时间倒计时（`00:00:00` 格式，满仓显示"已满仓"），与自家完全一致
- **验证**：node -c 通过；smoke 124 pass / 7 fail / 0 error
- **版本**：index.html 三处 `?v=20260727w`

## x — 压缩"新的车友"页面间距，让推荐车友至少显示一个
- **问题**：「新的车友」页面中搜索框与邀请区域之间空白过大（红框位置），导致"推荐车友"被推到屏幕外看不到
- **修改**（纯 CSS）：
  - `.nf-section` padding: 14px → 10px
  - `.nf-invite` padding: 20px → 12px，margin: 10px 0 → 6px 0
  - `.nf-invite-img` font-size: 48px → 36px，margin-bottom: 8px → 4px
  - `.nf-invite-text` font-size: 13px → 12px，line-height: 1.6 → 1.5
  - `.nf-invite-reward` margin: 8px 0 12px → 4px 0 8px
  - `.nf-invite-btn` padding: 8px 36px → 6px 30px
  - `.nf-count-chip` padding: 4px 12px → 3px 12px，margin-bottom: 10px → 6px
  - `.nf-search-row` margin-bottom: 8px → 4px
  - `.nf-input` padding: 9px 12px → 7px 12px
  - `.nf-header` margin-bottom: 6px → 4px
  - `.nf-desc` margin-bottom: 10px → 6px
  - `.nf-bot-list` margin-top: 10px → 6px
- **验证**：node -c 通过；smoke 124 pass / 7 fail / 0 error
- **版本**：index.html 三处 `?v=20260727x`

## y — 修复其他玩家信息面板"最栽培我的老板"文案
- **问题**：`showUserInfoPopup()`（查看其他玩家信息面板）中显示「最栽培我的老板」，但这是看别人的信息，应改为第三人称
- **修改**：`app.js` 第 3488 行 `最栽培我的老板` → `最栽培Ta的老板`（仅改其他玩家面板，玩家自身面板保持"最栽培我的老板"不变）
- **验证**：node -c 通过；smoke 124 pass / 7 fail / 0 error
- **版本**：index.html 三处 `?v=20260727y`

## z — 玩家改名系统
- **需求**：个人信息面板名字后加✏️改名按钮→弹窗（输入框+🎲随机+确认）；首次免费/之后60000黄金；屏蔽字库；随机名=省份+百家姓+200常用名；重名"改名字被占用"；空白/屏蔽字"名字非法"
- **实现**：
  - 常量：RENAME_COST_BEANS、BANNED_WORDS(~50词)、RENAME_PROVINCES(33省市)、RENAME_SURNAMES(百家姓)、RENAME_GIVEN_NAMES(200)
  - 函数：generateRandomName/isNameValid/isNameTaken/openRenameModal/confirmRename
  - defaultState 新增 renameCount:0；renderProfile 加 ✏️按钮；action handler 3个；CSS 完整弹窗样式
  - __cx 暴露全部改名相关函数和常量
- **验证**：node -c 通过；smoke 137 pass / 7 fail(预存) / 0 error；新增13项测试全 PASS
- **版本**：index.html 三处 `?v=20260727z`

## aa — 思域TYPE R (ID51) 素材替换（去背透明底）
- **需求**：ID51 思域TYPE R 车辆素材替换为新的实拍照片，需去背保证透明底
- **处理**：源图 635×367 红色本田思域Type R 实拍 → PIL HSV 分割：
  - 红色高饱和区(sat>90) + 深色区(val<50) + 中等饱和度车身过渡 + 白色反光区
  - 形态学清理：MaxFilter(5)→MinFilter(5)→MaxFilter(3)
  - 裁剪非透明边界 → 缩放至 210×140
- **输出**：覆盖 `assets/cars/Red_Honda_Civic_Type_R_hot_hat_2026-07-24T03-40-46.png`
- **效果**：透明度 13.6%，不透明 81.6%，车身完整保留，背景干净
- **版本**：index.html 三处 `?v=20260727aa`

## ab — 好友列表项布局调整 + 好友家选车弹窗恢复原型
- **需求1**（好友信息区布局）：车库卡片中"在XX家"区域改为 [👤头像] 名字 + 进度条在名字下方且在头像右边，头像高度与名字+进度条对齐；整体点击可拜访好友
  - HTML: `gg-friend-info` 改为 flex 横向布局（avatar左 + right容器右），right 内 name + bar-wrap 纵向排列
  - 新增 `data-action="visit-friend-home" data-friend-uid` 点击事件
  - CSS: `.gg-fi-avatar` 40×40 圆角8px; `.gg-fi-right` flex纵向; `.gg-fi-name` 13px 800字重; hover 变色
- **需求2**（选车弹窗恢复原型）：之前任务t误改为 pf-card 卡片式，现恢复为行式布局（原型图）
  - HTML: `renderParkAtFriendModal()` 中 `pf-card` → `vf-park-row` 行式结构
    - 左侧(vfpr-left): 评级徽章 + 车图(100×62) + 品牌logo(右下) + 车名 + 状态标签(左下)
    - 右侧(vfpr-right): ⏱时间 + 💰收入/分钟 + 📦容量 + 停车按钮
  - CSS: 全新 `.vf-park-list`(纵向) / `.vf-park-row`(横向flex) / `.vfpr-*` 子样式
  - 删除旧 `.vf-park-grid` / `.pf-card` / `.pf-income-*` / `.pf-body` / `.pf-footer` 等
  - smoke 测试选择器更新: `.pf-card`→`.vf-park-row`, `.pf-status`→`.vfpr-status`
- **验证**：node -c 通过；smoke 137 pass / 7 fail(预存) / 0 error
- **版本**：index.html 三处 `?v=20260727ab`

## ac — 好友信息区名字恢复"在XX家"前缀
- **修正**：ab 版本误将名字从"在王宝强家"改为仅"王宝强"，例图要求保留"在...家"格式
- **版本**：index.html 三处 `?v=20260727ac`

## ad — 好友家选车弹窗精修（车图/定位/布局对齐车库）
- **问题**：ab/ac 版本选车弹窗车辆素材偏小(100×62)、状态标签位置错误、评级/品牌/名字排列不对
- **修改**：
  - HTML: `vfpr-left` 内重构为 `vfpr-img-wrap`(relative容器) + `vfpr-name-row`(品牌+名字)
    - 评级徽章 → absolute top-left 盖在图上
    - 车图 → 140×90（与车库一致）
    - 品牌logo → absolute bottom-right 图内
    - 状态标签 → absolute bottom-left 盖在图上（与车库卡片一致）
    - 名字行：品牌图标 + 车名 + 增强等级
  - CSS: 全部重写 `.vfpr-*` 样式，复用 `.pc-rating`/`.pc-brand-logo`/`.pc-car-img` 类名规则
  - 按钮颜色改为绿色(#22c55e)匹配原型截图
- **验证**：node -c 通过；smoke 137 pass / 7 fail(预存) / 0 error
- **版本**：index.html 三处 `?v=20260727ad`

## ae — 车库卡片好友信息区与收入区等高对齐
- **问题**：车库中"在XX家"好友信息区（头像+名字+进度条）比普通卡片的收入/容量区（2行文字）高很多，导致网格卡片高度参差不齐
- **修改**：
  - `.gg-friend-info` 和 `.gg-card-stats` 统一 `min-height: 52px`
  - 好友信息区紧凑化：avatar 40→34px、进度条 22→18px、名字 13→11px、gap 4→2px
  - 两侧内容垂直居中对齐（justify-content: center）
- **验证**：node -c 通过；smoke 137 pass / 7 fail(预存) / 0 error
- **版本**：index.html 三处 `?v=20260727ae`

## af — 思域TYPE R (ID51) 素材重新去背（第二次）
- **问题**：aa 版本处理后的素材背景仍有山坡/路面残留
- **改进**：
  1. 空间预裁切：切除顶部 108px（天空+大部分山坡），只处理车辆所在区域
  2. 更严格颜色阈值：红色 sat>95（原100）、深色 val<42（原40）、排除范围扩大
  3. 泛洪填充清除边缘连通透明区
  4. 最终透明度 26.7%，车身完整保留
- **版本**：index.html 三处 `?v=20260727af`

## ag — 好友家选车弹窗按原型重做（最终版）
- **问题**：ad/ae 版本选车弹窗反复修改后布局仍与原型不符，用户要求按原型图精确重做
- **原型要求**（对照截图）：
  - 左区：评级徽章(D) + 车名(绿色)横排 → 大车图(140×90) → 状态标签(左下角，带图标)
  - 右区：时间/收入/容量信息行 + 停车按钮（白底边框样式）
  - 状态标签复用车库 `gg-card-status` 样式（🏠闲置中绿色 / 🅿️停车中深色）
  - 左右区间有分隔线
- **修改**：
  - HTML: `vfpr-left` 内重构为 `vfpr-head`(rating+name) + `vfpr-img-wrap`(carImg+gg-card-status)
    - 删除 vfpr-name-row、品牌logo重复显示
    - 状态改用 `statusIcon+statusTxt` 格式，class 改为车库一致的 `gg-st-idle/gg-st-park/gg-st-friend`
  - CSS: 完全重写 `.vf-park-row` 为左右分栏布局（border-right 分隔线）
    - `.vfpr-head`: rating + 绿色车名横排
    - `.vfpr-img-wrap`: 140×90 车图容器，`.gg-card-status` absolute 定位左下
    - `.vfpr-right`: 信息行 + 白底边框按钮
  - smoke: 选择器 `.vfpr-status` → `.gg-card-status`，期望值加图标前缀
- **版本**：index.html 三处 `?v=20260727ag`

## ah — 有员工工作的车辆无法收回车库
- **需求**：点"收回车库"时，若该车有员工正在外出工作，拒绝并提示"当前车辆有员工外出工作，暂时无法收回。"
- **修改**：`recallCar()` 开头加检查——`inst.empIid` 存在且 `emp.workEnd > now()` 时 toast 拒绝并 return
- **版本**：index.html 三处 `?v=20260727ah`

## ai — 思域TYPE R素材缓存问题修复
- **问题**：af 版本已处理透明底素材并覆盖原文件，但浏览器缓存了同名旧图片（图片URL无版本参数），用户看到的仍是旧版
- **解决**：复制为新文件名 `Red_Honda_Civic_Type_R_v2.png`，更新 game_data.js 中 ID51 的 image 引用，强制浏览器加载新文件
- **版本**：index.html 三处 `?v=20260727ai`

## aj — 选车弹窗缓存问题排查
- **问题**：用户报告选车弹窗显示为纯文字列表（无车图/按钮），与 ag 版本代码不符
- **排查**：代码(app.js:3791-3849)和CSS(1280-1301)均正确，smoke测试137 pass含🏠/🅿️图标确认新代码运行正常
- **结论**：浏览器缓存旧版JS/CSS导致显示过时布局，bump版本至aj要求用户清缓存刷新
- **版本**：index.html 三处 `?v=20260727aj`

## ak — 车库卡片显示工作员工头像（车图左下角）
- **需求**：车库内的车辆，如果有员工在工作，在车图区域左下角显示员工头像（参考车行 pc-driver-overlay 样式）
- **修改**：
  - **app.js `renderGarageTab()`**：在图片区生成 `.gg-card-emp-overlay` 叠加层（28×28圆形紫渐变+红点+title+emp-info action）
  - **css/style.css**：新增 `.gg-card-emp-overlay` + `.gg-card-emp-status` 样式
- **验证**：node -c 通过；smoke 137 pass / 7 fail(预存) / 0 error
- **版本**：index.html 三处 `?v=20260727ak`

## al — 车库卡片员工头像改为信息区显示（去掉车图叠加层）+ 刷新持久化修复
- **问题1**：用户反馈 ak 版本在车图左下角加了员工头像叠加层，但实际只需要在信息区（统计/好友信息位置）显示员工头像，不需要车图上的
- **问题2**：刷新后界面员工栏仍显示忙碌，但车库车辆卡片上员工头像消失
- **修改**：
  1. **删除** `garageEmpHtml`（`.gg-card-emp-overlay` 车图叠加层）及其 CSS
  2. **新增** `garageEmpInfo`（`.gg-card-emp-wrap` 信息区行内组件）：在统计数据/好友信息区域下方显示 [👤头像+名字] 横排，参考车行 `pc-income-emp-wrap` 样式；busy 红框/idle 绿框；点击 emp-info
  3. 条件不变：`inst.empIid` 存在且 `gemp.workEnd > now()` 时渲染
- **验证**：node -c 通过；smoke 137 pass / 7 fail(预存) / 0 error
- **版本**：index.html 三处 `?v=20260727al`

## am — 福利（原首充）入口可领取时显示红点
- **需求**：福利入口（chip-fc）右上角在「奖励可领取」时显示红点
- **逻辑**：`updateInfobar()` 新增 `fcRdot = !fcClaimed && (S.fc.recharged || adsWatched>=5 || friendsInvited>=5)`（与 renderFirstCharge 的 canClaim 条件一致）
- **修改**：
  - app.js：`fcRdot` 加入结构签名 `_infobarSig`（末尾加一位），使红点状态变化能触发 infobar 重建
  - chip-fc 渲染加 `${fcRdot?'<span class=\"rdot\"></span>':''}`（与 chip-gacha/quest/sevenday 写法一致；修正了初次写入的双反斜杠转义 bug）
  - smoke.js：新增 4 项福利红点测试（初始无红点 / 充值达成→有红点 / 领取后入口隐藏）
- **验证**：node -c 通过；smoke 141 pass / 7 fail(预存) / 0 error
- **版本**：index.html 三处 `?v=20260727am`

## an — ID51 思域TYPE R 素材去背 v3（第三次处理）
- **问题**：用户反馈 ID51 思域 TYPE R 车辆素材仍有底图背景未去除（前两次 aa/af/ai 处理后因缓存/质量问题仍未解决）
- **处理**：
  - 使用 PIL 对源图（933×602 红色思域 + 浅灰摄影棚背景）执行去背：HSV色彩分割（低饱和+高亮度） + 边缘泛洪填充（4角+边缘种子，亮度>580扩散） + 形态学清理（MaxFilter膨胀3→MinFilter腐蚀5→MaxFilter闭合3） + 空间裁切至内容bbox(827×376) + LANCZOS缩放至 210×140
  - 输出文件名 `Red_Honda_Civic_Type_R_v3.png`（新文件名破缓存）
  - game_data.js ID51 的 image 字段从 v2 更新为 v3
- **结果**：透明区域占比 32.6%，车体完整保留
- **验证**：node -c 通过；smoke 141 pass / 7 fail(预存) / 0 error
- **版本**：index.html 三处 `?v=20260727an`

## ao — 去好友家停车弹窗布局修复（素材/按钮截断 + 状态底板过长）
- **问题1**：车辆素材和「停车」按钮显示不全（按钮文字被截断为"信左"）
- **问题2**：状态标签（🏠闲置中/🅿️停在自己家）底板过长，几乎占满车图区域宽度
- **修改**：
  - `.vf-park-row` 移除 `overflow: hidden`（导致右侧内容被裁剪）
  - `.vfpr-img-wrap > .gg-card-status` 新增 `width: fit-content; white-space: nowrap;`（强制状态标签自适应文本宽度）
- **验证**：node -c 通过；smoke 141 pass / 7 fail(预存) / 0 error
- **版本**：index.html 三处 `?v=20260727ao`

## ap — 安排员工工作选车弹窗车辆排序
- **需求**：安排员工工作选择车辆弹窗内车辆排序：未安排工作 > 已安排工作，车辆容量从高到低
- **修改**：
  - `app.js` `renderWorkArrange(eidx)`：对 `spotCars` 加 `.sort()` —— 先按 `empIid` 是否为空（空在前），同组按 `inst.capacity||CAR_BY_ID[carId].capacity` 降序
  - `__cx` 调试钩子新增 `renderWorkArrange` 导出（供测试调用）
  - `smoke.js`：新增 4 项测试（渲染4行 / 排序=未安排>已安排且容量降序 / 前2辆「上车工作」按钮 / 后2辆「立即召回」按钮）
- **验证**：node -c 通过；smoke 145 pass / 7 fail(预存) / 0 error
- **版本**：index.html 三处 `?v=20260727ap`

## aq — 车鉴详情弹窗车辆图片居中修复
- **问题**：银色闪电、Onyx、R.S.01 三辆车在车鉴详情弹窗（showCarInfo）内偏右未居中，但在车鉴网格中正常
- **根因**：`.ci-img-wrap` 用 `display:inline-block;text-align:center` 对固定尺寸 img 居中效果差；而车鉴网格 `.gg-gal-thumb` 用 `display:flex;align-items:center;justify-content:center` flex 居中
- **修改**：`.ci-img-wrap` 从 `display:inline-block;text-align:center` 改为 `display:flex;align-items:center;justify-content:center`（与车鉴网格一致）
- **验证**：node -c 通过；smoke 145 pass / 7 fail(预存) / 0 error
- **版本**：index.html 三处 `?v=20260727aq`

## ar — 思域TYPE R素材变形修复（object-fit + 重新处理）
- **问题**：ID51 思域 TYPE R 在车鉴详情弹窗内明显变形压扁
- **根因**（双重）：
  1. `carImg()` 函数使用内联样式 `object-fit:cover`，强制图片拉伸填满容器，覆盖全局 `.pc-car-img { object-fit: contain !important }` 规则
  2. v3 素材处理时被强制 resize 到 210×140（改变了原始宽高比 ~2.2:1 → 1.5:1）
- **修改**：
  - **app.js `carImg()`**：`object-fit:cover` → `object-fit:contain`（保持图片原始比例，不拉伸变形）
  - **v3 素材重新处理**：用 `thumbnail((210,140))` 替代 `resize((210,140))`，保持宽高比等比缩放至 210×95（~2.21:1），透明底不变
- **验证**：node -c 通过；smoke 145 pass / 7 fail(预存) / 0 error
- **版本**：index.html 三处 `?v=20260727ar`

## as — 开罚单按钮30分钟门槛UI限制
- **问题**：用户反馈停车仅33秒（显示00:33）就能成功开罚单，需求是停留<30分钟不可贴罚单
- **根因**：「开罚单」按钮渲染时未判断停车时长，始终显示可点击状态；虽然 `ticketFriend()` 内部有 `parkedMins < 30` 拦截，但 UI 层面误导用户
- **修改**：
  - **app.js `renderFspotGrid()`**：按钮渲染前计算 `parkedMins`（与 ticketFriend 完全一致逻辑），三种状态：
    1. 已贴单 → 灰色「已贴单」禁用
    2. 停车≥30分钟 → 红色「开罚单」可点击
    3. 停车<30分钟 → 黄色倒计时按钮（`m:ss`格式）禁用，显示剩余等待时间
  - **css/style.css**：新增 `.fspot-ticket-wait-btn`（黄底棕字等宽数字样式）
- **验证**：node -c 通过；smoke 142 pass / 8 fail(预存) / 0 error
- **版本**：index.html 三处 `?v=20260727as`

## at — TapTap 发布准备：SDK 对接 + 安卓封装
- **目标**：将首富车行发布到 TapTap 测试版，对接分享和广告 SDK
- **技术路线确认**：
  - 发布方式：安卓 APK 封装（Capacitor WebView），非 TapTap Maker Lua 引擎
  - 广告平台：TapTap 广告平台（用户已有开发者凭证）
  - 分享方式：先用系统分享面板（无需审核），后续再接微信/QQ/微博原生 SDK
- **完成的工作**：
  1. **文件同步**：根目录游戏文件 → `android-app/www/`（index.html, app.js, game_data.js, css/, assets/ 129张车素材）
  2. **ChexingSharePlugin.java**：Capacitor 原生插件，`share()` 调起系统 Intent 分享面板，`shareImage()` 支持图片分享（FileProvider）
  3. **ChexingAdPlugin.java**：Capacitor 广告插件，`showRewardVideo()` 激励视频接口（当前为测试模拟模式，待接入真实 TapTap 广告 SDK）
  4. **MainActivity.java**：注册两个插件
  5. **chexing-sdk.js**：JS Bridge 层，提供统一 API（`ChexingSDK.share()`, `ChexingSDK.showRewardAd()`, `ChexingSDK.initAd()`），浏览器环境自动降级
  6. **app.js 集成**：
     - `watchAd()` 改为调用 `ChexingSDK.showRewardAd()`，广告完整观看才计数
     - `doExchange()` 同上，兑换需先看完广告
     - 新增 `shareGame()` 全局分享函数 + `case 'share-game'` action
     - `boot()` 中自动初始化广告 SDK + 预加载
  7. **AndroidManifest.xml**：添加 ACCESS_NETWORK_STATE、ACCESS_WIFI_STATE 权限
  8. **index.html**：引入 chexing-sdk.js（在 app.js 之前）
- **待完成**：
  - 本机无 Java/Android SDK，无法本地构建 APK
  - ChexingAdPlugin 当前为测试模拟模式，需要用户提供 TapTap 广告 SDK 凭证后接入真实广告
  - 分享功能当前使用系统面板，后续可升级为微信/QQ/微博原生 SDK
- **验证**：node -c 通过（app.js + chexing-sdk.js）；smoke 142 pass / 8 fail(预存) / 0 error

## at — TapTap Maker 接入尝试与架构分析
- **用户需求**：将当前版本同步到 TapTap Maker 的「车行」项目并上传测试版本
- **已完成**：
  1. 安装 `@taptap/maker` MCP（`npx -y @taptap/maker install --ide codes,cursor,claude`）
  2. 初始化 Maker 项目（`npx -y @taptap/maker init` 在 taptap-maker/ 目录）
  3. 项目已连接 Git 远端（maker.taptap.cn），project_id: `9207c499-1c63-4263-b605-f3924b05ad4e`
  4. AI skills 已安装（claude/codex/cursor/gemini 各8个）
- **关键发现 — 架构不兼容**：
  - TapTap Maker 使用 **urhox Lua 游戏引擎**（基于 Urho3D），所有项目只能运行 Lua 脚本
  - 入口文件为 `main.lua`，UI 系统为 NanoVG + Yoga，无 WebView/HTML 渲染能力
  - 「首富车行」是纯 HTML/CSS/JS Web 游戏（localStorage），无法在 Lua 引擎中运行
  - **结论**：无论在 Maker 中新建还是使用现有「车行」项目，都无法直接运行我们的游戏
- **解决方案确定**：
  - 改用 **Capacitor 安卓封装方案**（android-app/ 目录已搭建完成）
  - 构建 APK 后通过 **TapTap 开发者控制台**（developer.taptap.cn）直接上传，绕过 Maker
  - 用户选择自行安装 JDK 17 + Android Studio 环境，后续指导构建
- **android-app 项目现状**：
  - www/: index.html, app.js(?v=as), game_data.js, chexing-sdk.js, css/style.css, assets/cars/(129张)
  - Java 插件: MainActivity.java, ChexingSharePlugin.java, ChexingAdPlugin.java
  - 配置: appId=com.chexing.game, appName=车行
- **待用户操作**：安装 JDK 17 + Android Studio → 执行 cap sync + gradlew assembleDebug → 上传 APK 到 TapTap

## av2 — 修复 TapTap Maker MCP「经常连接失败」
- **现象**：用户启用 taptap-maker MCP 后常连接失败；当前会话工具列表无 `mcp__taptap*` 工具，确认未连上
- **诊断**（用 child_process 拉起 server 做 JSON-RPC initialize 握手）：
  1. `@taptap/maker@0.0.27` 包可正常从 registry 拉取，网络无问题
  2. 原 `mcp.json` 命令 `cmd.exe /d /s /c npx.cmd -y -p @taptap/maker taptap-maker` —— 每次冷启动要现下包 **~12 秒**，超过连接器宿主启动超时 → 偶发失败；且 npm 缓存清理报 `genie-safe-delete`/`trash` 失败，进一步 destabilize
  3. 直接握手证明 server 本身正常（返回 `serverInfo: taptap-maker 0.0.27`，capabilities tools/resources 齐全）
- **修复**：
  1. 预装到受管工作区：`cd C:\Users\Administrator\.workbuddy\binaries\node\workspace && npm.cmd install @taptap/maker`（3s，无清理报错）
  2. 入口定位：`node_modules/@taptap/maker/bin/taptap-maker`（无扩展名 JS，需 node 运行）；`.bin/taptap-maker.cmd` 在无同目录 node.exe 时会回退到 PATH 的 `node` —— 连接器宿主 PATH 不一定有 node，故**不可直接用 .cmd**
  3. 改用受管 node.exe 直接跑入口，零 PATH 依赖、零 npx 下载：`mcp.json` command 改为 `C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2\node.exe`，args 改为 `C:\Users\Administrator\.workbuddy\binaries\node\workspace\node_modules\@taptap\maker\bin\taptap-maker`
  4. 实测握手 `connected=true` 且 stderr 干净 → 启动毫秒级稳定
- **注意**：改 `mcp.json` 后需重启连接器 / WorkBuddy 让其重载配置（已信任的 server 改 args 不会热生效）
- **结论**：TapTap 发布的正确路径仍是 Capacitor APK（见 at 两条）；MCP 仅用于 Maker 项目探查，且 Maker 用 urhox Lua 引擎与 Web 游戏不兼容，价值有限

## aw — 接入 TapTap 登录（原生 Android SDK，Capacitor 桥接）
- **路线**：用户确认用原生 Android SDK（非 Web OAuth），已有 client_id 待填
- **官方 SDK（v4）**：依赖 `com.taptap.sdk:tap-core:4.10.4` + `com.taptap.sdk:tap-login:4.10.4`，发布在 **Maven Central**（根 build.gradle 的 `mavenCentral()` 已覆盖，无需额外仓库）
- **桥接模式**（沿用 ChexingSharePlugin/ChexingAdPlugin）：新建 `android-app/.../java/com/chexing/game/ChexingLoginPlugin.java`，`MainActivity.onCreate` 里 `registerPlugin(ChexingLoginPlugin.class)`
- **关键 API**：
  - 初始化：`new TapTapSdkOptions(clientId, clientToken, TapTapRegion.CN)` → `TapTapSdk.init(context, opts)`（核心+登录统一初始化一次）
  - 登录：`TapTapLogin.loginWithScopes(activity, new String[]{Scopes.SCOPE_PUBLIC_PROFILE}, new TapTapCallback<TapTapAccount>(){ onSuccess/onCancel/onFail })`；结果取 `account.getOpenId()/getUnionId()/getName()/getAvatar()`
  - 退出：`TapTapLogin.logout()`
  - imports：`com.taptap.sdk.core.{TapTapSdk,TapTapSdkOptions,TapTapRegion}`、`com.taptap.sdk.login.{Scopes,TapTapLogin,TapTapAccount}`、`com.taptap.sdk.kit.internal.callback.TapTapCallback`、`com.taptap.sdk.kit.internal.exception.TapTapException`
- **JS 桥**：`chexing-sdk.js` 增加 `initLogin/login/getAccount/logout`（浏览器降级提示 unsupported_platform）；`app.js` 在 `defaultState` 加 `taptap:null`，`boot` 调 `ChexingSDK.initLogin()`，`renderProfile` 加登录状态区（按钮 `data-action="tap-login"` / 退出 `tap-logout`），新增 `tapLogin()/tapLogout()`，`__cx` 暴露
- **必填凭证（用户待提供）**：`ChexingLoginPlugin.java` 顶部 `TAPTAP_CLIENT_ID` / `TAPTAP_CLIENT_TOKEN` 占位符替换为开发者后台值；**且必须**在 developer.taptap.cn 把包名 `com.chexing.game` + 签名证书 MD5 配到应用，否则登录报 `signature not match`
- **坑**：①bundled `android/app/src/main/assets/public/index.html` 旧快照**漏了 chexing-sdk.js 脚本标签**，已补并加 `?v=20260727aw`；② proguard 需 `-keep com.taptap.**/com.tds.**`（已加）；③登录页默认横屏，竖屏游戏可能需 `screenOrientation` 配置（暂未设）
- **校验**：node -c app.js/chexing-sdk.js 通过；APK 因沙箱无 JDK/SDK 无法本机构建（见 at 的 GitHub Actions 方案）

## av/aw：APK 构建路径定论
- **沙箱实测**：小文件(<~100KB)可下载(18KB/s)，中/大文件(gradle 8MB 实测仅 169B)被限速断流 → 拉不动 JDK190MB/SDK150MB/Gradle100MB → **沙箱本地构建 APK 不可能**
- **破解软件请求：已拒绝**（侵权违法，且全部构建工具都免费：OpenJDK/Temurin、Android SDK cmdline-tools、Gradle、Capacitor 皆免费开源）
- **合法构建路径（二选一）**：
  1. GitHub Actions 云端构建（推荐，已就绪）：仓库根=android-app/，工作流 `android-app/.github/workflows/build.yml`（temurin JDK17 + setup-android + cap sync + assembleDebug，产物 app-debug.apk）。已 `git init -b main` 并提交首版（248文件，排除 node_modules/build）
  2. 用户本机：装 Temurin JDK17 + Android SDK cmdline-tools → `cd android-app && npm install && npx cap sync android && cd android && ./gradlew assembleDebug`
- **用户待办（沙箱无法代做 push）**：在 github.com/new 建空仓 → `cd android-app && git remote add origin <url> && git push -u origin main` → Actions 页看构建 → Artifacts 下 app-debug.apk
- **注意**：capacitor.plugins.json 为空是正常的（三个 ChexingXxx 插件是 MainActivity 手动 registerPlugin 的本地插件）；workflow 路径已修正为仓库根=android-app（之前 cd android-app/android 矛盾）

## az：沙箱到 GitHub 完全不通
- **实测**：`git ls-remote https://github.com/...` → "Empty reply from server"；`curl -sI https://github.com` → 超时(124)；`api.github.com` 偶发 200 但 curl exit 23。**结论：沙箱对 GitHub 的 git 协议/主页连接被重置/掐断**（与封大文件下载同类限制）
- **影响**：即便用户给 PAT，我也**无法从沙箱 push 或盯 Actions 构建**。GitHub Actions 路线必须由用户「能正常联网的本机」push 触发
- **已交付**：`git archive --format=zip -o ../chexing-android-src.zip HEAD` 生成干净源码包（39MB，排除 node_modules/build，关键文件齐全），供用户下载回本机构建/推送。位置：`2026-07-23-12-28-02/chexing-android-src.zip`
- **用户待办（沙箱代做不了）**：①本机解压 → `npm install && npx cap sync android && cd android && ./gradlew assembleDebug`（需本机 Temurin JDK17+Android SDK），或 ②`git remote add origin <url> && git push` 触发 GitHub Actions 出 app-debug.apk ③可选填 TapTap 凭证进 ChexingLoginPlugin.java

## a0：TapTap 凭证已嵌入 + 版本升级
- 用户提供了真实 client_id / client_token（developer.taptap.cn），已写入 `ChexingLoginPlugin.java` 顶部常量并 `git commit`（已重新 `git archive` 生成含凭证的 `chexing-android-src.zip`，校验：占位符 0、node_modules 未混入）
- TapSDK 版本由 4.10.4 升到 **4.10.7**（tap-core + tap-login），与官方模块清单一致
- **注意**：凭证属敏感信息，日志不存明文；用户仍需在 TapTap 后台配 包名 com.chexing.game + 签名 MD5 才能登录不报 signature not match

## TapSDK 七模块接入完成 + APK 构建结论（下午续）
- 提交 179a92c：合规/更新/评价/正版/分享/排行/成就 7 模块全部接入（TapSdkHolder.java 统一初始化、ChexingTapPlugin.java 桥接、ChexingLoginPlugin.java 重写去掉 Scopes 导入）
- 前端：chexing-sdk.js v1.2.0（+11 方法）、app.js 个人页 TapTap 服务面板 + tt* 处理器、CSS 已同步 www/
- 关键坑：addAppId() 返回 void 不能链式；signingConfigs 必须在 buildTypes 之前；嵌套 git 仓库需 rm .git 后重新 add
- APK 构建结论：沙箱内无法本地构建（无 JDK/SDK，长任务被杀），但大文件下载已放开 → GitHub Actions 路径可行（.github/workflows/build-apk.yml 就绪）
- _build/build_apk.sh 已重建（曾被清空），供用户本机构建 debug 包
- 待办：等用户提供 GitHub 仓库地址 + PAT 即可 push 触发云构建；TAPTAP_LEADERBOARD_ID 在 app.js 中为空需用户填写

## 思域TYPE R 车辆图片修复（尺寸+色调）
- 问题：v3 图片 210×95（其他车均为 210×140），高度差 1/3 导致车位卡片中显示偏大；红色车漆饱和度不足
- 修复：ImageGen 基于用户参考图重新生成（高保真 input_fidelity=high）→ Pillow 裁剪到标准 210×140 透明底 PNG
- 文件：`Red_Honda_Civic_Type_R_v3.png` → `Red_Honda_Civic_Type_R_v4.png`，game_data.js 引用已更新，已同步 android-app/www/

## 车行车位卡片移除车图上员工头像叠加层
- 问题：renderParkCard() 中 pc-driver-overlay（车图左下角小圆圈头像）不应显示（截图2箭头位置）；员工信息已在收入进度区(pc-income-emp-wrap)显示，重复了
- 修复：删除 workEmpHtml 整个 if 块（原 line 1471-1484 的 pc-driver-overlay 渲染），变量保留为空字符串
- 车库 gg-card-emp-wrap 信息区显示不变（截图1箭头位置，正确）
- 验证：pc-driver-overlay 已从 app.js 完全清除（grep 零匹配）；语法通过；已同步 android-app/www/

## 清理项目内未使用的素材资源
- 方法：Python 脚本交叉对比 /assets 全部文件 vs 代码(game_data.js 的 image/logo 字段 + app.js/index.html/css/style.css 引用) → 生成未使用清单
- 删除 45 个未使用文件（源 /assets：180→135 文件，释放 ~29.4MB）：
  - 根 assets/：8 张 AI 生成的货币图标候选(A_bright_green/A_cartoon_golden/A_single_*_cut/A_single_golden_*/A_single_US_dollar/Remove_the_background) + swipe-finger-right.png
  - assets/cars/：24 张 AI 生成候选图(时间戳重复版、错误配色版、思域 v3/Bright_red 旧版、五菱宏光单独生成版等)
  - assets/ratings/：12 张 rating_A~S.png 及 @2x（游戏用 CSS 文本渲染评级，未用图片）
- 同步清理 3 处副本：android-app/www/assets（同 45 个 + 额外 4 个仅存在于该副本的孤立文件：buttons-sprite.png + 思域 Red_Honda_Civic_Type_R_v2/hot_hat/hot_hat_cut）、android-app/android/app/src/main/assets/public/assets（同 45 个）
- 安全策略：用 shutil.move 移到 _build/_asset_trash/（93 个文件副本，共 60MB），非永久删除，可恢复
- 校验：game_data.js 引用的 113 张图 + app/html/css 引用的 4 张资源全部仍存在 → 游戏不破
- 助手脚本留存：_build/scan_assets.py、clean_assets.py、verify_clean.py、unused_assets.txt（_build 已 gitignore）

## 车库员工显示位置修正（叠加到车图左下角，不再撑高卡片）
- 问题1：garageEmpInfo 在卡片信息区正常流中渲染（stats下方），导致有员工工作的卡片比其他卡片高出一段
  - 修复：将 garageEmpInfo 从 stats 下方移入 .gg-card-img 内部，用 .gg-card-emp-overlay 绝对定位在车图左下角（bottom:3px, left:3px, z-index:4），半透明白底+毛玻璃效果
  - 同时删除原 stats 下方的 garageEmpInfo 渲染位置（避免重复）
- 问题2：刷新后员工头像消失 —— 已排查：数据层正确(empIid/workEnd持久化)、CSS无display:none、渲染条件与员工列表一致(gemp.workEnd>now())
  - 可能原因：车库条件检查 workEnd>now() 在刷新后若工作时间已过则不显示（而员工列表可能因渲染时序差几ms仍显示busy）；车位卡片(pc-income-emp-wrap)不检查busy状态应不受影响
  - 本次修改后车库员工改为车图叠加层显示，更醒目；如问题持续需用户提供具体截图定位是车位还是车库
- CSS：新增 .gg-card-emp-overlay（absolute定位+半透明背景+backdrop-filter）+ .gg-card-emp-overlay .gg-card-emp-wrap（覆盖margin/padding）
- 验证：node-c 通过；已同步 android-app/www/

## 更名按钮方形底框（uip-rename-btn）
- 需求：个人信息面板玩家名字后的 ✏️ 改名按钮加方形底框，跟随在名字后
- 修改：css/style.css .uip-rename-btn 从 background:none;border:none 改为 26×26px 固定正方形 + rgba(0,0,0,.06) 浅底 + 1.5px 圆角边框(border-radius:6px) + flex-shrink:0 + hover 加深
- 验证：语法通过；已同步 android-app/www/

## APK 云构建启动（GitHub Actions）
- 用户提供了 GitHub PAT（ghp_yOc...jc9，账号 Leroy-kingj）。此前 az 日志记录的「沙箱到 GitHub 完全不通」已过时——权限放开后实测 api.github.com 返回 200、curl 可创建仓库
- 提交 82a16e1（fix: 车库员工叠加车图左下角 + 更名按钮方形底框 + 素材清理 + 车位卡片尺寸/箭头修正）；taptap-maker/ 仍正确排除在提交外
- 用 API 创建空仓库 Leroy-kingj/chexing-rich-car-dealer（HTTP 201）；git push master 成功
- Actions 工作流 build-apk.yml 自动触发：Run 30279647914 状态 in_progress（Build Android APK TapTap）
- 后台轮询脚本 _build/poll_action.py（task jsQOKD）监控构建完成，完成后拉取 logs
- 下载位置：GitHub 仓库 Actions 页 → Artifacts → app-release-apk（已签名 release）

## 车位卡片统一尺寸修复 v2（Grid 均分行高 + 箭头位置修正）
- **问题1（回退）**：上版用 `min-height: 238px` 撑卡片，导致所有状态（含空位/锁定）都过高超出屏幕
- **修复1**：删除 .park-card 的 min-height；改为 `.parking-grid` 加 `grid-auto-rows: 1fr; align-items: stretch; flex: 1; min-height: 0`——Grid 填满 swipe-panel 高度 → 两行均分 → 所有 4 张卡片自动等大
- **问题2（回退+重修）**：上版箭头 top:50% 反而更高了（箭头是 absolute 相对于 #homeview 整页，非仅卡片区）
- **修复2**：top: 50% → **56%（#homeview 中卡片区约占 28%~83%，中点≈56%）
- 验证：语法通过；已同步 android-app/www/

## 刷新后员工显示修复（beforeunload + 反向查找兜底）
- **问题**：安排员工工作后刷新/重进游戏，车行车位卡片和车库卡片均不显示工作员工（但员工列表正常显示忙碌）
- **根因分析**：
  1. 缺少 `beforeunload` 自动保存——虽然操作后有 save()，但页面关闭/刷新时若恰在 tick 之间可能丢失最后写入
  2. 渲染仅依赖 `inst.empIid` 单一路径——若该字段因任何原因丢失，无恢复机制
  3. 车库有严格 `gemp.workEnd > now()` 检查——时序敏感
- **修复（三重防御）**：
  1. 新增 `window.addEventListener('beforeunload', () => { save(); })` 保底保存
  2. `renderParkCard()` 加兜底：`inst.empIid` 为空时通过 `S.employees.find(e => e.workCarIid === inst.iid)` 反向查找，找到后自动修复 `inst.empIid`
  3. `renderGarageTab()` 同样加反向查找兜底
- 验证：node-c 通过；已同步 android-app/www/

## GitHub Actions 构建失败修复（cordova.plugins 缺失）
- **第一次构建 Run 30279647914 失败**：`MissingResourceException: Could not read script '.../capacitor-cordova-android-plugins/cordova.variables.gradle' as it does not exist`
- **根因**：`android-app/android/.gitignore` 第 93 行忽略了 `capacitor-cordova-android-plugins/`、`app/src/main/assets/public/`、`capacitor.config.json`、`capacitor.plugins.json`、`config.xml`——这些是标准 Capacitor 模板忽略规则（CI 通常跑 npx cap sync 重新生成），但我们的 CI 直接跑 Gradle 不跑 cap sync
- **修复**：
  1. 注释掉 android-app/android/.gitignore 中 5 条 Cordova/Capacitor 生成物忽略规则
  2. `git add -f` 强制添加 89 个文件（cordova plugins 6 个 + public assets 79 个 + config 文件 4 个）
  3. 提交 dd334e1 并 push
- **第二次构建 Run 30280323689 已触发 in_progress**，后台轮询监控中

## 思域TYPE R素材v5修复（侧面视角+透明底+宽度填满）
- **问题**：v4(210×140)虽尺寸正确，但是斜正面视角（近方形），在横向画布中宽度只占66%；且背景有白色不透明残留
- **根因**：ImageGen v4生成的是正面/斜前方视角，而所有正常车辆素材(car_01等)都是侧面视角横向撑满
- **修复**：
  1. ImageGen重生成：基于用户原图reference，指定side profile view + transparent background + high quality
  2. PIL精确裁剪：用红/暗色像素检测算法定位车辆实际区域(1006×356，宽高比2.77:1)，去除周围半透明边缘
  3. 等比缩放至210×140宽度优先填满，居中置于透明画布
- **文件**：`Red_Honda_Civic_Type_R_v5.png`（新），game_data.js ID51引用 v4→v5，已同步android-app/www/assets/cars/
- **效果对比**：v4宽度占比66%/斜正面/白底残留 → v5宽度100%/侧面视角/完全透明

## 蓝屏修复（app.js语法错误）+ 项目改名「抢车位：华夏崛起」
- **蓝屏根因**：app.js 第3148行多余 `}` —— 上轮员工刷新修复时 `garageEmpInfo` 的 if 块多加了一个闭合括号，导致 SyntaxError: missing ) after argument list，整个 IIFE 无法解析，页面只渲染了 index.html 的静态结构（返回按钮+标题）
- **修复**：删除第3148行多余的 `}`；node -c 验证通过
- **项目改名**：所有 UI 显示的「首富车行」→「抢车位：华夏崛起」：
  - SCREEN_TITLES 全部5个页面标题
  - setTitle fallback 默认值
  - 分享功能 title + contents
  - shareGame 默认 title/text/url
  - 公告欢迎语
  - index.html `<title>` 标签 + `<h1 id="tb-title">` 默认文字
- **版本 bump**：`?v=20260727aw` → `?v=20260727at`（强制破缓存）
- 已同步 android-app/www/

## 思域TYPE R素材v6（最终版：原图抠图+等比缩放）
- **问题**：v5用ImageGen重新生成导致视角变成侧面（与其他车斜前方视角不一致），底图有半透明残留
- **用户明确要求**：直接用原图抠图去背+等比缩放，不要ImageGen生图
- **处理**：PIL对用户原图(933×602)执行饱和度/亮度检测去背→边缘泛洪清理→裁剪(827×375)→等比缩放至210×140画布内(208×132)居中透明底
- **效果**：透明底 ✅ | 原图视角不变 ✅ | 宽度99%填满 ✅ | 红色车漆完整 ✅
- **文件**：`Red_Honda_Civic_Type_R_v6.png`，game_data.js引用v5→v6，已同步android-app/www/

## APK 云构建终于成功（Run 30283899001, success）
- **三次失败根因链**：
  1. Run 30279647914/30280323689 失败：缺 `cordova.variables.gradle` → 因 android-app/.gitignore 忽略 capacitor-cordova-android-plugins 等未跟踪；已 git add -f 89文件修复（前轮）。
  2. Run 30282592949 仍失败：`:capacitor-android` 模块来自 `node_modules/@capacitor/android/capacitor`，但 `node_modules` 被 gitignore 未进仓库 → Gradle 找不到子模块。**修复**：workflow 增加「Set up Node.js」+「npm ci」两步恢复依赖。
  3. Run 30282592949 编译失败：`ChexingTapPlugin.java:220 builder.addAppId()` 在 TapSDK 4.10.7 为 **private**；官方分享示例根本不调 addAppId（appId 由 TapTapSdk.init 的 CLIENT_ID 自动填充）。**修复**：删除该行；顺手把 Java 插件里残留「首富车行」默认文案改为「抢车位：华夏崛起」。
- **网络坑**：github.com:443 出口间歇性被重置/超时（api.github.com 正常）；最终靠带 PAT 的 HTTPS URL 多次重试（第4次成功）推送。
- **成果**：Run 30283899001 success；Artifact `app-release-apk`(15.8MB) → 本地下载提取 `_build/apk_out/app-release.apk`(16.3MB)。
- **提交**：887cd91(workflow npm ci) / 9537bbf(游戏修复:思域v6+蓝屏+改名) / 2ba2264(Java插件修复)。
- **下载**：GitHub Actions Run 30283899001 → Artifacts → app-release-apk；或用本地 `_build/apk_out/app-release.apk`。

## 登录/启动页 + 改名按钮修复（~00:28）
- **登录/启动页**：将原加载页(#loading)替换为全屏登录界面
  - 背景使用用户提供的splash图(assets/splash_login.jpg，蓝保时捷+城市天际线)
  - 显示游戏标题"抢车位：华夏崛起"+副标题"抢占城市车位 建造你的停车帝国"
  - TapTap登录按钮（渐变红橙底，圆角胶囊，统一风格）
  - "跳过登录"文字链接 + 底部版本号
  - 登录中显示loading遮罩(spinner + "正在连接TapTap...")
  - **流程变更**：boot()不再直接进入游戏；新增enterGame()函数控制进入时机
    - 已有登录态 → splash展示1.5s后自动进入
    - 未登录 → 等待用户点登录或跳过
    - 登录成功 → enterGame()过渡到主界面
  - **移除**：个人信息面板(renderProfile)中的旧TapTap登录区域（整块uip-taptap-row）
    - 替换为UID行末尾的小✓badge指示已登录状态
  - 新增action: splash-tap-login, splash-skip-login
  - 新增函数: splashTapLogin(), enterGame()
- **改名按钮位置修复**：
  - 名字和按钮包裹在.uip-name-row(display:inline-flex)容器中
  - margin-left从6px改为**10px**（固定距名字末尾10px）
  - 按钮尺寸微调26px→24px更紧凑
  - 自适应名字长度（inline-flex自然跟随）
- **文件改动**：index.html(loading区重写), app.js(boot/enterGame/tapLogin/renderProfile), css/style.css(登录页样式+改名按钮+badge), assets/splash_login.jpg(新)
- **版本号**：?v=20260727au，已同步android-app/www/

## APK命名+图标更换（~00:34）
- **APK输出名**：workflow新增"Rename APK to qcwhxjq"步骤，构建后cp为qcwhxjq.apk；artifact名从app-release-apk改为qcwhxjq
- **APK图标**：用户提供的蓝车俯视车位图(assets/app_icon.jpg, 1920x1920)
  - PIL生成5个密度的ic_launcher.png + ic_launcher_round.png (48/72/96/144/192px)
  - 生成5个密度的adaptive icon foreground (108/162/216/324/432px)
  - 覆盖android-app/android/app/src/main/res/mipmap-*/全部图标文件
- **提交**：ef2dab4，已推送触发新构建
