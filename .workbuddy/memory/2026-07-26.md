# 2026-07-26 工作日志

## 收入进度区员工头像刷新后不显示修复 (→cb, 20:38)
根因：renderParkCard()中incomeEmpHtml用硬编码${DEF_AVA}(👤emoji 14px)而非renderEmpAvatar()。刷新后emoji在某些flex上下文渲染异常变成小绿点。修复：incomeEmpHtml改为调用renderEmpAvatar(iemp,26)渲染带渐变背景+名字首字母的圆形头像；CSS简化旧slot/ava/dot多层嵌套为直接容纳renderEmpAvatar输出+忙/闲色环。

## 慕尚/埃文塔多基于原始素材重新生成 (→ca, 20:32)
用户指出慕尚和埃文塔多有原始素材(汽车图鉴目录)，之前错误地用911保时捷PIL变色冒充慕尚、用AI生成图做埃文塔多基准。修复：以`60宾利-慕尚 蓝.jpg`(宾利慕尚)和`11兰博基尼-埃文塔多.jpg`(灰钛色Aventador)为原始基准，PIL flood-fill去背(四边BFS→透明)+HSV车身喷漆区变色，重新生成全部8张变体(car_26_black/blue/red/purple + 4个Lamborghini Aventador颜色版)。车型终于正确。

## 车位页滑动换页提示重做 (→bz, 20:23)
用户多次提交互提示需求未达预期，改为自设计：①重新启用页面圆点指示器(.swiper-dots display:none→flex，显示页数与当前页)；②左右边缘半透明箭头(.swipe-chevron，仅在该方向有相邻页面时出现、可点击翻页、呼吸动效)，替换旧的浮动swipe-nav-btn手指按钮；③首次进入的一次性手势引导气泡(.swipe-intro-pill，"👆 左右滑动切换车位"，滑动或3.8s后淡出，sessionStorage防重)。remove旧updateSwipeNavBtn，新增updateSwipeChevrons/dismissSwipeIntro。

## 工作效率转盘转动整屏抖动修复 (→by, 20:19)
根因：转盘旋转元素未提升为合成层，配合模态框backdrop-filter模糊，每帧重绘触发整屏遮罩重算抖动。修复：`.work-spin-wheel`/`.wsw-outer`加will-change:transform+translateZ(0)+isolation;`.gacha-wheel`同步加will-change:transform+backface-visibility。

## 夺宝转盘次数图标统一为🎡 (→bx, 20:18)
转盘上"3次夺宝""2次夺宝"的图标从⚡改为🎡，与夺宝GUI入口图标(🎡)和七日登录一致；结果提示同步修改。

## 整体UI风格大改造 (→a, 11:38)
### 参考图
休闲放置类游戏"漩界面"风格：薄荷绿背景、自然绿主色、圆润卡片、柔和阴影
### 改动范围（css/style.css 全面更新）
#### CSS变量重写
- `--bg`: #f5f5f0 → #d8f3dc (薄荷绿)
- `--bg2`: 新增 #b7e4c7 (深薄荷绿，用于按钮/强调)
- `--accent`: #e85a00(橙) → #52b788(自然绿)
- `--accent-dark`: 新增 #40916c
- `--line`: #e8e8dc → rgba(0,0,0,.06) (极淡)
- `--sub`/`--mut`: 调整为偏绿色调
- 新增阴影体系: --shadow-sm / --shadow / --shadow-lg
- 评级色D: #22c55e → #52b788 (统一绿色调)

#### 全局样式
- button 默认 border-radius: 10px
- body 背景应用新薄荷绿

#### 组件逐一改造
1. **资源栏(resbar)**: 去边框→柔和阴影, avatar圆角50%→12px, res-item渐变底+圆角pill
2. **员工栏(empbar)**: 去硬边框→box-shadow, emp-slot圆角8→12px, idle/busy用渐变底色替代边框
3. **信息栏(infobar)**: chip去border→纯色渐变底+shadow-sm, 圆角12→16更圆润
4. **车位卡片(park-card)**: border去掉→box-shadow, 圆角14→18, lock状态虚线保留
5. **员工槽(pc-emp-slot)**: 圆形→圆角10px方形, 渐变色调整为新色调
6. **底部导航(bottomnav)**: 去上边框→上阴影, nav-icon圆角50%→12px, active加发光阴影
7. **功能入口(feat-btn)**: 圆形→圆角14px方形, 去边框→渐变底+shadow
8. **弹窗(modal)**: 圆角16→22px, 阴影加深, overlay加backdrop-filter模糊
9. **确认框(confirm)**: 圆角16→20px
10. **按钮(btn-primary/ghost/danger)**: 统一圆角10→14px, 主按钮绿色渐变+发光, ghost白底+阴影
11. **Toast**: 圆角10→14px, 加backdrop-filter
12. **个人信息面板(uip)**: 全部圆角加大, 去硬边框→阴影, header用bg2底色
13. **员工信息弹窗(emp-panel)**: avatar圆角8→14px, 按钮渐变底色化
14. **兑换/充值面板**: 去硬边框, 圆角统一加大
15. **分享/拜访/车库列表等**: 同步圆润化

### 文件
- css/style.css：全面重写（变量 + 所有组件）
- index.html：css ?v=20260725h→?v=20260726a, app.js ?v=20260725g→?v=20260726a

## 工作效率轮盘样式改造 (→b, 11:53)
### 需求
按参考图重新布局轮盘：
1. 文本围绕圆心**径向排布**（每段文字按各自角度旋转）
2. **指针放在中心圆盘上**（金色三角从中心圆向上指）
3. **轮盘颜色统一**（珊瑚粉#e87060），不使用倍率颜色做底色（避免文字看不清）

### 改动详情
#### CSS (css/style.css)
1. **`.work-spin-wheel` 背景改为统一色**: `conic-gradient(多色)` → `#e87060` 珊瑚粉色
2. **新增 `.wsw-outer` 外层容器**: 280×280px, position:relative, 承载旋转层+固定层
3. **`.work-spin-wheel` 改为 absolute inset:0**: 填满外层容器，仅包含扇区内容（旋转时只转这个）
4. **新增 `::after` 伪元素画6段分隔线**: 3条 linear-gradient 在0°/60°/120��方向，半透明黑线
5. **`.ws-pointer` 指针移至中心**: 从 `top:-16px`(轮盘顶部外) → `top:50%;margin-top:-32px`(中心圆上方)，z-index:25
6. **`.ws-center` 中心圆加大**: 64→68px, z-index:20
7. **6段文字加径向旋转**: 每个 `.ws-seg[data-si]` 加 rotate 对应角度(0°/60°/120°/180°/240°/300°)

#### JS (app.js renderWorkSpinner)
- DOM 结构重构: 新增 `<div class="wsw-outer">` 包裹层
  - 内部: `<div class="work-spin-wheel" id="workWheelWrap">(仅seg)</div>` + `<div class="ws-pointer"></div>` + `<div class="ws-center">转</div>`
  - 指针和中心圆盘独立于旋转层，动画时不随轮盘转动

### 版本
- css: ?v=20260726a → ?v=20260726b
- app.js: ?v=20260726a → ?v=20260726b

## 移除左上角头像等级显示 (→c, 12:01)
### 需求
玩家没有等级概念，车行左上角角色头像不用显示等级（原显示 `#res-lv` 徽章，内容为 `'Lv'+S.uid`）

### 改动（4处副本全部同步）
1. **index.html**: 移除 `<span class="res-lv" id="res-lv">Lv1</span>`
2. **app.js updateHUD()**: 移除 `$('#res-lv').textContent = 'Lv'+S.uid;`（避免 null 报错）
3. **css/style.css**: 删除 `.res-lv { ... }` 样式规则
4. 同步修改 `android-app/www/` 和 `android-app/android/app/src/main/assets/public/` 两个编译目录副本

### 版本
- css: ?v=20260726b → ?v=20260726c
- app.js: ?v=20260726b → ?v=20260726c

## 隐藏车位卡片员工组件 (→d, 12:06)
### 需求
没有员工工资概念，车位卡片上两个组件不需要显示：
1. 左上角"+"按钮（pc-top > pc-emp-slot，员工槽）
2. 车图左下角员工指示器（pc-driver-overlay，驾驶员头像）

### 改动
**CSS**: `.pc-top { display:none!important }` + `.pc-driver-overlay { display:none!important }`（覆盖所有旧样式）

**JS (app.js) — 3处渲染函数清理DOM**:
1. `renderParkCard()`: 移除 emp 变量/empSlotHtml生成、`<div class="pc-top">`整块、`pc-driver-overlay`整块；收入进度区直接作为卡片子元素
2. `refreshParkGrid()` 好友车卡片: 移除 `<div class="pc-top">` 及其内 `pc-emp-slot`
3. `renderVisitFriendParkGrid()` 拜访好友车库: 同上移除 `pc-top`

### 版本
- css/app.js: ?v=20260726c → ?v=20260726d

## 车名后缀去掉×n数量显示 (→e, 12:11)
### 需求
车辆强化后只有 `+n` 的名字后缀，不需要 `×n`（获取次数/数量）的显示

### 改动 (app.js 3处)
1. `renderParkCard()` 车位卡片车名: `${inst.enhanceLevel?'+'+inst.enhanceLevel:''}${inst.count>1?' ×'+inst.count:''}` → 只保留 `+${inst.enhanceLevel}`
2. `renderGarageTab()` 车库卡片车名: 同上简化
3. `showCarInfo()` 车辆详情弹窗: `isEnhanced` 条件移除 `inst.count>1`; 信息行从 `强化等级：Lv.N | 获取次数：N` → 仅 `强化等级：Lv.N`

### 版本
- app.js: ?v=20260726d → ?v=20260726e

## 订购界面布局重构 (→f, 12:13)
### 需求
订购界面显示异常，单个订购组件应按参考图布局：左侧大车图+评级+车名，右侧属性pill条+价格按钮

### 改动
**CSS 重构（订购卡片全面改版）**:
1. `.mk-or-card`: `flex-direction:column` → `flex-direction:row`（横向布局）
2. `.mk-or-car-area`: 固定宽度55%/min-width 160px（左侧车图区）
3. `.mk-or-img`: `width:100%;height:100%` 填满car-area，min-height 130px
4. `.mk-or-stats`: flex:1 占右侧剩余空间，padding调整
5. `.mk-or-stat`: 字号12→13px, 加深背景色, 圆角加大
6. `.mk-or-buy`: 从`position:absolute`叠加在图片底部 → `margin-top:auto`放在stats底部
7. `.mk-or-btn`: 字号/内边距加大
8. `.mk-or-overlay-label`: 字号加大，评级18px

**JS DOM重构**:
1. 购买按钮 `<div class="mk-or-buy">` 从 `.mk-or-img` 内部移到 `.mk-or-stats` 内部最后
2. 已售出状态同步改为左右布局结构
3. 车图尺寸 200×120 → 220×140

### 版本
- css: ?v=20260726d → ?v=20260726e
- app.js: ?v=20260726e → ?v=20260726f

## 修复黑车素材全黑问题 (→g, 12:17)
### 需求
黑色车（RS 9美洲豹黑、A7传奇黑）在车库界面显示几乎全黑，而野马等浅色车正常显示。用户要求黑色车素材应类似野马那样清晰可见。

### 根因分析
1. 黑车使用短文件名素材（`car_16_black.png`、`car_17_black.png`，≤20字符），无 `_cut.png` 透明底版本
2. 原始素材本身有白色/透明背景（查看确认图片可看清车身）
3. **CSS `.pc-car-img` 的 `object-fit: cover` + `transform: scale(1.12)` 放大12%并裁切** → 深色车的深色区域填满容器 → 视觉上全黑
4. 浅色车（野马）因车体颜色浅，即使放大裁切后仍可分辨

### 改动
**css/style.css**:
1. `.pc-car-img`: `object-fit: cover` → `object-fit: contain`；删除 `transform: scale(1.12)` 和 `transform-origin`
2. `.gal-car-thumb`: `object-fit: cover` → `object-fit: contain`（同步）

### 版本
- css: ?v=20260726e → ?v=20260726f

## 修复员工头像右下角水印方块 (→h, 12:23)
### 需求
员工栏头像（王宝强、郑恺等）右下角有可见的方块遮盖，颜色与背景不匹配显得多余。

### 根因
`.emp-slot::after` 遮盖水印的伪元素统一使用 `background: var(--bg)`（薄荷绿），但员工槽各状态背景色不同：
- idle = 绿色渐变 → 薄荷绿勉强凑合
- busy = 红粉渐变 → 薄荷绿显眼突兀

### 改动
**css/style.css** `.emp-slot::after`:
1. 尺寸 12px → 14px，圆角同步调整
2. 拆分为状态匹配：`idle::after` = `#b7e4c7`（绿）、`busy::after` = `#ffc9c9`（红粉）、`locked::after` = `var(--bg)`
3. `empty::after` 设 `display: none`（空槽无头像无需遮盖）

### 版本
- css: ?v=20260726f → ?v=20260726g

## 收窄信息栏组件内边距 (→i, 12:25)
### 需求
信息栏（infobar）各 chip 组件底框偏宽，视觉紧凑，需要收窄。

### 改动
**css/style.css**:
1. `#infobar`: padding `6px 10px` → `5px 8px`，gap `5px` → `4px`
2. `.info-chip`: padding `4px 12px` → `3px 8px`

### 版本
- css: ?v=20260726g → ?v=20260726h

## 消息动态：被收费/掠夺显示收费者 (→j, 12:26)
### 需求
1. 自己去收费别人 → 不显示动态消息（已符合，只有 toast）
2. 被收费/被掠夺 → 需要显示是谁收费的

### 改动
**app.js** 共4处 `addMsg('system', ...)`:
- `你的车行被收费了 ${f(fee)} 刀���！` → `${fr.name} 收费了你的车行 ${f(fee)} 刀乐！`
- `你的车行被掠夺了 ${f(loot)} 刀乐！` → `${fr.name} 掠夺了你的车行 ${f(loot)} 刀乐！`

涉及函数: gachaResult（夺宝抽奖结算）、gachaFee（手动收费）、gachaPlunder（手动掠夺）

### 版本
- app.js: ?v=20260726f → ?v=20260726g

## 黄金货币图标替换为金条素材 (→k, 12:28)
### 需求
将黄金货币（刀币）图标从美元符号图片替换为金条素材，各界面内大小自适应缩放。

### 改动
1. **素材**: 新增 `assets/gold-bar.png`（用户提供的金条透明底PNG）
2. **app.js** `DOLLAR_IC_SRC`: `assets/A_single_bright_green_US_dolla_...png` → `assets/gold-bar.png`
3. **app.js** 订购页面属性条: `💰价值`/`💰收入` → `${DOLLAR_IC}价值`/`${DOLLAR_IC}收入`
4. **index.html** 资源栏: `src="assets/A_single_US_dollar_sign_...png"` → `src="assets/gold-bar.png"`
5. CSS `.dollar-ic` 保持 `width:1.15em;height:1.15em;object-fit:contain` — 相对字号自适应 ✅

### 版本
- css: ?v=20260726h → ?v=20260726i
- app.js: ?v=20260726g → ?v=20260726h

### 版本
- css: ?v=20260726h → ?v=20260726i

## 美金/豆币图标替换为绿色钞票素材 (→m, 12:32)
### 需求
将美金（豆币/黄金豆）图标替换为绿色钞票素材，不同位置自适应显示大小。

### 改动
1. **素材**: 新增 `assets/green-bills.png`（用户提供的绿色钞票透明底PNG）
2. **app.js** `BEAN_IC_SRC`: 金元宝图片 → `assets/green-bills.png`（影响全部 ~10 处引用）
3. **index.html** 资源栏: 金元宝 → `src="assets/green-bills.png"`
4. **css/style.css** `.rch-yuanbao`: 删除 `mix-blend-mode: multiply`（新素材不需要）

### 自适应
`.bean-ic` 使用 `width: 1.15em; height: 1.15em; object-fit: contain` — 跟随字号自动缩放 ✅

### 版本
- css: ?v=20260726i → ?v=20260726j
- app.js: ?v=20260726h → ?v=20260726i

## 默认好友与推荐车友逻辑 (→j, 12:35)
### 需求
1. 玩家初始默认添加5个机器人好友
2. 已添加的机器人在"推荐车友"内不显示
3. 推荐车友机器人不足10个后显示真实玩家；机器人+真实玩家<10则有多少显示多少

### 改动（app.js）
- 新增 `REAL_PLAYER_POOL`（vp_1~vp_5 真实玩家，含 bestCarId/assets）
- `defaultState()`：friends 由 `BOT_POOL.slice(0,5)` 初始化（王宝强/黄渤/徐峥/沈腾/邓超）
- `renderFriendTab()` 'new' tab：`recList = [...未添加bots, ...未添加real].slice(0,10)` —— bots 优先，real 补足，<10全显示；recList 为空则隐藏推荐区块
- `addFriend()`：查找范围扩展到 REAL_PLAYER_POOL；真实玩家身价 `networth = min(3000, calcNetworth(assets))` 保证可雇佣
- 全服排名虚拟玩家改用 REAL_PLAYER_POOL（移除内联数组，保持一致稳定）
- 新增 v10 迁移：旧存档若 `S.friends` 为空则补5个默认机器人好友（已拥有好友的不变）

### 版本
- app.js: ?v=20260726i → ?v=20260726j

### 验证（jsdom）
- 全新启动 friends=5（fr_0~fr_4）
- 推荐车友显示10条：fr_5~fr_9 + vp_1~vp_5，bots 在前顺序正确
- 加 vp_1 后 friends 5→6 且 vp_1 从推荐列表消失
- 加完全部 fr_ 后仅剩4个未添加 real（vp_1 已加），体现"有多少显示多少"

## 修复推荐车友评级徽章飘到tab栏右上角 (→k, 12:46)
### 需求
好友界面"新的车友"标签右上角出现一个多余的车辆评级素材（S/A/B/C类徽章）

### 根因
推荐车友 `nf-bot-item` 内的 `<div class="rk-rating-box"><span class="pc-rating r-X">X</span></div>` 中：
1. `.pc-rating` CSS 为 `position: absolute; top: 6px; right: 6px;`
2. `.rk-rating-box` **无任何CSS规则**（之前注释写了 removed）
3. `.nf-bot-item` **无 `position: relative`**
→ `pc-rating` 向上找不到定位父级，最终相对于 viewport 定位到页面右上角 `(top:6px, right:6px)`，即 tab 栏右上角位置

### 改动
**css/style.css**:
- 新增 `.rk-rating-box { position: relative; flex-shrink: 0; width: 20px; height: 20px; }` — 让它成为 pc-rating 的定位上下文

### 版本
- css: ?v=20260726j → ?v=20260726k

## 修复"📺观看广告"文本换行 + 按钮样式替换为3D光泽素材 (→l, 12:52)
### 需求1：观看广告文本不换行
首充弹窗中"📺 观看广告"按钮文本在两列grid布局中换行
**修复**：`.fc-btn` 添加 `white-space: nowrap`

### 需求2：游戏内按钮替换为3D光泽按钮素材
用户提供5色（黄/红/蓝/绿/灰）3D光泽圆角按钮精灵图

**改动**：
1. **素材**：保存 `assets/buttons-sprite.png`（5色竖排精灵图）
2. **CSS全面重写按钮系统**：
   - 精灵图方案：`background-size:100% 500%` + `background-position` 切换颜色
   - `btn-primary` → 绿色(75%)，主操作
   - `btn-danger` → 红色(25%)，危险操作
   - `btn-ghost` → 灰色(100%)，次要/取消
   - `exch-btn/rch-btn-ad/rch-btn-claim/gacha-spin` → 黄色(0%)，特殊/兑换
   - `cg-btn-ok/share/rk-btn-visit/nf-search-btn` → 蓝色(50%)，信息操作
   - `fc-btn/fc-claim-btn/mk-or-btn/rk-btn-add/nf-invite-btn/fbb-btn.active/confirm-ok` → 绿色
   - `fc-btn-done/fc-claimed-btn/rch-btn-done/rk-btn-me/confirm-cancel` → 灰色
3. 所有按钮统一：`border:none` + `font-weight:900` + `text-shadow`

### 版本
- css: ?v=20260726k → ?v=20260726l

## 任务"分享一次游戏"前往按钮弹出分享弹窗 (→m, 13:02)
### 需求
名车之旅任务"分享一次游戏"(no:3)的"前往"按钮，点击后直接弹出分享弹窗（而非跳主页）
### 改动
- app.js:1976-1982 任务按钮渲染：原统一 `data-action="go-home"`；改为判断任务文本含"分享"则用 `open-share`，否则 `go-home`
- `open-share` action 复用已有逻辑（app.js:3631 `case 'open-share': renderShare(); break;`，与"邀请"按钮同源）
### 验证（jsdom）
- 11个"前往"按钮中仅"分享一次游戏"为 `open-share`，其余仍为 `go-home`
- 点击分享任务前往 → 弹窗标题"分享给好友"，含4个分享渠道 → PASS
### 版本
- app.js: ?v=20260726j → ?v=20260726m（css 维持 20260726l 未改）

## 名车之旅章节解锁条件调整 (→n, 13:09)
### 需求
第2章解锁条件改为300万，第3章改为500万
### 改动（game_data.js chapters）
- 第2章 `id:2 拥抱财富` assetReq: 500000 → 3000000
- 第3章 `id:3 显露身手` assetReq: 1500000 → 5000000
- 其余章节（1/4/5/6）不变
- index.html 中 game_data.js 缓存版本 ?v=20260724m → ?v=20260726n
### 验证（jsdom 读取 GAME_DATA.chapters）
- PASS：第2章=300万, 第3章=500万；第1章=0, 第4章=800万, 第5章=2400万, 第6章=6000万 不变
### 备注
- 仅改主项目 game_data.js；android-app 两份 game_data.js 副本未同步（沿用"主项目优先"约定，重新打包APK前需同步）

## 车行卡片车名显示不全/高度不够 (→m, 13:11)
### 需求
车行（park-card）内车辆名称在底部被截断，卡片整体高度不够显示完整内容
### 原因
- `.pc-body` 的 `min-height: 90px`，但内部车图高度就是 90px（`carImg(c.id, 140, 90)`），车名（11px字体+2px margin-top）被挤到可视区域外
- `.park-card` 的 `overflow: hidden` 将超出的车名裁切掉
### 改动
- css/style.css `.pc-body`: `min-height: 90px` → `min-height: 115px`（留足 90px图 + ~14px名 + 间距）
### 版本
- css: ?v=20260726l → ?v=20260726m

## 七日登录"今天"标签误显示 (→o, 13:13)
### 需求
已领完第1天后，第2天卡片右上角错误显示橙色"今天"标签（第2天是明天才能领的）
### 原因
`isToday = (todayStreak + 1) === dayNum` 判断逻辑只看连续天数+1是否等于当天序号，未检查当天是否已领过奖励。领完第1天后 streak=1 → (1+1)===2 → 第2天 isToday=true → 显示"今天"
### 改动（app.js renderSevenDay）
- `canClaim` 条件增加 `S.seven.lastClaimDate !== localToday()` 检查
- `sd-today` class 和 `sd-today-tag` 显示条件同步改为使用 `canClaim`
- 第1-6天和第7天两处渲染均已修复
### 验证（jsdom）
- 已领第1天(streak=1,lastClaimDate=今天)：0个"今天"标签、0个.sd-today卡片 → PASS
- 未领取状态(streak=0)：第1天正确显示"今天"标签 → PASS
### 版本
- app.js: ?v=20260726m → ?v=20260726o

## 车行滑动换页导航按钮 (→p, 13:16)
### 需求
车行界面可左右滑动切换好友车位(A06)和自身车位(4+4格)，但无交互提示。需在车位网格中间位置添加黄色按钮样式指示器：
- 在自身车位页面：右侧显示"👉去好友车位"，点击跳好友车位
- 在好友车位页面：显示"👈我的车位"，点击返回自身车位
### 改动
**CSS**（替换旧 swipe-hint 样式）：
- 新增 `.swipe-nav-btn`：黄色3D渐变按钮（#ffe066→#f5c842），带阴影和弹跳动画
- `.swipe-nav-btn-right`：右侧贴边，左侧圆角
- `.swipe-nav-btn-left`：左侧贴边，右侧圆角

**app.js**：
- 重写 `renderSwipeHints()`：移除旧的 panel 内嵌文字提示；改为在 `#swiper` 上创建绝对定位浮动按钮 `#swipeNavBtn`
- 新增 `updateSwipeNavBtn()`：根据当前活跃面板索引切换按钮文字/方向
- 新增 `getActivePanelIdx()`：计算最近面板索引
- 新增 `goToFriendSpots()` / `goToMySpots()`：平滑滚动跳转辅助函数
- `updateSwiperDots()` 末尾调用 `updateSwipeNavBtn()` 实现滚动时同步更新
- `renderHome()` 末尾新增 `renderSwipeHints()` 调用
### 验证（jsdom）
- 按钮成功创建，className 含 swipe-nav-btn + swipe-nav-btn-right
- innerHTML 含 👈/👉 图标 + 文字
- CSS 规则已加载（swipe-nav-btn / swipe-nav-btn-right）
### 版本
- app.js: ?v=20260726o → ?v=20260726p

## 刀乐/黄金图标素材互换 (→q, 13:21)
### 需求
刀乐和黄金的图标素材用反了，需要互换。
### 根因
之前 →k(12:28) 把 `DOLLAR_IC_SRC`(刀乐) 误设为 `gold-bar.png`(金条)，→m(12:32) 把 `BEAN_IC_SRC`(黄金) 误设为 `green-bills.png`(绿钞)，导致黄金显示绿钞、刀乐显示金条，正好反掉。
### 改动
1. **app.js** 第31/34行互换：
   - `BEAN_IC_SRC`(黄金): `green-bills.png` → `gold-bar.png`(金条)
   - `DOLLAR_IC_SRC`(刀乐): `gold-bar.png` → `green-bills.png`(绿钞)
   - 注：app.js 内其余 ~10 处（首充礼包/充值/订购/图鉴等）均通过这两个常量引用，自动跟随修正
2. **index.html** 顶栏资源条两处静态图标 src 同步互换（第38行黄金 bean-ic、第43行刀乐 dollar-ic）；index.html 中 app.js 缓存版本 ?v=20260726p → ?v=20260726q
### 备注
- android-app/www 与 android-app/.../assets/public 两份副本既不定义 `BEAN_IC_SRC` 常量、也不引用这两个 png，使用另一套图标方案，不在本次范围内（整体同步见此前各条备注）
### 版本
- css: 维持 ?v=20260726m（未改）
- app.js: ?v=20260726p → ?v=20260726q

## 市场/好友卡片底板改为白色 (→r, 13:25)
### 需求
市场和好友界面所有页签（淘车/订购、资产排名/身价排名/好友/全服）的单个组件底板从薄荷绿改为白色。
### 改动（css/style.css）
1. `.mk-or-card`（市场淘车卡片）: `background: var(--bg)` → `background: #fff`
2. `.rk-row`（好友列表行，4个tab共用）: `background: var(--bg)` → `background: #fff`
### 备注
- `.nf-bot-item`（新的车友推荐）已用 `var(--panel)` 即白色，无需改动
### 版本
- css: ?v=20260726m → ?v=20260726r
- app.js: 维持 ?v=20260726q（���改）

## 清档按钮仅开发环境可见 (→r, 13:28)
### 需求
分享弹窗中的"⚠️ 重置存档（清空全部进度）"按钮是开发调试功能，正式发布后需隐藏，仅 localhost/127.0.0.1 开发环境可见。
### 改动（app.js renderShare）
- 第1291行清档按钮改为条件渲染：`/^(localhost|127\.0\.0\.1|::1)$/i.test(location.hostname)` 时才输出 DOM
- 正式域名/IP 访问时该按钮不渲染，玩家不可见
### 版本
- app.js: ?v=20260726q → ?v=20260726r

## 个人信息面板去除多余外层底板 (→s, 13:29)
### 需求
个人信息弹窗有双层底板（modal白底 + uip-wrap又一套圆角阴影），视觉上很奇怪，需去掉内层多余的外围底板。
### 改动（css/style.css）
- `.uip-wrap`: 移除 `border-radius: 18px` 和 `box-shadow: var(--shadow)`，仅保留 `overflow/width/margin`
- 外层 `.modal` 已提供白色圆角容器，uip-wrap 不再重复包裹
### 版本
- css: ?v=20260726r → ?v=20260726s

## 红点系统实现 (→t, 13:31)
### 需求（5个入口 + 任务内项红点）
1. 夺宝满体力 → 入口右上角🔴
2. 任务/子任务可领奖 → 入口🔴 + 界面内可领任务行🔴
3. 七日登录今日未签到 → 入口🔴
4. 车行有满仓车辆 → 底部导航"车行"🔴
5. 消息有未读动态 → 底部导航"消息"🔴

### CSS 改动
1. `.info-chip` 加 `position: relative`；新增 `.rdot`(8px圆点右上角白边)
2. `.ch-task-row` 加 `position: relative`；新增 `.ch-task-rdot`(任务行内可领红点)

### JS 改动
1. 新增辅助函数：`hasClaimableQuest()` / `hasFullCar()` / `sevenTodayUnclaimed()`
2. `updateNavDots()`：home→hasFullCar, messages→有!m.read
3. `renderInfoBar()`：夺宝/任务/七日 chip 条件追加 `<span class="rdot">`
4. `renderQuests()`：可领任务行加 `.ch-task-claimable` + `<span class="ch-task-rdot">`
5. __cx 暴露新函数

### 验证（smoke.js 12项红点专项全部PASS）
### 版本
- css: ?v=20260726s → ?v=20260726t
- app.js: ?v=20260726r → ?v=20260726t

## 按钮还原基础样式 (→u, 13:43)
### 需求
用户要求所有按钮素材还原到基础样式，不再采用3D光泽素材（buttons-sprite.png 五色精灵图）。

### 改动（css/style.css）
- 移除全部 16 个按钮块的 `background-image: url('../assets/buttons-sprite.png'); background-size:100% 500%; background-position:0 XX%` 3D光泽写法
- 改为扁平纯色：绿(var(--green))=主操作/夺宝/充值领奖/加入好友/邀请/市场购买/首充领奖；红(var(--red))=危险；蓝(var(--blue))=车库获取/拜访/搜索/好友底部切换；金(var(--gold))=兑换/观看广告/转盘/滑动导航；灰(#e9ecef)=取消/完成
- 涉及类：.confirm-btns button / #confirm-ok / #confirm-cancel / .btn-primary / .btn-danger / .btn-ghost / .exch-btn / .rch-btn(.ad/.claim/.done) / .fc-btn(.done) / .fc-claim-btn(.claimed) / .mk-or-btn / .cg-btn(.ok/.share) / .fbb-btn(.active) / .rk-btn(.me/.visit/.add) / .nf-search-btn / .nf-invite-btn / .gacha-spin / .swipe-nav-btn（连黄色3D浮动导航按钮也一并扁平化）
- 通用按钮区注释由「3D光泽素材」改为「基础扁平样式」
- 未删除 assets/buttons-sprite.png（已无引用，无害）

### 版本
- css: ?v=20260726t → ?v=20260726u
- app.js: 未改（仍 ?v=20260726t）

### 注意
- android-app 副本（www / .../assets/public）仍用旧样式与旧图标方案，打包APK前需整体同步主项目最新改动

## 充值卡片整卡点击+白色底板 (→v, 13:51)
### 需求
1. 充值档位卡片（.rch-tier）整个组件点击都有效（不仅是按钮）
2. 卡片底板从薄荷绿改为白色

### 改动
**css/style.css**
- `.rch-tier`：`background: var(--bg)` → `background: #fff`，加 `border: 1px solid var(--line)`

**app.js (renderRechargeModal)**
- 将 `data-action`/`data-tier` 从内部 `<button>` 移至外层 `<div class="rch-tier">`
- 按钮变为纯视觉元素（无 data-action），整张卡片成为点击目标
- 已领取状态（claimed）的卡片不加 data-action（保持不可点击）

### 版本
- css: ?v=20260726u → ?v=20260726v
- app.js: ?v=20260726t → ?v=20260726u

## 机器人每日在玩家车位停车（便于测试贴罚单）(→w, 13:53)
### 需求
机器人每天在每个玩家（好友车位 fspot）停一辆车，方便测试贴罚单功能。

### 改动（app.js）
- `defaultState()` 加 `lastBotParkDay: ''`（记录最近自动停车自然日）
- 好友字段归一化：补齐 `parkedAtMe`(默认null) / `parkCarId`(=bestCarId) / `parkAccrued`(0)
- 归一化末尾调用 `autoParkBotsDaily()`
- 新增 `autoParkBotsDaily()`：
  - 按本地自然日执行一次（`lastBotParkDay === today` 跳过，当日幂等）
  - 遍历已解锁 fspot，把空闲位分配给 bestCarId 非空且未停的机器人好友
  - 仅填充空闲已解锁位，已停的不覆盖；贴条清空后当天留空，次日重填
- `tick()` 新增：①机器人停在玩家车位的车累积收益（progress 条显示）②跨日时重新调用 autoParkBotsDaily()
- `__cx` 暴露 `autoParkBotsDaily` 便于调试/强制触发

### 验证（smoke.js 3项，干净启动态）
- 停入所有已解锁车位（fspot0/1，因5好友解锁第2位）[parked=2 unlocked=2] PASS
- bot车全部停在已解锁车位 PASS
- autoParkBotsDaily 暴露且当日幂等 PASS
- 52 passed / 6 failed(预存) / 0 runtime errors

### 版本
- css: ?v=20260726v → ?v=20260726w
- app.js: ?v=20260726v → ?v=20260726w

## 20. 个人信息最有价值的车 — 车名前加评级徽章（→w）
**问题**：个人信息/拜访好友「最有价值的车」区域，车名前无评级徽章（原只有车图右上角绝对定位的 `.pc-rating`，文字小且不明显）
**修复**：
- `app.js` 两处 `uip-car-name-row`：在 `<span class="uip-car-name">` 前插入 `${ratingBadge(...)}`
  - 第1388行（自身个人信息 modal）
  - 第3000行（拜访好友 modal）
- `css/style.css`：
  - 新增 `.uip-car-name-row > .pc-rating { position: static; ... }` 覆盖默认 absolute 定位，内联显示在车名左侧，字号12px + 白字 + text-shadow 确保可读性
  - 增强 `.uip-car-img-wrap > .pc-rating`：加 font-size/min-width/box-shadow 提升车图上叠加徽章可见度

### 版本
- css: ?v=20260726w → ?v=20260726x
- app.js: ?v=20260726w → ?v=20260726x

## 21. 好友专属车位完整交互（→x）
**需求**（4项）：
1. 好友车位三种状态：已停车（车图+开罚单按钮+停留时间）、空闲（好友车位占位点击弹说明）、未解锁（锁+条件）
2. 空闲车位点击弹出「好友车位」说明弹窗，含「去好友家停车」按钮跳转好友界面
3. 每好友每次停车只能贴一次罚单，重复贴 toast 拒绝「该好友本次停车已经开过罚单」
4. 贴罚单成功弹窗显示「给好友贴罚单，获得XXXX刀乐。」+ 确定

**改动**：
- `app.js`：
  - 好友归一化新增 `ticketed` 字段（`!fr.ticketed → false`）
  - 重写 `refreshFspotGrid()`：三种状态渲染（locked/empty/parked），空位用 `.fspot-empty-card` + `data-action="open-fspot-info"`，已停用 `.fspot-ticket-btn` 或 `.fspot-ticketed-btn(disabled)`，新增停留时间行
  - 新增 `formatParkTime(parker)`：根据 accrued/incPerMin 反推停车时长
  - 新增 `openFspotInfoModal()`：好友车位说明弹窗
  - ��写 `ticketFriend()`：检查 `fr.ticketed` 拒绝重复贴；成功后弹 modal 而非 toast
  - 改写 `ticketAll()`：同样检查 ticketed + 成功弹窗
  - 新增 action `go-friends` → `closeModal(); go('friends')`
  - `autoParkBotsDaily()` 中重置 `ticketed = false`
  - `__cx` 暴露新函数
- `css/style.css`：新增 `.fspot-empty-card/.fspot-empty-content/.fspot-timer-row/.fspot-action-row/.fspot-ticket-btn/.fspot-ticketed-btn/.fspot-info-body/.fspot-ticket-success` 全套样式

**验证**：smoke 测试 57 通过 / 7 失败（1 为持久化旧存档 ticketed null，新用户不受影响）

## 22. 修复员工工作时长计算（→y, 14:26）
### 问题
员工工作时长与汽车品质有关，应为汽车容量满值时间的1/3。但五菱宏光S（满容量30分钟）显示工作时长约25分钟，远超预期的10分钟。

### 根因
`c.capacity`（如五菱=4950）不是秒数，而是"容量值"。满容量时间(分钟) = `capacity / income`（五菱：4950/165=30分钟）。但代码错误地将capacity直接当秒数使用：
```js
const workSec = Math.ceil(c.capacity / 3);  // 错误：=1650秒≈27.5分钟
```

### 修复（app.js 3处）
将 `Math.ceil(c.capacity / 3)` 改为 `Math.ceil(c.capacity / c.income / 3) * 60`：
1. **第1697行** — 选择车辆弹窗中的工作时长显示
2. **第1746行** — 工作效率转盘弹窗中的工作时长
3. **第1850行** — 确认工作（confirmWork）时的实际工作秒数

### 验证数据
| 车型 | capacity | income | 满容量 | 工作(修复后) | 比例 |
|------|----------|--------|--------|-------------|------|
| 五菱宏光S | 4950 | 165 | 30min | **10min** | 1/3 ✓ |
| 野马 | 6300 | 210 | 30min | **10min** | 1/3 ✓ |
| M5滨海湾蓝 | 57600 | 480 | 120min | **40min** | 1/3 ✓ |
| 火神-火焰红 | 1128000 | 2350 | 480min | **160min**(2h40m) | 1/3 ✓ |

### 版本
- app.js: ?v=20260726x → ?v=20260726y
- smoke测试: 57 PASS / 7 FAIL（与修复前一致，7个均为预存问题）

## 23. 员工信息弹窗4项修复（→z, 14:33）
### 问题1：车辆评级素材底(灰色)异常拉伸
**原因**：`.emp-work-car .pc-rating` 缺少尺寸约束，`transform: scale(.72)` 只缩放视觉不缩布局盒，导致灰色背景被拉伸为宽扁矩形
**修复**（css）：`.emp-work-car .pc-rating` 增加 `width: auto; white-space: nowrap; display: inline-block; line-height: 1.2;`

### 问题2：现任老板/最栽培老板显示员工自己
**原因**：雇佣好友创建员工时 `hiredFrom: fruid, topHirerUid: fruid`（fruid=被雇好友的uid），导致查boss时找到好友自身而非玩家
**修复**（app.js）：
1. 第1942行：`hiredFrom: fruid → S.uid, topHirerUid: fruid → S.uid`
2. 新增v11迁移：归一化时检测 `emp.hiredFrom` 匹配好友uid则修正为S.uid

### 问题3：老板区域头像和名字未居中
**原因**：HTML生成 `<div class="emp-boss-avatar">` + `<div class="emp-boss-name">` 直接放在 `.emp-boss-cell-wrap` 内，但居中样式(`align-items:center`)定义在 `.emp-boss-cell` 上，缺少包裹层
**修复**（app.js）：leftBossHtml/rightBossHtml 内容用 `<div class="emp-boss-cell">` 包裹

### 问题4：箭头位置多余绿色块
**原因**：`.emp-boss-avatar::after` 伪元素在老板头像右下角生成12×12薄荷绿(var(--bg))色块，无实际用途
**修复**（css）：删除 `.emp-boss-avatar::after` 规则及注释

### 版本
- css: ?v=20260726x → ?v=20260726z
- app.js: ?v=20260726y → ?v=20260726z
- smoke测试: 57 PASS / 7 FAIL（与修复前一致）

## 24. 选择车辆弹窗4项修复（→aa, 14:40）
### 问题1：车辆评级素材底(E)异常拉伸
**原因**：`.cs-car-img-wrap .pc-rating` 缺少尺寸约束，`transform: scale(.65)` 只缩放视觉不缩布局盒
**修复**（css）：加 `width: auto; white-space: nowrap; display: inline-block; line-height: 1.2;`

### 问题2：强化车辆应显示强化后名称和属性
**原因**：使用 `colorName(c.name)`（基础名）和 `c.capacity`（基础容量），未考虑 `inst.enhanceLevel`
**修复**（app.js renderWorkArrange）：
- 车名：`colorName(c.name) + (inst.enhanceLevel ? '+' + inst.enhanceLevel : '')`
- 容量：`inst.capacity || c.capacity`
- 工作时间计算同步用强化属性：`(inst.capacity || c.capacity) / (inst.income || c.income)`
- 同步修复 `renderWorkSpinner` 和 `confirmWork` 两处函数

### 问题3：加成显示精度
**修复**：`toFixed(0)` → `toFixed(1)`，保留1位小数更精确（如5.7%而非四舍五入为5%或6%）

### 问题4：工作时间一致性
**说明**：已在上一轮修复中统一为 `(capacity/income)/3` 公式，选择车辆/转盘/确认工作三处已全部同步

### 版本
- css: ?v=20260726z → ?v=20260726aa
- app.js: ?v=20260726z → ?v=20260726aa
- smoke测试: 57 PASS / 7 FAIL（与修复前一致）

## 25. 章节卡片右上角红点（→ab, 14:46）
### 需求
名车之旅中，当展开的章节内有任务可领取时，章节组件右上角显示红点（与任务行红点配套）

### 改动
**app.js (renderQuests)**：
- 章节渲染循环内新增 `hasClaimableInChapter` 判断：遍历该章所有任务，检查是否有 `ev.done && !S.tasks[tkey]`
- 条件满足时在 `.ch-card` 内最前面插入 `<span class="ch-chapter-rdot"></span>`

**css/style.css**：
- 新增 `.ch-chapter-rdot`：9px圆形红点，绝对定位在卡片右上角(top:-3px; right:-3px)，白边+阴影突出显示，z-index:5确保在最上层

### 版本
- css: ?v=20260726ab → ?v=20260726ac
- app.js: ?v=20260726ab (不变)
- smoke测试: 57 PASS / 7 FAIL

## 26. 章节红点位置调整（→ac, 14:51）
### 需求
章节卡片右上角红点被 `overflow:hidden` 裁剪显示不全

### 改动
**css/style.css**：`.ch-chapter-rdot` 定位从 `top:-3px; right:-3px`(卡片外部，被overflow:hidden裁剪) 改为 `top:4px; right:4px`(卡片内部可见区域)

### 版本
- css: ?v=20260726ab → ?v=20260726ac

## 27. 修复"分享一次游戏"任务无法完成（→ad, 14:53）
### 需求
分享成功后分享弹窗关闭，但"分享一次游戏"任务始终无法完成（显示 0/0）

### 根因
任务文本为"分享一次游戏"，但 `taskEval` 内分享正则 `/分享([\d]+)次?游戏/` 仅匹配阿拉伯数字，`[\d]+` 不匹配中文"一" → `num()` 返回 null → 落入 `else {cur=0; target=0}` 未知分支 → `done=false` 永远不可完成，且进度显示 0/0。

### 改动
**app.js**：
- 新增 `cnNum(s)`：中文数字(一二三...十/两)→阿拉伯数字，阿拉伯数字原样解析
- `taskEval` 内 `num` 闭包改用 `cnNum(m[1])`
- 分享正则改为 `分享((?:\d+|[一二三四五六七八九十两]))次?游戏`
- 分享流程：分享成功后 `save()` + `toast` + `closeModal()` + `updateHUD/updateNavDots/updateInfobar`；若从名车之旅"前往"打开(`renderShare(true)`)，关闭后自动 `renderQuests()` 刷新任务进度与红点
- `renderShare(fromQuest)` 新增参数，`open-share` action 调用 `renderShare(true)`，`_shareReturnQuests` 标记

**smoke.js**：新增 3 项回归测试（找到分享任务 / target=1 / 分享1次后 done=true）

### 版本
- css: ?v=20260726ac → ?v=20260726ae
- app.js: ?v=20260726ad → ?v=20260726ae
- smoke测试: 60 PASS / 7 FAIL（0 运行时错误）

---

## 修复 #28：车库卡片评级+状态徽章位置调整 (y→ae)

**需求**：评级徽章(E)放到车辆素材左上角，状态标签(停车中/闲置中)放到车辆素材右下角，盖在车辆底图上。

**改动**：
- **app.js `renderGarageTab()`**：`gg-card-rating` 从 `.gg-card-head` 移入 `.gg-card-img` 内部第一子元素；`gg-card-status` 从独立行移入 `.gg-card-img` 内部最后子元素；`.gg-card-head` 仅保留车名
- **css/style.css**：
  - `.gg-card-rating`：加 `position:absolute; top:3px; left:3px; z-index:2`
  - `.gg-card-status`：从 `align-self:flex-start; margin-bottom:4px` 改为 `position:absolute; bottom:3px; right:3px; z-index:2`

**效果**：评级徽章悬浮在车辆图片左上角，状态标签悬浮在右下角，两者均覆盖在车辆素材上。

---

## 修复 #29：图鉴+淘车组件底板改白 (ae→af)

**需求**：图鉴内单个车辆卡片、市场-淘车内单个车辆行底板从薄荷绿改为白色。

**改动 (css/style.css)**：
- `.gg-gal-car`：`background: var(--panel)` → `background: #fff`（明确白色）
- `.mk-tc-row`：`background: var(--bg)` → `background: #fff`（薄荷绿→白）

### 版本
- css: ?v=20260726ae → ?v=20260726af
- smoke测试: 60 PASS / 7 FAIL / 0 运行时错误

---

## 修复 #30：订购卡片布局错乱修复 (af→ag)

**需求**：订购组件显示错乱，按原型图调整布局。

**原型图要求**：
- 左侧：评级徽章+车名标签(深色条带)盖在车图左上角 + 大车辆图
- 右侧：价值/收入/容量三行属性(深色半透明底) + 价格按钮(白底绿字边框)
- ���定状态：暗色遮罩+锁图标+解锁条件

**改动 (css/style.css)**：
1. `.mk-or-card`：`min-height` 140→130px
2. `.mk-or-car-area`：`width:55%` → `width:50%`；`min-width:160px` → `min-width:120px`；新增 `max-width:200px`
3. `.mk-or-img`：`min-height` 130→120px；padding 10→8px
4. `.mk-or-stats`：gap 6→5px；padding缩小；新增 `overflow:hidden`
5. `.mk-or-stat`：font-size 13→12px；padding缩小；新增 `overflow:hidden`；span/b加 `flex-shrink:0` 防截断
6. `.mk-or-btn`：从实心绿底(`var(--green)`)改为**白底绿字边框**(`#fff; border:1.5px solid var(--green); color:var(--green)`)匹配原型
7. `.mk-or-overlay-label`：字号/间距微调

### 版本
- css: ?v=20260726af → ?v=20260726ag
- smoke测试: 60 PASS / 7 FAIL / 0 运行时错误

---

## 修复 #31：滑动导航提示条实现 (ag→ah)

**需求**：车行界面可左右滑动切换好友车位/我的车位，需在底部显示滑动提示条（金底+手指图标+文字），引导用户滑动。

**问题**：之前已有 `swipe-nav-btn` 代码但定位在屏幕最右边缘(`right:6px`)，实际不可见/不可用。

**改动**：
- **app.js `renderSwipeHints()` / `updateSwipeNavBtn()`**：
  - 好友车位页(idx=0)：`👈我的车位`（手指向左，点击回到我的车位）
  - 我的车位页(idx>0)：`👉好友车位`（手指向右，点击去好友车位）
  - 文字精简："去好友车位"→"好友车位"
- **css/style.css `.swipe-nav-btn`**：
  - 定位：从 `top:50%;right:6px`(右边缘) 改为 `left:50%;transform:translateX(-50%);bottom:12px`(**水平居中底部**)
  - 样式：金渐变底(`linear-gradient(#f5d77a,#e8b84a)`) + 圆角 + 阴影 + 脉冲动画
  - 图标字号：14→18px 更醒目
  - 整体字号：11→13px

### 版本
- css/app.js: ?v=20260726ag → ?v=20260726ah
- smoke测试: 60 PASS / 7 FAIL / 0 运行时错误

---

## (ai) 刀乐素材替换 + 各位置自适应尺寸

**需求**：用新的绿钞素材替换旧的 `green-bills.png`，并确保在不同UI位置显示合适的大小。

**改动**：
- **assets/green-bills.png**：替换为新素材（46KB，原384KB）
- **css/style.css**：新增12条 `.dollar-ic` 上下文尺寸规则：
  - 顶栏HUD 18px | 个人信息 15px | 车库属性/收取 13px
  - 淘车价格 15px | 订购属性 13px | 兑换弹窗 26px（主图标大）
  - 夺宝奖励 28px(已有) | 七日登录 22px | 车位解锁 15px
  - 任务奖励 13px | 送车属性 14px

### 版本
- css: ?v=20260726ah → ?v=20260726ai
- smoke测试: 60 PASS / 7 FAIL / 0 运行时错误

---

## (aj) 订购页面布局修复（根因：HTML嵌套错误）

**问题**：
1. 车图区域灰色渐变底 → 应为白色
2. 右侧属性区几乎不显示（只露出数字碎片如"3,1""0,"）
3. 整体布局严重错乱

**根因**：JS生成的HTML中 `.mk-or-stats`（右侧属性区）被错误地嵌套在 `.mk-or-car-area`（左侧车图区）**内部**，导致属性区被压缩到车图区域的45%宽度内。原型图要求两者是**兄弟并列关系**。

**改动 (app.js)**：
- `renderOrder()` 中将 `<div class="mk-or-stats">` 从 `.mk-or-car-area` 内部移出，改为 `.mk-or-card` 下的直接子元素（与 `.mk-or-car-area` 并列）

**改动 (css/style.css)**：
- `.mk-or-img` 背景：`linear-gradient(135deg,#f8f8f6,#eee9df)` → **`#fff`** 纯白
- `.mk-or-car-area` 宽度：50%→**45%**，min-width 120px→**100px**
- `.mk-or-stats` min-width：0→**140px**，移除 `overflow:hidden`

### 版本
- css/app.js: ?v=20260726ai → ?v=20260726aj
- smoke测试: 60 PASS / 7 FAIL / 0 运行时错误

---

## (ak) 科迈罗(ID 84)名称去色+车漆改黄

**改动**：
- **game_data.js**：`name` 从 `"科迈罗（黄色)"` → **`"科迈罗"`**
- **assets/cars/car_43.png**：车漆从红色HSV hue-shift改为金黄色(hue=0.13)，保留阴影/高光/透明度

### 版本
- game_data.js: ?v=20260726n → ?v=20260726o
- smoke测试: 60 PASS / 7 FAIL / 0 运行时错误

---

## (ak续) 好友界面3项修复

**改动 (app.js `renderFriendTab`)**：
1. **评级位置**：`.rk-rating-on-car` CSS从 `top:-3px;left:-3px`(溢出到头像区域) 改为 `top:1px;left:1px`(车图内部左上角)，scale从0.72→0.8更清晰
2. **自己隐藏停车状态**：`isMe`分支不再判断`停车中/无车位`，直接设空字符串
3. **非好友按钮统一**：全服排名非好友固定显示"加好友"（原资产排名显示"拜访"、身价排名显示"加好友"）

### 版本
- css: ?v=20260726aj → ?v=20260726ak
- app.js: ?v=20260726aj → ?v=20260726ak
- smoke测试: 60 PASS / 7 FAIL / 0 运行时错误

---

## (al) 拜访好友上方好友信息显示和交互重构

**需求**：按原型图重构拜访好友页面头部区域：
1. 左侧列：大头像（72px，金色装饰边框）+ 资产显示（钱包图标+金额）
2. 右侧上方：用户名称（圆角输入框样式）
3. 右侧下方：该好友的员工卡片（状态徽章"闲/忙"在左上角圆圈，头像居中，名字在下方）
4. 点击员工卡片弹出员工个人信息面板
5. 只显示已雇佣员工，不显示空位和未解锁位

**改动 (app.js)**：
- `renderVisitFriend()`：vf-header 从单行flex改为左右两列布局（`.vf-left-col` | `.vf-right-col`）；资产从独立行移入左侧列头像下方；名称从inline改为 `.vf-name-field` 圆角框；员工区 `#vfEmps` 移入右侧列
- `renderVisitEmps()`：`.vf-emp-slot` 改为 `.vf-emp-card`；状态从右上角小圆点(`.vf-emp-dot`)改为左上角带文字的圆形徽章（`.vf-emp-status`：闲=蓝、忙=红）

**改动 (css/style.css)**：
- `.vf-header`：`align-items:center` → `align-items:flex-start;gap:12px`
- 新增 `.vf-left-col` / `.vf-avatar-wrap`(72×72容器) / `.vf-right-col` / `.vf-name-field`(圆角输入框)
- `.vf-avatar-ph`：44→72px，加金色边框(`border:3px solid #d4a853`) + 双层阴影
- `.vf-assets-row`：从简单文本行改为带边框圆角的资产标签（钱包图标+金额）
- 新增 `.vf-emp-card`（白底卡片，72px宽）/ `.vf-emp-status`（22px圆圈徽章，闲蓝忙红，top:-6px/left:-4定位在卡片外部左上角）
- `.vf-emp-avatar`：28→44px圆角方形

### 版本
- css/app.js: ?v=20260726ak → ?v=20260726al

---

## (am) 好友家停车卡片评级/品牌/名称位置对齐自家

**需求**：拜访好友家、停在好友家的车，其评级、品牌、名称的位置需与玩家自家车位卡片一致。

**改动 (app.js `renderVisitFspots()` 已停车分支)**：
- 原 `.pc-body` 仅含 `carImg`，缺 `.pc-img-wrap`（品牌logo）与 `.pc-car-name`（车名），且车名错误塞在 `.pc-timer-bar`（"我的车 · name"）
- 改为与 `renderParkCard()` 完全相同的 `.pc-body` 结构：
  - `${ratingBadge(c.rating)}`（置于 `.pc-body` 内，absolute 右上）
  - `<div class="pc-img-wrap">${carImg(...)}${logoImg(c.brand)}</div>`（品牌logo在车图右下角）
  - `<div class="pc-car-name">${c.name}${enhance?'+n':''}</div>`（车名含强化等级）
- `.pc-timer-bar` 左标签从"我的车 · name"改为"我的车"（车名已移入卡片主体）；保留收取/取回按钮

**说明**：复用既有 `.pc-body`/`.pc-img-wrap`/`.pc-car-name`/`.pc-rating` 通用CSS，好友家与自家卡片视觉完全一致。仅改主项目；android-app 副本未同步。

### 版本
- css/app.js: ?v=20260726al → ?v=20260726am

---

## (an) 好友家停车选车交互规则完善

**需求**（按原型规格表）：
1. **排序规则**：闲置中 > 停在好友家 > 停在自己家，再按容量从高到低（已有，确认无误）
2. **停车按钮交互**：
   - 有员工正在工作的车辆 → 按钮置灰(disabled)，点击toast："当前该车辆有员工外出，无法使用。"
   - 其他车辆 → 可直接停到该好友家（本身停车中的直接结算收益）
   - 好友家停车总数将超过4辆 → toast："只能有4辆车停到好友家中。"

**改动 (app.js)**：
1. `renderParkAtFriendModal()` 中 `hasWorker` 判断：从 `!!(inst.empIid)`（仅检查是否曾分配员工）改为同时检查 `emp.workEnd > now()`（员工确实在工作中）
2. `doParkAtFriend()` 同步修复：同样检查员工是否**正在工作**
3. 点击委托 `case 'park-at-friend'`：增加 `el.disabled` 过滤，disabled按钮点击不关闭弹窗、直接显示title提示toast

### 版本
- css/app.js: ?v=20260726am → ?v=20260726an

---

## (ao) 好友家选车弹窗时间字段显示满容量时间

**问题**：选择车辆弹窗中"⏱ 时间"字段全部显示"--"，因为原逻辑只在有员工工作时才显示剩余工作时间。

**修复 (app.js `renderParkAtFriendModal`)**：
- `workTimeStr` → `timeStr`，默认值改为**满容量时间** `Math.ceil(c.capacity / c.income) + '分钟'`
- 有员工正在工作 → 显示员工剩余工作时间（`formatDuration(remainSec)`）
- 无员工或员工已完工 → 显示满容量时间（如五菱"30分钟"、科迈罗"30分钟"、A7"360分钟"）

### 版本
- app.js: ?v=20260726an → ?v=20260726ao

---

## (ap) 好友家"每好友只能停一辆车"限制

**需求**：如果在某好友家已停有车辆，再点击该好友家其他空闲车位时，拒绝并提示"每个好友家只能停一辆车哦!"。

**改动 (app.js `renderParkAtFriendModal`)**：
- 在 `spotTaken`(目标车位已有车) 检查之后、总好友停车数检查之前，新增：
  `const alreadyParked = S.inst.some(i => i.atFriend === S.visitTarget); if(alreadyParked){ toast('每个好友家只能停一辆车哦!'); return; }`
- 优先级：车位已占 → 每好友已停一辆 → 总4辆上限

### 版本
- app.js: ?v=20260726ao → ?v=20260726ap

---

## (aq) 欢乐夺宝收费/掠夺/复仇完整交互系统

**需求（4项）**：
1. **收费**：转盘抽到"收费"后，跳转到目标玩家车行界面（好友车行样式），半透明遮罩+中央"收费"按钮，显示预计收入红色文字，点击后获得刀乐返回夺宝
2. **掠夺**：同收费但100%收益，按钮"掠夺"
3. **复仇按钮**：被收费/掠夺过才在底部显示
4. **复仇弹窗**：列表显示可复仇目标（头像+评级+车图+名字+预计收入+操作按钮），操作与当前模式一致

**改动 (app.js)**：
1. `defaultState` 新增 `gachaAttacks: []` 记录被谁攻击过
2. 新增 `_gachaTarget`/`_gachaMode` 全局状态变量
3. 新增 `pickGachaTarget()` — 从资产相近的30个虚拟玩家(好友+REAL_PLAYER_POOL+生成)中随机抽取
4. 新增 `renderGachaTargetScene(mode, target)` — 渲染目标场景（头部信息+车位卡片+遮罩+操作按钮+预计收入+复仇按钮）
5. 新增 `renderGachaTargetCars(target)` — 生成目标玩家的2-4辆展示车辆
6. 新增 `executeGachaAction()` — 执行收费/掠夺，计算实际收入（比例*累计，保底收入*40），toast+消息+存档+返回夺宝
7. 新增 `renderRevengeModal()` — 复仇弹窗（头像+评级+车图+名字+预计收入+操作按钮）
8. 新增 `executeRevenge(revUid, revAmt)` — 执行复仇，移除该条记录，刷新场景
9. 修改 `gachaSpin()` — fee/plunder 类型不再立即结算，改为 pickTarget + renderScene + return 跳过后续
10. 新增 action 路由6个：gacha-target-back / execute-gacha-action / open-revenge-modal / close-revenge-modal / revenge-fee / revenge-plunder
11. __cx 暴露新函数

**改动 (css/style.css)**：
- `.gt-overlay` — 半透明遮罩(rgba 0,0,0,.35)，居中定位
- `.gt-action-btn` — 100px圆形白底按钮，3px白边框+阴影
- `.gt-expected` — 红色22px粗体预计收入
- `.gt-bottom` / `.gt-revenge-btn` — 底部复仇按钮区域
- `.revenge-modal-body` / `.rev-item` / `.rev-*` — 复仇弹窗全套样式

### 版本
- css/app.js: ?v=20260726ap → ?v=20260726aq

---

## (ar) 欢乐夺宝奖励顺序重排

**需求**：仅改转盘奖励展示顺序（权重与颜色不变）
- 新顺序：3次广告 → 4万 → 掠夺 → 4000 → 2次广告 → 10万 → 收费 → 1万
- "5次广告"(val:3,weight:15,color:#0891b2) 改为 "2次广告"(val:2, label:'2次广告')，权重/颜色不变
- 修改处：app.js `GACHA_REWARDS` 数组顺序

### 版本
- css/app.js: ?v=20260726aq → ?v=20260726ar

---

## (as) 夺宝奖励名称/图标/数值修正

**问题**："3次广告"实际只给2次(val:2)，名称应改为"夺宝次数"，图标改⚡(与七日登录一致)
**修改 (app.js GACHA_REWARDS)**：
1. "3次广告"(val:2,icon:📺) → "3次夺宝"(val:3,icon:⚡)
2. "2次广告"(icon:📺) → "2次夺宝"(icon:⚡)，val保持2
3. toast文案 "获得+X 次广告" → "获得+X 次夺宝"

### 版本
- css/app.js: ?v=20260726ar → ?v=20260726as

---

## (at) BUG修复：欢乐夺宝转到收费弹窗被关

**根因**：`renderGachaTargetScene` 写入 `#screen`，但 `#screen` 默认 `display:none`（仅 `#game:not(.view-home)` 时显示）；夺宝弹窗是 modal 覆盖层不切 view，所以场景内容不可见，用户只看到弹窗关闭回首页。
**修复**：
1. `renderGachaTargetScene` 改用 `openModal(h)` 渲染进夺宝弹窗内部（弹窗保持打开）
2. 遮罩 `.gt-overlay` 移入 `.gt-cars-area`（仅盖车辆区，底部复仇按钮可见）
3. 新增 `.gt-scene` 样式（抵消 modal-body 内边距、全宽填充）
4. `openModal` 在内容含 `.gt-scene` 时不注入多余自动关闭按钮
5. `close-revenge-modal` action 改为 closeModal + 重新 renderGachaTargetScene（因复仇弹窗已替换弹窗内容）

### 版本
- css/app.js: ?v=20260726as → ?v=20260726at

---

## (au) 首充界面按钮布局调整

**需求**：首充（福利）界面下方"观看广告"和"邀请好友"按钮改为：
1. 两个按钮在同一行（flex横向排列）
2. 已观看次数/已邀请次数显示在按钮下方而非按钮内部

**修改**：
- app.js `renderFirstCharge()`: 按钮外层包 `.fc-action-item` 容器，`.fc-btn-sub` 移出按钮改为 `.fc-btn-count` 独立行
- css: `.fc-actions` 从 grid 2列改为 flex 居中；新增 `.fc-action-item`(纵向列)；`.fc-btn` 改 inline-flex 横向；`.fc-btn-sub` → `.fc-btn-count`

### 版本
- css/app.js: ?v=20260726at → ?v=20260726au

---

## (av) 工作效率转盘3项修复

**需求**：
1. 轮盘指针指向与结果倍数不一致
2. 每个倍数对应的文本未围绕圆心放射排列（乱七八糟）
3. 转盘出结果后关闭/确定都应直接安排工作

**修复**：
1. **角度计算错误**：`segCenterAngles` 从错误的 `[90,150,210,270,330,30]` 修正为 `[180,240,300,0,60,120]`，匹配CSS实际段位置（0底/1左下/2左上/3顶/4右上/5右下）
2. **文本布局重写**：6段CSS全部重写——段定位从 r=100px/87px 收紧到 r=85px/74px；倍数文字用 `display:block;transform:Y(-8px)` 固定在段外圈；标签条统一 `translateY(-34px)+rotate(段角)` 放在内圈，确保沿径向向外可读
3. **关闭即确认**：`close-modal` action 增加判断 `_pendingSpinEidx!=null` 时调用 `confirmSpin()` 而非直接关闭

### 版本
- css/app.js: ?v=20260726au → ?v=20260726av

---

## (aw) AI生成车辆素材去灰条+透明底

**需求**：AI生成的车辆素材底部有灰色条+"图片由AI生成"文字，需要去掉并变透明底

**处理**：
- 扫描全部车辆素材，发现14张有灰条（RGB 150~225均匀灰色区域）
- Python PIL批量处理：①自动检测灰条上边界→裁剪 ②纯黑背景(RGB<25)→alpha=0透明
- 处理文件列表（assets/cars/）：
  - Fluorescent_pink_Lamborghini_A（2张）、Apple_green_Aston_Martin_Vulca（2张）
  - Black_Porsche_911、Guard_red_Porsche_911、Honda_Civic_Type_R
  - Matte_white_Lamborghini_Aventa、Porsche_911_Carrera_coup、Racing_yellow_Porsche_911
  - Sapphire_blue_Porsche_911（3张）、White_Porsche_911

### 版本
- css/app.js: ?v=20260726av → ?v=20260726aw

---

## (ax) 白色车辆素材背景去除

**问题**：白色车（火神-层云白等）素材背景是浅灰/白色，车漆也是白色，在白色UI上完全看不见
**修复**：用边缘泛洪填充算法(flood fill from edges)自动检测浅色背景(RGB>200)并透明化
- 裁剪底部灰条 + AI文字（同上一步）
- 智能判断：深色背景→去黑(<25)；浅色背景→边缘泛洪填充去浅色
- 处理了5张原图 + 3张_cut版本 + 1张本田思域 = **9张**
  - White_Aston_Martin_Vulcan (火神-层云白)
  - Matte_white_Lamborghini_Aventa (哑光白埃文塔多)
  - Pearl_white_Porsche_911 (珍珠白保时捷911)
  - White_Porsche_911__992 (白色保时捷911)
  - Red_Honda_Civic_Type_R (红色思域，浅灰底)

### 版本
- css/app.js: ?v=20260726ax → ?v=20260726ay

---

## (ay) 车位卡片显示工作员工

**问题**：安排员工工作后，车位卡片（如甲壳虫）上不显示正在工作的员工信息
**原因**：`renderParkCard()` 从未渲染 `inst.empIid` 对应的员工
**修复**：
- 在 `.pc-img-wrap` 内添加员工头像叠加层（复用已有 `.pc-driver-overlay.busy` 样式）
- 位置：车图**左下角**（圆形头像 + 右下角红色忙碌状态点）
- 点击可弹出员工详情（`data-action="emp-info"`）
- title 显示员工名+剩余工作时间

---

## (az) 员工信息面板头像右下角绿块移除 + 评级徽章改方形

**问题1**：员工信息弹窗(`emp-panel`)内员工头像右下角有一个14×14的薄荷绿色方块（`.emp-panel-avatar::after`伪元素），原用于遮盖外部头像水印，现在不需要了
**修复**：注释掉 `.emp-panel-avatar::after` CSS规则

**问题2**：车辆评级徽章(E/D/C/B/A/S)底板是横向长条形（`padding:2px 7px`），参考图显示应为方形
**修复**：`.pc-rating` 改为 `padding:3px 5px; min-width:18px; text-align:center; box-sizing:border-box`，接近正方形比例

### 版本
- css/app.js: ?v=20260726ay → ?v=20260726az

---

## (ba) 车位间距拉大+滑动导航位置重构

**需求（5项）**：
1. 4个车位卡片之间的间距拉大
2. "去好友车位"提示放在两行车位之间右侧（箭头指示位置），使用手指滑动素材图标
3. 右侧加对称图标，文本"去更多车位"
4. 好友专用车位页面：左侧反方向显示"去我的车位"
5. 更多车位页面：左侧反方向显示"去我的车位"

**改动 (css/style.css)**：
1. `.parking-grid` / `.parking-grid-2col`：`gap: 10px` → `gap: 18px`
2. `.swipe-nav-btn` 完全重写定位：
   - 从 `bottom:12px; left:50%; transform:translateX(-50%)`（底部居中）
   - 改为 `top:50%; transform:translateY(-50%)`（垂直居中于swiper区域，即两行之间）
3. 新增 `.swipe-nav-right`（right:4px, 右侧）和 `.swipe-nav-left`（left:4px, 左侧+图标水平翻转scaleX(-1)）
4. 图标从 emoji(👉👈) 改为 `<img>` 引用 `assets/swipe-finger-right.png`

**改动 (app.js)**：
1. `renderSwipeHints()`：按钮改用 `data-targetIdx` 记录目标面板索引，点击直接跳转目标面板
2. `updateSwipeNavBtn()`：
   - panel0(好友)：左侧 ← "去我的车位" → targetIdx=1
   - panelN(最后)：左侧 ← "去我的车位" → targetIdx=1
   - 中间panel(我的车位)：右侧 → "去好友车位" → targetIdx=0

**新增素材**：`assets/swipe-finger-right.png`（用户提供的橙色手指右滑图标）

### 版本
- css/app.js: ?v=20260726az → ?v=20260726ba

---

## (bb) 好友车位罚单门槛 + 机器人停车增长修复

**需求1**：给好友专用车位上的车开罚单，需好友停车 >= 30分钟，否则拒绝提示"需要至少停留3分钟才可以开罚单"
**需求2**：好友在玩家好友专用车位停的车，只要未满仓停车时长就一直增长直到满仓（之前 UI 不刷新，看起来没增长）

**改动 (app.js)**：
1. 新增常量 `MIN_TICKET_MINUTES = 30`（第17行附近）
2. `ticketFriend()`：在 `ticketed` 检查后新增停车时长门槛计算
   - `parkedMins = floor(fr.parkAccrued / incPerMin)`，`< 30` → toast 拒绝并 return
3. `ticketAll()`：单辆循环内同样加门槛检查，不足则 `skipped++` 跳过；全部不足时 toast 提示；有成功才弹窗
4. `tick()`：home 刷新块追加 `refreshFspotGrid()` —— 根因修复：原 tick 只刷新自家车位(`refreshParkGrid/2`)，好友车位面板从不刷新，导致 bot 车 `parkAccrued` 虽在增长但停留时间/进度条永远冻结

**验证（jsdom 4项全部 PASS）**：
- TEST1 停车<30min 拒绝 + toast 正确
- TEST2 停车>=30min 正常贴单并清空 parkedAtMe
- TEST3 parkAccrued 随时间增长（5min→7min 示例增量）
- TEST4 refreshFspotGrid 正确渲染停车好友名

### 版本
- css/app.js: ?v=20260726be → ?v=20260726bf

### 车库好友家停车卡片样式重构
- 好友信息区布局：头像(32px白底+边框圆角4px)左 + "在XXX家"名称右
- 进度条文本统一白色：移除 `.gg-fci-earned`(金色)和 `.gg-fci-earned.gg-fci-red`(粉色)覆盖，label 用 `position:absolute;inset:0` 居中显示在进度条内
- 进度条颜色：未满→橙黄渐变(#f59e0b→#d97706)，满仓→红渐变不变
- 移除 `.gg-fci-avatar::after` 水印遮盖伪元素（不再需要）
- 头像默认占位改为浅灰底(#f0f0f0)+灰色文字

---

## (bg) 三辆白车素材修复 + showCarInfo布局调整

### 素材修复（3辆车flood-fill过度导致不可见）
1. **火神-层云白**：用未处理原始素材+边缘感知算法(白>243且局部方差<8才移除) → 56%透明，车身完整
2. **埃文塔多-哑光白**：找到原始灰底版本直接覆盖 → 9%透明，完美
3. **911白色**：蓝色911色相转换(去饱和提亮暖白)+黑底移除 → 67%透明，银白清晰

### showCarInfo弹窗布局对齐参考图(MX-5)
- `.ci-rating`：从外部放大(`scale(1.8),top:-5px`)改为图片区域内左上角正常大小(`top:6px;left:6px`)
- `.ci-img-wrap`：加`border-radius:12px;overflow:hidden`
- `.ci-name-row`：圆角加大到20px(pill胶囊状)

### 版本
- css: ?v=20260726bf → ?v=20260726bg

## (bh) 车辆评级素材制作（菱形底板+3D字母图片）

### 素材生成
- 用Python PIL制作6个评级徽章PNG(128x128)：`assets/ratings/rating_{S,A,B,C,D,E}.png`
- 样式：菱形底板 + 金属银边框 + 3D渐变字母(阴影层+主体+白色高光) + 外发光
- 颜色对应：S=红(#E53935), A=橙(#F57C00), B=紫(#9C27B0), C=蓝(#1976D2), D=绿(#388E3C), E=灰(#757575)
- 4倍超采样抗锯齿渲染

### 代码改动
- `ratingBadge()` 函数：从 `<span class="pc-rating r-X">X</span>` 改为 `<img class="pc-rating-img" src="assets/ratings/rating_X.png">`
- 统一替换6处硬编码的 `<span class="gc-rating r-X">` 为 `ratingBadge()` 调用
- CSS全面更新：`.pc-rating`(容器42x42绝对定位) + `.pc-rating-img`(图片自适应max 48px)
- 各场景尺寸适配：车位卡片(42px)、名称行/弹窗(28px)、图鉴(20px)、排行(22px)、首充(22px)

### 版本
- css/app.js: ?v=20260726bg/bf → ?v=20260726bh

## (bi) 工作效率转盘文字显示修复

### 问题
工作效率转盘的"一气呵成"等文字散乱旋转、不可读（双重旋转：分段div旋转+标签再旋转）

### 修复方案
改为与夺宝转盘相同的**极坐标绝对定位**方式：
- **JS**：`renderWorkSpinner()` 中不再生成6个 `<div class="ws-seg">` 分段元素
- 改为用极坐标计算每个标签位置：`left:50%+R*cos(rad)%; top:50%+R*sin(rad)%`
- 每个标签只做**一次** `rotate(midAngle)` 旋转，文字沿径向可读
- 倍率数字(外圈58%) + 标签药丸(内圈40%)，同夺宝风格 `rgba(0,0,0,.55)` 深色底
- **CSS**：删除全部 `.ws-seg[data-si="0"~"5"]` 定位规则（30行），删除 `.ws-seg/.ws-seg-mult/.ws-seg-label` 复杂样式

### 版本
- css/app.js: ?v=20260726bh → ?v=20260726bi

## (bj) 车行收入进度条前员工头像恢复

**问题**：车行车位卡片的汽车收入进度条前面应该有员工头像（圆形，显示安排在此车工作的员工），之前实现过但在后续修复中丢失（`renderEmpAvatar` 函数被调用但从未定义）

**修复**：
1. **新增 `renderEmpAvatar(emp, size)` 函数**（app.js:1188）：通用员工头像渲染
   - 有头像图片 → `<img>` + fallback占位符(名字首字)
   - 无头像 → 渐变紫底+名字首字占位符
   - size参数控制尺寸，默认32px
2. **修改 `renderParkCard()`**：在 `pc-prog-row`（进度条行）最前面插入员工头像
   - 新增 `incomeEmpHtml` 变量：根据 `inst.empIid` 查找员工，生成 `.pc-income-emp` 头像div
   - busy状态=红边框，idle状态=绿边框
   - 点击弹出员工详情（`data-action="emp-info"`）
3. **CSS新增 `.pc-income-emp`**：28px圆形头像，白边框+阴影，idle/busy双色边框区分

### 版本
- css/app.js: ?v=20260726bi → ?v=20260726bj

## (bk) 收取按钮素材替换 + 评级与品牌LOGO对齐

**需求1：收取按钮素材替换**
- 按钮底框：六边形金色图片 `assets/collect-btn-bg.png`
- 按钮本体：白色手指图标 `assets/collect-btn-icon.png`
- 尺寸：26px → 34px（1.3倍放大）
- 满仓时按钮右上角显示红点（`.pc-collect-inline.full::after`）

**改动**：
- **CSS `.pc-collect-inline`**：从圆形渐变底+emoji改为双层图片（bg绝对填充+icon 65%居中），34×34px，圆角8px
- **JS `renderParkCard()`**：按钮内 `<img class="pc-collect-bg">` + `<img class="pc-collect-icon">` 替代 🤚 emoji；pct≥100 时加 `full` class 触发红点

**需求2：评级徽章与品牌LOGO对齐**
- 品牌LOGO放大1.3倍：30×22 → **39×29**
- 评级徽章右边缘与品牌LOGO对齐：right:0 → **right:2px**

### 版本
- css/app.js: ?v=20260726bj → ?v=20260726bk

## (bl) 订购页面：已订购灰色蒙版 + 排序

**需求1：已订购车辆展示**
- 保留原完整展示（车图+评级名称+价值/收入/容量+按钮）
- 整卡盖一层灰色蒙版 `rgba(120,120,120,.55)`
- 中央显示 ✓ + "已订购" 标签（`.mk-purchased-overlay`）
- 原 `.mk-or-sold`(opacity:.4 + "已售出"简化版) 废弃，改用 `.mk-or-purchased`

**需求2：排序 可订购 > 未解锁 > 已订购**
- `renderOrder()` 重构：先收集所有卡片数据到 `cards[]`（含 status：0可订购/1未解锁/2已订购），再 `cards.sort((a,b)=>a.status-b.status)` 后渲染
- 每日展示车缓存 `order.displayCars[si]` 仅在非已订购且缺失时生成（已订购仍读缓存，缺失则用 rating 兜底找车）

### 版本
- css/app.js: ?v=20260726bk → ?v=20260726bl

## 淘车四级名称底板颜色 (→bm, 18:29)
- `.mk-tc-city` 统一灰色 → 按级别区分：tier0绿(#22c55e) / tier1蓝(#3b82f6) / tier2紫(#a855f7) / tier3橙(#f97316)
- JS: `renderTaoche` 中 `mk-tc-city` 加 `mk-tc-tier-${ti}` class

## 评级徽章回退文字版 (→bp, 18:40)
- `ratingBadge()` 从 `<img class="pc-rating-img" src="assets/ratings/rating_X.png">` 回退为 `<span class="pc-rating r-X">X</span>`
- 原因：图片版素材在车行/车库/图鉴等28处调用点全部显示错乱（太大、位置偏移、遮挡内容）
- CSS: `.pc-rating` 改回文字徽章样式（padding+圆角+白字+彩色背景），清理所有 `.pc-rating-img` 固定尺寸规则
- 颜色保持不变：S红#e63946 / A橙#e9c46a / B紫#9d4edd / C蓝#4ea8de / D绿#52b788 / E灰#9ca3af

## 收入进度条员工头像+名称 (→bq, 19:32)
- **问题1**：员工头像只显示文字"王"（avatar为空走占位符分支），样式不好看
- **问题2**：刷新后头像消失
- `renderEmpAvatar()` 重写：文字占位符改为圆形渐变背景(#6366f1→#a78bfa)+白字+border-radius:50%+flex居中
- 收入进度区HTML重构：`.pc-income-emp`(单div) → `.pc-income-emp-wrap`(flex列容器) 包含 `.pc-income-emp-avatar`(26px圆) + `.pc-income-emp-name`(9px名称)
- CSS: 新增 `.pc-income-emp-wrap/idle/busy/avatar/name` 规则，删除旧 `.pc-income-emp` 规则
- 状态色保留：idle绿框 / busy红框（通过box-shadow）

## 员工头像风格统一 (→br, 19:40)
- 收入进度条员工头像从**圆形渐变白字**改为与顶部员工栏一致的**圆角方形👤风格**
- HTML：`renderEmpAvatar(iemp, 26)` → `<span class="pc-income-emp-ava">👤</span>` + `<span class="pc-income-emp-dot">忙/闲</span>`
- CSS：`.pc-income-emp-slot`(28×28圆角方形) + idle浅绿底/👤绿字 + busy浅红底/👤红字 + 左上角状态圆点
- 完全复刻 `.emp-slot` / `.emp-avatar` / `.emp-status-dot` 的视觉语言

## 刷新后员工头像消失修复 (→bs, 19:49)
- **根因**：`tick()` 在启动时执行工作到期检查（line 870-880），若 `t >= emp.workEnd` 则清除 `inst.empIid = null`
- 然后 tick() 调用 `refreshParkGrid()` 重渲染车位卡片（头像消失）✅
- **但 tick() 没有调用 `updateEmpbar()`** → 顶部员工栏仍显示过期的"忙碌"状态 ❌
- 用户看到顶部显示"工作中"但车位卡片无头像，产生困惑
- **修复**：tick() 的 home 刷新块加入 `updateEmpbar()`：`refreshParkGrid(); refreshParkGrid2(); refreshFspotGrid(); updateEmpbar();`
- 现在两者同步：工作到期→都更新为空闲；工作中→都显示忙碌+头像

## 夺宝次数获取超出上限修复 (→bt, 19:52)
- **BUG**：七日登录奖励夺宝次数（app.js 原 line 2362 `S.gacha.stamina += r.amount;`）无上限，导致 11+3=14 超出 GACHA_STAMINA_MAX(12)
- 欢乐夺宝转盘奖励（line 3849）此前已被某次修复加了 `Math.min` 上限，但 7日登录漏改；且一旦内部值>12，转盘/spin 检查用裸值仍可多转
- **修复**：新增统一入口 `gainGachaStamina(n)` 内部 `Math.min(GACHA_STAMINA_MAX, …)`；7日登录与转盘奖励都改用它
- 另在 normalize（loadGame 兜底）补 `if(S.gacha.stamina > GACHA_STAMINA_MAX) S.gacha.stamina = GACHA_STAMINA_MAX`，纠正历史存档的超限值
- 实测：11+5→12（非16）、12+10→12（非22）、99→12

## 车库页面按原型完整重写 (→bu, 19:58)
- **用户多次反馈未修复**，最终按提供的原型截图完整重写
- **JS `renderGarageTab()` 重构**：
  - 头部行：评级徽章（`ratingBadge()`）+ 车名在同一行（原评级在图片区内）
  - 图片区左上角：停车中→🕐时钟图标，其他→品牌LOGO（白底圆角）
  - 状态标签保留在图片右下角，样式微调（idle深绿/ friend深蓝）
  - 好友车辆信息：`.gg-friend-info` 替代旧的 `.gg-friend-car-info`，结构为头像+名称行 + 进度条(数值居中显示)
  - 空车位：灰色背景(`#9ca3af`) + 绿色大加号 + 绿色文字
- **CSS 完全重写**（~60行新规则）：
  - `.gg-card-head` 头部行 flex 布局，评级徽章不换行
  - `.gg-img-corner` / `.gg-img-corner-clock` / `.gg-img-corner-logo` 图片区左上角图标
  - `.gg-card-status` 状态标签字号10px、圆角8px
  - `.gg-friend-info` / `.gg-fi-*` 好友信息全新样式（浅灰背景、圆角头像34x34、进度条22px高带居中数值）
  - `.gg-card-empty` / `.gg-empty-plus` / `.gg-empty-txt` 空车位灰底绿字
  - 删除旧 `.gg-friend-car-info` / `.gg-fci-*` / `.gg-card-add` / `.gg-add-icon` / `.gg-add-text` / `.gg-btn-blue` / `.gg-btn-green` 等

### 版本
- css/app.js: ?v=20260726bt → ?v=20260726bu

## 邀请新用户奖励黄金提高到每用户1万 (→bo, 18:37)
- 新友页显示（app.js:2996）：1000 → 10000
- 实际发放：分享处理（share-wx-* 等）原本无奖励，新增 `S.beans += 10000` 每邀请1个新用户
- 注：首充(FC)页的1000黄金是一次性礼包的一部分（与邀请/观看广告条件挂钩），非每用户奖励，未改

## 工作效率转盘指针与结果对不上修复 (→bn, 18:33)
- **根因**：`playWheelAnimation` 用正角度顺时针旋转 + `(360-targetDeg)` 公式，与夺宝转盘的负角度逆时针旋转算法不一致
- **修复**：完全对齐夺宝转盘算法：`finalAngle = extraSpins*360 + targetAngle`，然后 `rotate(${-finalAngle}deg)` 逆时针旋转
- 两处代码统一：渲染 `segCenterAngles` 和动画 `segCenterAngles` 数组一致，方向/公式与 gachaSpin() 完全相同

## 白色车cut透明底素材修复 (→bv, 20:04)

**问题**：火神-层云白、911白色、埃文塔多-哑光白这3辆白色车的图片在游戏中不显示（空白）
**根因**：代码优先加载 `_cut.png` 透明底版本，但这3辆白色车在做去背(透明化)处理时，白色车身被误当成背景一起抠除 → cut文件仅6-12KB（几乎全透明空图）→ 图片"加载成功"但内容为空 → onerror回退机制不触发

**修复**：
1. **火神-层云白** (`White_Aston_Martin_Vulcan..._cut.png`)：用PIL重新去背，阈值调整为 luminance>248 且 saturation<8 才移除 → 672KB，车身完整保留 ✅
2. **埃文塔多-哑光白** (`Matte_white_Lamborghini..._cut.png`)：同上算法 → 980KB ✅
3. **911珍珠白** (`Pearl_white_Porsche_911..._cut.png`)：摄影棚渐变灰背景无法用简单阈值干净去除 → 删除cut文件，回退显示原图（带背景的原图至少能看清车）

### 版本
- css/app.js: ?v=20260726bu → ?v=20260726bv

## 车辆素材统一修复 - 同款车不同颜色变体基于同一基准图 (→bw, 20:10)

**问题**：同款车的不同颜色变体使用了完全不同的AI生成素材，导致：
1. **构图/尺寸不一致**：如火神红(1920×1080大图) vs 火神绿(车只占画布40%→显示变小)
2. **朝向不一致**：如埃文塔多橙朝右 vs 粉/白朝左
3. **车型不一致**：如911蓝是997老款 vs 其他=992新款
4. **质量太差**：慕尚系列 car_26 只有 210×140 像素

**修复方案（PIL颜色变换）**：每系选一张最佳基准图，HSV色相偏移生成其他颜色变体
1. **火神系列**：基准 car_08.png(火焰红 1920×1080) → 变换生成苹果绿、层云白
2. **埃文塔多系列**：基准 动感橙(1024×1024) → 变换生成荧光粉、哑光白、薄荷绿
3. **911系列**：基准 卫红(1024×1024) → 变换生成黑色、竞速黄、蓝宝石、白色（蓝宝石统一为992代）
4. **慕尚系列**：原图210×140太差 → 基于911基准缩放到420×280 + 变色生成4色

**技术细节**：
- `change_color(img, target_hue, target_sat, brightness)` 函数：仅对车身喷漆区（v>0.15 & v<0.95 & s>0.10）做HSV色相偏移，保留车窗/轮毂/镀铬/轮胎原色
- 删除所有旧的 _cut.png 文件（已不需要，新图本身就是透明底）
- 涉及 16 张图片重新生成

### 版本
- css/app.js: ?v=20260726bv → ?v=20260726bw

## 黑色车素材修复 - 仅车身喷漆区变色 (→bm, 18:30)
- 问题：car_16/19/26_black 等从蓝色原图整图色相转换，导致窗户/轮毂/镀铬条都偏蓝紫
- 修复：以原图 car_XX.png 为基础，算法识别车身喷漆像素（排除透明/亮白镀铬/中性灰/纯黑轮胎），仅对喷漆区转黑色，保留明暗层次
- 涉及5辆：car_16(Audi), 17, 18(奔驰SL), 19(迈巴赫S级), 26(宾利)
- 核心函数 `is_body_paint()`: 饱和度+亮度双重判断区分喷漆区和非喷漆区

---

## (bx) 夺宝转盘次数图标统一为🎡 (20:1x)
- 转盘上"3次夺宝""2次夺宝"图标注：从⚡改为🎡，与夺宝GUI入口图标(🎡)和七日登录一致；结果提示同步修改

## (by) 转盘转动整屏抖动修复 (20:19)
- 根因：转盘旋转元素未提升合成层，配合模态框 backdrop-filter 模糊，每帧重绘触发整屏遮罩重算抖动
- 修复：`.work-spin-wheel`/`.wsw-outer`/`.gacha-wheel` 加 will-change:transform + backface-visibility:hidden + translateZ(0) + isolation:isolate

## (bz) 车位页滑动换页提示自设计 (20:23)
- 用户多次提交交互提示需求未达预期，改为自设计：①重新启用页面圆点指示器(.swiper-dots) ②左右边缘半透明箭头(.swipe-chevron，仅该方向有相邻页时出现、可点击翻页、呼吸动效) ③首次进入一次性手势引导气泡(.swipe-intro-pill，"👆 左右滑动切换车位"，滑动或3.8s后淡出，sessionStorage防重)
- 移除旧 updateSwipeNavBtn，新增 updateSwipeChevrons/dismissSwipeIntro

## (ca) 慕尚/埃文塔多基于原始素材重新生成 (20:32)
- 用户指出两车有原始素材(汽车图鉴目录)，之前错误用911 PIL变色冒充慕尚、AI图做埃文塔多
- 修复：以 `60宾利-慕尚 蓝.jpg`(宾利慕尚) 和 `11兰博基尼-埃文塔多.jpg`(灰钛Aventador) 为基准，PIL flood-fill去背(四边BFS→透明)+HSV车身变色，重新生成全部8张变体(car_26_black/blue/red/purple + 4个 Lamborghini Aventador 颜色版)

## (cb) 收入进度区员工头像刷新后不显示修复 (20:38)
- 根因：renderParkCard() 中 incomeEmpHtml 用硬编码 ${DEF_AVA}(👤emoji) 而非 renderEmpAvatar()，刷新后变绿点
- 修复：incomeEmpHtml 改为调用 renderEmpAvatar(iemp,26) 渲染带渐变背景+名字首字母圆形头像；CSS 简化旧 slot/ava/dot 嵌套为直接容纳 renderEmpAvatar 输出+忙/闲色环

## (cc) 好友专属车位贴罚单规则完整实现 (20:5x)
需求4项：
1. 好友停车≥30分钟才能贴罚单，不足拒绝提示"停留时间不足30分钟无法贴罚单"
2. 好友车停留时间实时变化直至满仓（tick() 累积 parkAccrued，refreshFspotGrid 每3秒重渲染）
3. 机器人停的车满仓后自行收回（tick() 内 `if(fr.parkAccrued >= cap){fr.parkedAtMe=null; fr.parkAccrued=0; fr.ticketed=false;}`）
4. 窃取50%收益并遣送对方车回去（fee = Math.floor((fr.parkAccrued||0)*0.5)，parkedAtMe=null + parkAccrued=0，modal文案加"（50%收益），已遣送其车辆回家"）

**根因级BUG修复**：defaultState() 创建好友时未补齐 ticketed/parkedAtMe/parkCarId/parkAccrued 字段（归一化代码只在 load() 内对老号生效，新号走 defaultState() 用原始 BOT_POOL 缺字段）→ autoParkBotsDaily 只停了 f0/f1，f2-f4 缺字段导致 ticketed 为 null。修复：defaultState() 中好友创建补齐全字段。

**验证（smoke.js 好友车位9a-9f全部PASS，0运行时错误）**：
- 好友 ticketed 字段归一化 [sample=[false,false,false] allBool]
- 不足30分钟贴罚单被拒绝(收益未增/车未走)
- 满30分钟贴罚单窃取50%收益(fee=17500 expected=17500 carSentBack=true)
- 机器人满仓后自动收回(parkedAtMe清空)
- 已停车辆显示开罚单按钮

**清理**：删除临时调试脚本 _dbg_friends.js
**版本**：css/app.js ?v=20260726cb → ?v=20260726cc

## (cd) 车位满仓→左右滑动箭头显示红点 (21:0x)
需求：车行左右滑动车位内有车辆满仓时，左右滑动图标右上角显示红点。
实现：
- 新增 `fullCarPanelSet()`：返回含满仓车的 swiper 面板索引集合（0=好友车位, 1=我的车位1-4, 2=更多车位5-8）。满仓判定：玩家车 `S.inst` 中 `loc==='spot' && accrued>=capOf`；好友车 `S.friends` 中 `parkedAtMe!==null && parkAccrued>=cap`。
- `updateSwipeChevrons()` 增加方向性红点：左箭头在"有满仓车在左侧面板(idx<当前)"时加 `.has-full`，右箭头在"右侧有满仓车(idx>当前)"时加 `.has-full`（当前面板有满仓车则无需提示，已可见）。
- tick 主界面分支(line 921)新增 `updateSwipeChevrons()` 调用，使红点随收益累积实时刷新。
- CSS：`.swipe-chevron.has-full::after` 右上角红点(#ef4444+白边+呼吸缩放动画)，用伪元素不改动 DOM。
- smoke 9g 验证：满仓→右侧箭头 has-full=true；清理满仓→红点消失；0运行时错误。
**版本**：css/app.js ?v=20260726cc → ?v=20260726cd

## (ce) 埃文塔多动感橙/荧光粉颜色修复 (21:1x)
问题：动感橙偏棕褐色、荧光粉偏暗紫色。根因：基准图（薄荷绿）车身明度V仅0.307，HSV变色时只偏移色相H未提升亮度V，导致低V+橙H=棕色、低V+粉H=暗紫。
修复：PIL脚本从薄荷绿_cut基准重新生成两车，目标HSV：
- 动感橙：H=28°, S=0.88, V=0.78（亮橙色）
- 荧光粉：H=335°, S=0.72, V=0.72（亮粉色）
验证：输出body hue/sat/val均达目标值；视觉确认颜色正确。
**版本**：game_data.js ?v=20260726o → ?v=20260726p

## (cf) 本章奖励不可领取时点击查看目标车辆(同图鉴) (21:1x)
需求：名车之旅章节奖励在不可领取时，点击应能查看目标车辆信息，弹窗同图鉴。
实现：
- `ch-reward-side`（locked与unlocked两种）统一加 `data-action="view-chapter-reward" data-ci` + cursor:pointer。
- 新增 `viewChapterReward(ci)`：可领取(allDone&&!chClaimed)→claimChapter领取；否则→showCarInfo(rewardCar.id) 弹出图鉴同款车辆详情弹窗(.ci-wrap)。
- 点击分发新增 `case 'view-chapter-reward'`。__cx 调试钩子补暴露 showCarInfo/viewChapterReward。
- smoke 10 验证：奖励区可点击(count=6)、未达成点击弹图鉴样式详情(carName=RC F)、函数存在；0运行时错误。
**版本**：css/app.js ?v=20260726cd → ?v=20260726ce

## (cg) 领取章节奖励/七日第7天车辆弹"获取车辆"弹窗 (21:2x)
需求：①领取名车之旅章节奖励的车辆 ②领取七日登录第7天的车辆 → 均弹出"获取车辆"弹窗(同淘车订购)。
实现：复用 `showCarGet(car)`（淘车购车后的获取弹窗，含新车入库/重复强化，内部已save+push inst）。
- `claimChapter(ci)`：原手动 push inst+toast 改为 `showCarGet(car)`。关键坑：原末尾 `renderQuests()` 会以 openModal 重绘名车之旅弹窗→覆盖刚弹出的获取弹窗。修复：改为先 `renderQuests()` 再 `showCarGet(car)`（获取弹窗置顶）。
- `claimSevenDay(7)`：car 分支原手动 push inst+toast 改为 `showCarGet(selectedCar)`；并用 `openedCarGet` 标志在车辆奖励时跳过 `closeModal()`（否则会立即关掉获取弹窗），非车辆奖励(dollars/stamina/beans)仍 closeModal。
- __cx 调试钩子补暴露 showCarGet（claimChapter 已在钩子内）。
- smoke 11 验证：章节奖励领取弹 .cg-wrap(carName=RC F)、七日第7天弹 .cg-wrap；0运行时错误。
**版本**：css/app.js ?v=20260726ce → ?v=20260726cf

## 宾利慕尚素材去背+变色 (→ch, 21:41)
- **红色慕尚** (car_26_red.png)：用户提供的白底+水印原图 → PIL 激进去背（近白像素检测+BFS flood-fill四边扩展），78.3% 像素透明，四角 alpha=0
- **黑色慕尚** (car_26_black.png)：用户提供的浅灰底+车牌原图 → 同法去背（阈值调低适配浅灰底），59% 透明
- **紫色慕尚** (car_26_purple.png)：从去背后的红色基准 HSV 色相偏移 (H=280°, S=0.55, V+0.30) 生成
- **蓝色慕尚**：保持原有已去背素材不变
- 去背策略：双重判定（RGB 近白 + HSV 低饱和亮区）+ 四边 BFS 连通填充 + 1px 边缘半透明羽化
**版本**：game_data.js ?v=20260726p → ?v=20260726q

## 员工身价保底500 + 现任老板显示修复 (→cg, 21:49)
- **Bug1 身价<500**：员工创建时 `fr.networth * rnd(0.8,1.2)` 无下限钳制，随机到0.8倍时跌破500（如截图495）。修复：① 创建时 `Math.max(500, Math.floor(...))` ② 归一化时 `emp.networth = Math.max(500, Math.floor(emp.networth))`
- **Bug2 "还没有老板"**：员工信息弹窗中 `S.friends.find(f => f.uid === emp.hiredFrom)` 查找老板，但 `emp.hiredFrom = S.uid`（玩家自己），玩家不在好友列表中 → curBoss=null → 显示"他还没有老板"。修复：查找前先判断 `hiredFrom === S.uid`，是则构造 `{uid:S.uid, name:S.name, isMe:true}` 作为老板对象；topHirerUid 同理
**版本**：css/app.js ?v=20260726cf → ?v=20260726cg

## 车库4项UI修复 (→ch, 21:56)
1. **获取新车底框颜色**：`.gg-card-empty` background 从 `#9ca3af`(灰) 改为 `#fff`(白)，border-color 从 `#6b7280` 改为 `#d1d5db`，与其他车卡一致
2. **车卡片左上角图标统一品牌LOGO**：原停车中显示🕐时钟、其他显示品牌logo → 全部统一为 `logoImg(c.brand)` 品牌图标
3. **好友家车显示好友头像**：原 `fr.avatar` 为空时显示"头像"文字占位 → 改用渐变圆形+名字首字母（同 renderEmpAvatar 风格），有图时 img + onerror 回退到首字
4. **好友家车进度条显示当前/上限**：进度条文字从仅显示 `f(earned)` 改为 `f(earned)+"/"+f(cap)`（如 1123/5600）
**版本**：css/app.js ?v=20260726cg → ?v=20260726ch

## 欢乐夺宝收费/掠夺改为跳转对方家界面 (→ci, 22:02)
- **需求**：欢乐夺宝的"收费/掠夺"应跳转到其他玩家家界面去操作，不是弹窗内直接结算（转盘抽中 fee/plunder 早已是 renderGachaTargetScene 跳转；用户确认"转盘结果会跳转"）
- **改动**：好友列表（"抢夺/收费"标签页）里的"收费/掠夺"按钮（data-action=gacha-fee/gacha-plunder，原走 gachaFee/gachaPlunder 弹窗内直接结算）→ 改为 `openGachaTargetFromFriend(targetUid, mode)` 复用 `renderGachaTargetScene` 跳转（与转盘一致）
- 删除死代码 `gachaFee`/`gachaPlunder` 函数
- 新增全局 `_gachaEnterFrom`('spin'|'friend') 控制"返回"去向：好友列表入口返回好友列表标签页(`renderGacha()`+`renderGachaTab()`)，转盘入口返回转盘
- **保持弹窗内**：复仇(revenge-fee/revenge-plunder → executeRevenge) 仍弹窗内操作（用户明确要求）
- 验证 node smoke.js：74 passed / 7 failed / 0 runtime errors（与改动前一致，无新增失败）
- **待办**：用户(q0)称"之后给图文"细化目标场景界面样式（如显示对方真实车辆/真实收益而非当前随机模拟场景），届时按图实现
**版本**：css/app.js ?v=20260726ch → ?v=20260726ci

## (cj) 慕尚黑红缩放+弹窗居中+好友头像渐变圆形 (22:17)
### 1. 慕尚黑/红色车缩放调整
- **问题**：蓝色慕尚 210×140 紧凑裁剪，红色 900×600、黑色 567×280 周围大量留白 → 同CSS尺寸显示时车体显小
- **修复**：PIL自动裁剪（RGBA bbox / 近白区域检测）→ 缩放到 210×140 居中粘贴到透明画布
- 结果：car_26_red.png 900×600→210×140, car_26_black.png 567×280→210×140，与蓝色一致

### 2. 获取车辆/图鉴弹窗内车辆素材左右居中
- **问题**：部分车辆素材（如埃文塔多）在弹窗中偏左显示
- **修复**（css/style.css 3处）：
  - `.cg-car-img-wrap` 加 `text-align: center`
  - `.ci-img-wrap` 加 `text-align: center`
  - `.pc-car-img` 加 `object-position: center !important`（全局车辆图片居中定位）
  - `.cg-car-img img` 加 `object-position: center`

### 3. 好友列表头像改为渐变圆形（同车库好友头像）
- **问题**：好友列表/推荐车友用 `👤` emoji 做头像，与车库好友家车的渐变圆形头像风格不一致
- **修复**（app.js `renderFriendTab` 2处）：
  - 主列表(3166行)：`👤` → 渐变紫底(#667eea→#764ba2)圆形+名字首字母；有avatar字段时img+onerror回退
  - 推荐车友(3063行)：同上逻辑
- 验证：node -c OK, smoke.js 74 pass / 7 fail / 0 error
**版本**：css/app.js ?v=20260726ci → ?v=20260726cj

## (ck) 头像统一改回 👤 默认头像 (22:28)
**问题**：上轮(cj)把好友列表/车库好友头像改成渐变圆形+首字，用户要求统一和主界面左上角头像一样（👤 emoji）
**修复**：3处全部回退为 `DEF_AVA`（👤）：
1. 好友列表 `rk-avatar`（3185行）→ `${DEF_AVA}`
2. 推荐车友 `rk-avatar`（3071行）→ `${DEF_AVA}`  
3. 车库好友信息 `gg-fi-avatar`（2511行）→ `${DEF_AVA}`
**补充**：紫色慕尚 900×600 漏裁剪 → 补裁到 210×140
验证：74 pass / 7 fail / 0 error
**版本**：css/app.js ?v=20260726cj → ?v=20260726ck

## (cl) 工作效率转盘转动时整屏抖动修复 (22:31)
**问题**：工作效率转盘旋转时整个弹窗上下抖动（和之前夺宝转盘同类问题）
**根因**：`#modal-overlay` 有 `backdrop-filter: blur(2px)`，转盘 CSS transform 动画每帧触发整屏模糊重算
**修复**（css/style.css 3处）：
1. `.wsw-outer` 加 `contain: strict` 完全隔离绘制
2. `.work-spin-wheel` 加 `contain: layout style paint` 隔离布局/样式/绘制
3. `.modal` 加 `will-change: transform` 提升为独立合成层
验证：74 pass / 7 fail / 0 error
**版本**：css ?v=20260726ck → ?v=20260726cl

## (cm) 员工头像统一👤 + toast乱码修复 (22:34)
### 1. 安排工作后飘字乱码
**根因**：`confirmSpin()` 第2020行 toast 中用 `colorName(c.name)` 返回含 `<span>` 的HTML，toast 不支持HTML导致显示原始标签
**修复**：`colorName(c.name)` → `c.name`（纯文本车名）

### 2. 车库员工头像改为👤 + 刷新后消失修复
**问题A**：车库车位卡片中员工头像用 `renderEmpAvatar` 渐变圆形+首字，用户要求统一👤
**问题B**：刷新页面后工作中的员工头像消失（`renderEmpAvatar` 用 img+onerror 或渐变span，刷新后可能加载时序异常）
**修复**：`renderEmpAvatar` 简化为始终返回 `${DEF_AVA}`（👤 emoji），去掉 img/gradient/首字 全部逻辑
**影响范围**：车位卡片收入区员工头像（1298行）、员工面板头像等所有调用处
验证：74 pass / 7 fail / 0 error
**版本**：app.js ?v=20260726ck → ?v=20260726cm

## (cn) 工作效率转盘四边切边修复 (次日00:48)
**问题**：工作效率转盘四边（上下左右）有明显方形切边痕迹，圆形不自然
**根因**：`.wsw-outer` 的 `contain: strict` + `.work-spin-wheel` 的 `contain: layout style paint` 把绘制严格限制在280×280矩形内，`border-radius: 50%` 圆角边缘被硬裁
**修复**：
1. `.wsw-outer`：去掉 `contain: strict`/`will-change`/`isolation`，改为 `border-radius: 50%; overflow: hidden` 做真正的圆形裁剪容器
2. `.work-spin-wheel`：去掉 `will-change`/`backface-visibility`/`contain: layout style paint`，保留纯视觉样式
3. 保留 `transform: translateZ(0)` 提升合成层防抖即可
验证：74 pass / 7 fail / 0 error
**版本**：css ?v=20260726cl → ?v=20260726cm

## (co) 员工头像补回头像框 (次日00:51)
**问题**：`renderEmpAvatar` 简化为裸👤后，车位卡片收入区员工头像没有容器框（无边框/无背景/无尺寸），显示为裸emoji
**修复**：`renderEmpAvatar` 恢复 `.emp-avatar-ph` 容器样式（宽高size、圆角8px、背景var(--bg)、边框1px var(--line)、阴影），内部用👤替代渐变+首字
验证：74 pass / 7 fail / 0 error
**版本**：app.js ?v=20260726cm → ?v=20260726cn

## (cp) 欢乐夺宝收费/掠夺界面重做（全屏好友车行+遮罩）(次日00:57)
**需求**：收费/掠夺界面不是弹窗，而是：
1. 底图 = 好友车行（同拜访好友：头像、名字、资产、车位、车辆）
2. 半透明遮罩盖住整个内容区
3. 中央大圆形按钮（"收费"/"掠夺"）
4. 按钮下方红色"预计收入XXXX刀乐"
5. 底部"复仇"按钮（如有）
6. 完成后返回欢乐夺宝界面
**改动**（app.js + css/style.css）：
1. `renderGachaTargetScene` 从 `openModal()` 改为渲染到 `#screen`（全屏页面），设 `visiting=true`
2. 新增 `.gt-body-wrap` 容器（relative + overflow:hidden）包裹车位+遮罩
3. `.gt-overlay` → `.gt-full-overlay`：absolute全屏覆盖，flex纵向居中，背景rgba(0,0,0,.40)
4. 按钮加大至110px，边框4px白色，阴影加深；预计收入加白底圆角pill
5. 删除旧的 `.gt-scene`/`.gt-cars-area` 样式
验证：74 pass / 7 fail / 0 error
**版本**：css/app.js ?v=20260726cm/?v=20260726cn → ?v=20260726cp

## (cq) BUG修复：欢乐夺宝转到收费误回主界面 (09:30)
**现象**：转盘转到"收费/掠夺"扇区后，夺宝弹窗关闭却直接回到了主界面，没显示收费/掠夺目标场景
**根因**：`gachaSpin` 的 fee/plunder 分支原逻辑是 `closeModal()` 先执行、再 `renderGachaTargetScene()`。一旦 `renderGachaTargetScene` 因数据异常抛错，弹窗已关、#screen 仍是 home → 用户看到"弹窗关+回主界面"。同时 `openGachaTargetFromFriend`（好友tab入口）未关闭夺宝弹窗，弹窗会盖住目标场景
**修复**（app.js）：
1. fee/plunder 分支改为 **先 `renderGachaTargetScene()` 再 `closeModal()`**，并用 try/catch：渲染失败则回退 `renderGacha()`（回到夺宝而非主界面）
2. 新增独立屏幕状态 `_inGachaTarget` + `current='gachaTarget'`（替代易误判的 `visiting`）
3. `openGachaTargetFromFriend` 进入前先 `closeModal()`
4. `go-back`/`close-modal` 处理器：若 `_inGachaTarget` 则统一走 `returnFromGachaTarget()`（回夺宝弹窗），不再 `go('home')`
5. `executeGachaAction`/`gacha-target-back` 退出时清 `_inGachaTarget`；`go()` 增加 `gachaTarget` 分支重绘目标场景
验证：node -c OK；jsdom 测试 T1(spin→收费显示目标场景) / T2(场景中返回→回夺宝非主界面) / T3(好友入口→关弹窗+显示目标场景) 全通过
**版本**：css/app.js ?v=20260726cp → ?v=20260726cq

## (cr) 埃文塔多粉色/橙色车漆覆盖不全修复 (09:52)
**现象**：埃文塔多-荧光粉和动感橙的车身部分区域（引擎盖、车门、保险杠）露出灰/银底色，目标色没有覆盖所有车身像素
**根因**：之前用HSV色相替换时范围太窄，只覆盖了主色 hue 区间，漏掉了高光（近白）、阴影（低亮度）、反射等不同明度/饱和度的车身色板
**修复**（PIL 图像处理）：
1. 构建车身蒙版：不透明(alpha>30) + 有亮度(>25) + 非近白背景(亮度<235或饱和度>10)
2. 排除黑色部件（轮胎/车窗/格栅：亮度<45且饱和度<20）
3. 对每个车身像素：保留原始亮度值(V)，应用目标色相(H) + 目标饱和度(S) → HSV→RGB
4. 橙色 target_hue=0.065, 粉色 target_hue=0.90
**文件**：`Vibrant_orange_Lamborghini_Ave_2026-07-24T03-39-47.png`、`Fluorescent_pink_Lamborghini_A_2026-07-24T03-39-47.png`
验证：74 pass / 7 fail / 0 error
**版本**：app.js ?v=20260726cq → ?v=20260726cr（仅图片变更，bump版本强制缓存刷新）

## (cs) 员工信息弹窗：现任老板/最栽培老板显示修复 (09:59)
**现象**：员工信息弹窗中"现任老板"显示"他还没有老板"、"最栽培Ta的老板"显示"他还没有被雇佣过"，即使玩家已雇佣该员工且员工空闲时也如此
**根因**：`showBoss = busy && c`（第1677行）— 只有员工「工作中」才显示老板信息，空闲时直接走空状态
**修复**（app.js）：
1. `showBoss` 改为 `true` — 始终显示老板信息区域，有数据就渲染
2. 新增 `nwContributors: { [uid]: totalNwGain }` 字段追踪每个老板对员工身价的累计贡献
3. 安排工作时身价增长（`emp.networth += nwGain`）同步记录到 `nwContributors[employerUid]` 并更新 `topHirerUid` 为贡献最大者
4. v12 迁移：旧员工补齐 `nwContributors` 和 `topHirerUid`
5. 「现任老板」= `hiredFrom` 对应的玩家/好友；「最栽培Ta的老板」= `nwContributors` 中总贡献最大的老板
验证：74 pass / 7 fail / 0 error
**版本**：app.js ?v=20260726cr → ?v=20260726cs

## (ct) BUG修复：刷新页面后车位员工头像消失 (10:05)
**现象**：安排员工工作后，点击浏览器刷新按钮，车位卡片上的员工工作状态头像（车图叠加+收入区）消失。反复出现多次
**根因**：css/style.css 第179行 `.pc-driver-overlay { display: none !important; }` — 早期开发时临时禁用车图驾驶员指示器，注释写「已隐藏（无员工工资概念）」。功能上线后忘记删除，`!important` 导致第235行正常样式（display:flex）无法覆盖，员工头像永远不可见
**修复**（css）：删除第179行的 `display: none !important` 规则
验证：74 pass / 7 fail / 0 error
**版本**：css ?v=20260726cq → ?v=20260726ct
