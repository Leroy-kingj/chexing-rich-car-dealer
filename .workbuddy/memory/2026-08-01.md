# 2026-08-01 工作日志

## 打包更新（tt18）— 抢车位：华夏崛起
- 实现上一轮 4 个 bug 修复（app.js / css/style.css / index.html）：
  1. **头像重叠**：`css/style.css` 新增 `.res-avatar.has-real-avatar .res-avatar-emoji{display:none}`（有真实头像时隐藏默认👤）；员工老板位(isMe)与我的信息弹窗/排行「我的行」统一用 `S.avatar`。
  2. **移除「真实玩家」文本**：推荐车友 `nf-tap-badge` 与 `addFriend` 关注 toast 去掉该文案（仅留代码注释）。
  3. **好友车位解锁**：`addFriend`/`acceptFriend` 内加 `checkFspotUnlock()` + `refreshFspotGrid()`（原仅启动时检查，导致达标后仍锁）。
  4. **全服榜补机器人**：`listData` 由 `[...TAP_PLAYER_POOL, meRow]` 改为 `[...BOT_POOL, ...TAP_PLAYER_POOL, meRow]`（及空榜时 `[...BOT_POOL, meRow]`）。
- 版本号 `tt17 → tt18`（`index.html` 4 处 `?v=`）。
- `h5-build/` 与根目录同步（md5 全部一致）。
- Git：提交 `72bbe82`，SSH 推送 `master`（触发 GitHub Actions 构建 qcwhxjq.apk）。

## ⚠️ 安全事件：GitHub PAT 泄露导致推送被拦截
- `.workbuddy/memory/2026-07-28.md:465` 含明文 GitHub PAT `ghp_yOc34...jc9`（用户早前提供，用于 curl 下载 APK 构件）。
- 首次 `git push` 被 GitHub secret scanning 拦截（remote rejected，push declined due to repository rule violations）。
- 处理：脱敏该行（改为 `[REDACTED_GITHUB_PAT]`）+ 脱敏 `2026-07-27.md:775` 的截断引用，`git commit --amend` 后重推成功。
- **待办：请用户在 GitHub Settings → Developer settings → Personal access tokens 作废该 PAT**（已写入仓库历史/工作树，存在泄露风险）。教训：memory 笔记不要再粘贴真实 token。

## TapTap H5 上传（895103）
- 安全流程验证：`get_current_app_info` 再次显示缓存被静默篡改为 `897266`（扫雷：无尽）→ 立即 `select_app(430680,895103)` → `prepare_h5_upload(qc_stage)` 确认「抢车位：华夏崛起 (ID: 895103)」→ `upload_h5_game` → `get_app_status` 真实状态「已上线(1)/审核状态已上线(4)」（快速上架，不进人工审核，开发者可立即自测）。
- 已清理 `双响炮/qc_stage` 暂存。
